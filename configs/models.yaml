# LLM 与 Embedding 配置

llm:
  default_model: "gpt-4"
  models:
    - name: "gpt-4"
      provider: "poe"
      model_id: "gpt-4"
    - name: "default"
      provider: "poe"
      model_id: "gpt-4"

embedding:
  provider: "openai"
  model_id: "text-embedding-ada-002"
