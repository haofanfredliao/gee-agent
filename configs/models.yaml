# LLM 与 Embedding 配置

llm:
  default_model: "gemini-3.1-flash-lite"
  models:
    - name: "gemini"
      provider: "poe"
      model_id: "gemini-3.1-flash-lite"
    - name: "default"
      provider: "poe"
      model_id: "gemini-3.1-flash-lite"

embedding:
  provider: "openai"
  model_id: "text-embedding-ada-002"
