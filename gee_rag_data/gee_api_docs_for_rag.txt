## ee.Algorithms.CannyEdgeDetector
Applies the Canny edge detection algorithm to an image. The output is an image whose bands have the same names as the input bands, and in which non-zero values indicate edges, and the magnitude of the value is the gradient magnitude.

===

## ee.Algorithms.Collection
Returns a Collection containing the specified features.

===

## ee.Algorithms.CrossCorrelation
Gives information on the quality of image registration between two (theoretically) co-registered images. The input is two images with the same number of bands. This function outputs an image composed of four bands of information. The first three are distances: the deltaX, deltaY, and the Euclidean distance for each pixel in imageA to the pixel which has the highest corresponding correlation coefficient in imageB. The fourth band is the value of the correlation coefficient for that pixel [-1 : +1].

===

## ee.Algorithms.Date
Creates a Date.

===

## ee.Algorithms.Describe
Describes an object using a simple JSON-compatible structure.

===

## ee.Algorithms.Dictionary
Constructs a dictionary.

===

## ee.Algorithms.FMask.fillMinima
Fills local minima. Only works on INT types.

===

## ee.Algorithms.FMask.matchClouds
Runs the FMask cloud and shadow matching. Outputs a single band ('csm'), containing the computed cloud and shadow masks.

===

## ee.Algorithms.Feature
Returns a Feature composed of the given geometry and metadata.

===

## ee.Algorithms.GeometryConstructors.BBox
Constructs a rectangle whose edges are lines of latitude and longitude. The result is a planar WGS84 rectangle. If (east - west) ≥ 360 then the longitude range will be normalized to -180 to +180; otherwise they will be treated as designating points on a circle (e.g., east may be numerically less than west).

===

## ee.Algorithms.GeometryConstructors.LineString
Constructs a LineString from the given coordinates.

===

## ee.Algorithms.GeometryConstructors.LinearRing
Constructs a LinearRing from the given coordinates, automatically adding the first point at the end if the ring is not explicitly closed.

===

## ee.Algorithms.GeometryConstructors.MultiGeometry
Constructs a MultiGeometry from the given list of geometry elements.

===

## ee.Algorithms.GeometryConstructors.MultiLineString
Constructs a MultiLineString from the given coordinates.

===

## ee.Algorithms.GeometryConstructors.MultiPoint
Constructs a MultiPoint from the given coordinates.

===

## ee.Algorithms.GeometryConstructors.MultiPolygon
Constructs a MultiPolygon from the given coordinates.

===

## ee.Algorithms.GeometryConstructors.Point
Constructs a new Point from the given x,y coordinates.

===

## ee.Algorithms.GeometryConstructors.Polygon
Constructs a Polygon from the given coordinates.

===

## ee.Algorithms.GeometryConstructors.Rectangle
Constructs a rectangular polygon from the given corner points.

===

## ee.Algorithms.HillShadow
Creates a shadow band, with output 1 where pixels are illumunated and 0 where they are shadowed. Takes as input an elevation band, azimuth and zenith of the light source in degrees, a neighborhood size, and whether or not to apply hysteresis when a shadow appears. Currently, this algorithm only works for Mercator projections, in which light rays are parallel.

===

## ee.Algorithms.HoughTransform
Applies the Hough transform to an image. For every input band, outputs a band where lines are detected by thresholding the Hough transform with a value of lineThreshold. The output band is named [input]_lines, where [input] is the name of the original band. The defaults provided for the parameters are intended as a starting point for use with UINT8 images.

===

## ee.Algorithms.If
Selects one of its inputs based on a condition, similar to an if-then-else construct.

===

## ee.Algorithms.Image.Segmentation.GMeans
Performs G-Means clustering on the input image. Iteratively applies k-means followed by a normality test to automatically determine the number of clusters to use. The output contains a 'clusters' band containing the integer ID of the cluster that each pixel belongs to. The algorithm can work either on a fixed grid of non-overlapping cells (gridSize, which can be smaller than a tile) or on tiles with overlap (neighborhoodSize). The default is to use tiles with no overlap. Clusters in one cell or tile are unrelated to clusters in another. Any cluster that spans a cell or tile boundary may receive two different labels in the two halves. Any input pixels with partial masks are fully masked in the output. This algorithm is only expected to perform well for images with a narrow dynamic range (i.e., bytes or shorts). See: G. Hamerly and C. Elkan. 'Learning the k in k-means'. NIPS, 2003.

===

## ee.Algorithms.Image.Segmentation.KMeans
Performs K-Means clustering on the input image. Outputs a 1-band image containing the ID of the cluster that each pixel belongs to. The algorithm can work either on a fixed grid of non-overlapping cells (gridSize, which can be smaller than a tile) or on tiles with overlap (neighborhoodSize). The default is to use tiles with no overlap. Clusters in one cell or tile are unrelated to clusters in another. Any cluster that spans a cell or tile boundary may receive two different labels in the two halves. Any input pixels with partial masks are fully masked in the output.

===

## ee.Algorithms.Image.Segmentation.SNIC
Superpixel clustering based on SNIC (Simple Non-Iterative Clustering). Outputs a band of cluster IDs and the per-cluster averages for each of the input bands. If the 'seeds' image isn't provided as input, the output will include a 'seeds' band containing the generated seed locations. See: Achanta, Radhakrishna and Susstrunk, Sabine, 'Superpixels and Polygons using Simple Non-Iterative Clustering', CVPR, 2017.

===

## ee.Algorithms.Image.Segmentation.seedGrid
Selects seed pixels for clustering.

===

## ee.Algorithms.IsEqual
Returns whether two objects are equal.

===

## ee.Algorithms.Landsat.TOA
Calibrates Landsat DN to TOA reflectance and brightness temperature for Landsat and similar data. For recently-acquired scenes calibration coefficients are extracted from the image metadata; for older scenes the coefficients are derived from: Chander, Gyanesh, Brian L. Markham, and Dennis L. Helder. "Summary of current radiometric calibration coefficients for Landsat MSS, TM, ETM+, and EO-1 ALI sensors." Remote sensing of environment 113.5 (2009): 893-903.

===

## ee.Algorithms.Landsat.calibratedRadiance
Calibrates each band of an image by applying linear transformation with slope RADIANCE_MULT_BAND_N and y-intercept RADIANCE_ADD_BAND_N ; these values are extracted from the image metadata.

===

## ee.Algorithms.Landsat.pathRowLimit
Limits requests to an ImageCollection of Landsat scenes to return a controllable number of the best scenes for each request. This is intended for use with statistical algorithms like median composites that need a certain amount of good data to perform well, but that do not benefit substantially from additional data beyond that while becoming needlessly expensive. The default arguments select approximately one year's worth of good data. Note that in rare circumstances, when the tile boundary aligns with a Landsat WRS cell boundary, queries for adjacent tiles may yield conflicting results. This is why it is important that this algorithm only be used with statistical methods that can tolerate these inconsistencies.

===

## ee.Algorithms.Landsat.simpleCloudScore
Computes a simple cloud-likelihood score in the range [0,100] using a combination of brightness, temperature, and NDSI. This is not a robust cloud detector, and is intended mainly to compare multiple looks at the same point for relative cloud likelihood.

===

## ee.Algorithms.Landsat.simpleComposite
Computes a Landsat TOA composite from a collection of raw Landsat scenes. It applies standard TOA calibration and then assigns a cloud score to each pixel using the SimpleLandsatCloudScore algorithm. It selects the lowest possible range of cloud scores at each point and then computes per-band percentile values from the accepted pixels. This algorithm also uses the LandsatPathRowLimit algorithm to select only the least-cloudy scenes in regions where more than maxDepth input scenes are available.

===

## ee.Algorithms.ObjectType
Returns a string representing the type of the given object.

===

## ee.Algorithms.ProjectionTransform
Transforms the geometry of a feature to a specific projection.

===

## ee.Algorithms.Sentinel2.CDI
Computes the Cloud Displacement Index (CDI) from a Sentinel-2 Level 1C image. CDI is a measure of the optical separation in elevated objects due to sensor parallax. Returns a floating point band named "cdi". See Frantz, D., Hass, E., Uhl, A., Stoffels, J., & Hill, J. (2018). Improvement of the Fmask algorithm for Sentinel-2 images: Separating clouds from bright surfaces based on parallax effects. Remote sensing of environment, 215, 471-481.

===

## ee.Algorithms.String
Converts the input to a string.

===

## ee.Algorithms.TemporalSegmentation.C2c
An implementation of the Composite 2 Change (C2C) algorithm. This algorithm segments a time series using a piecewise linear fit with the minimum of segments required to fit the data within the given maximum root mean squared error (RMSE).  For every band given the algorithm will return the following bands: changeDate:A 1D array of doubles representing pairs of start and end dates for each fitted segment. The date format is determined by the dateFormat argument. value: A 1D array of doubles of the value of the band at the changeDate. magnitude: A 1D array of doubles providing the absolute difference between the values before and after a change date.  The first magnitude is always NaN. duration: A 1D array of doubles of the duration of the segment preceding the change date.  The first duration is always NaN. rate: A 1D array of doubles of the rate of change of the data preceding the. change date. The first rate is always NaN. postMagnitude: A 1D array of doubles of the absolute difference between the values after the change date and the value at the change date.  The last postMagnitude is always NaN. postDuration: The duration of the segment following the change date. The last postDuration is always NaN. postRate: The rate of change of the data following the change date. The last postRate is always NaN. indexRegrwoth: The difference between the value in change date and the value five data points after. recoveryIndicator: The indexRegrowth/magnitude ratio. regrowth60: Time difference between breakpoint and the  data point where the series value is 60% of the pre-distrubance value. regrowth60: Time difference between breakpoint and the  data point where the series value is 80% of the pre-distrubance value. regrowth60: Time difference between breakpoint and the  data point where the series value is 100% of the pre-distrubance value. See: Hermosilla et al. (2015) dx.doi.org/10.1016/j.rse.2014.11.005 for further details on the original algorithm. Algorithm implementation can be found on GitHub: https://github.com/saveriofrancini/C2C-GEE Acknowlegements: FORWARDS and NextGenCarbon. Citation: Txomin Hermosilla, Michael A. Wulder, Joanne C. White, Nicholas C. Coops, Daniel Coelho, Giovanni Ciatto, Noel Gorelick, and Saverio Francini. In preparation. Image compositing, time-series change detection and temporal metrics: Implementation of the Composite2Change (C2C) algorithm on Google Earth Engine.   This algorithm is in preview and is subject to change.

===

## ee.Algorithms.TemporalSegmentation.Ccdc
Implements the Continuous Change Detection and Classification temporal breakpoint algorithm. This algorithm finds temporal breakpoints in an image collection by iteratively fitting harmonic functions to the data. Fit coefficients are produced for all input bands, but the bands used for breakpoint detection can be specified with the 'breakpointBands' argument. For more details, see Zhu, Z. and Woodcock, C.E., 2014. Continuous change detection and classification of land cover using all available Landsat data. Remote sensing of Environment, 144, pp.152-171.

===

## ee.Algorithms.TemporalSegmentation.Ewmacd
Exponentially Weighted Moving Average Change Detection. This algorithm computes a harmonic model for the 'training' portion of the input data and subtracts that from the original results. The residuals are then subjected to Shewhart X-bar charts and an exponentially weighted moving average. Disturbed pixels are indicated when the charts signal a deviation from the given control limits. The output is a 5 band image containing the bands: ewma: a 1D array of the EWMA score for each input image. Negative values represent disturbance and positive values represent recovery. harmonicCoefficients: A 1-D array of the computed harmonic coefficient pairs. The coefficients are ordered as [constant, sin0, cos0, sin1, cos1...] rmse: the RMSE from the harmonic regression. rSquared: r-squared value from the harmonic regression. residuals: 1D array of residuals from the harmonic regression. See: Brooks, E.B., Wynne, R.H., Thomas, V.A., Blinn, C.E. and Coulston, J.W., 2014. On-the-fly massively multitemporal change detection using statistical quality control charts and Landsat data. IEEE Transactions on Geoscience and Remote Sensing, 52(6), pp.3316-3332.

===

## ee.Algorithms.TemporalSegmentation.LandTrendr
Landsat-based detection of Trends in Disturbance and Recovery: temporally segments a time-series of images by extracting the spectral trajectories of change over time. The first band of each image is used to find breakpoints, and those breakpoints are used to perform fitting on all subsequent bands. The breakpoints are returned as a 2-D matrix of 4 rows and as many columns as images. The first two rows are the original X and Y values. The third row contains the Y values fitted to the estimated segments, and the 4th row contains a 1 if the corresponding point was used as a segment vertex or 0 if not. Any additional fitted bands are appended as rows in the output. Breakpoint fitting assumes that increasing values represent disturbance and decreasing values represent recovery. See: Kennedy, R.E., Yang, Z. and Cohen, W.B., 2010. Detecting trends in forest disturbance and recovery using yearly Landsat time series: 1. LandTrendr - Temporal segmentation algorithms. Remote Sensing of Environment, 114(12), pp.2897-2910.

===

## ee.Algorithms.TemporalSegmentation.LandTrendrFit
Interpolates a time series using a set of LandTrendr breakpoint years. For each input band in the timeSeries, outputs a new 1D array-valued band containing the input values interpolated between the breakpoint times identified by the vertices image. See the LandTrendr Algorithm for more details.

===

## ee.Algorithms.TemporalSegmentation.StructuralChangeBreakpoints
Runs breakpoint detection, similar to R's strucchange::breakpoints function. Each pixel is fit by a piecewise linear/harmonic model, of the form Y = A + B * t + C * cos(2 * pi * season(t)) + D * sin(2 * pi * season(t)) + E * cos(4 * pi * season(t)) + F * sin(4 * pi * season(t)) + ... In this equation, 't' is the start time of the image in the format specified by 'dateFormat', and 'season(t)' is the fractional year of that start time (see the description of dateFormat for details). The maximum order of the harmonic terms is determined by 'seasonalModelOrder'. The result is an image containing two bands, plus two bands per band in the input: tStart , tEnd : each of these holds a 1D array, with one entry per segment in the piecewise linear fit; each entry contains the start time of the first or last images in that segment. By default the values here are in fractional years, for easy use with the coefficients. coefs_BANDNAME : there will be one such output band per input band. Each of these holds a 2D array, with one row per segment. The values in that row are the coefficients of the linear fit for that segment - that is, the values of A, B, C, ... for that segment. As described above, the values here are affected by 'dateFormat' . rmse_BANDNAME : there will be one such output band per input band. This holds a 1D array, with one entry per segment. The value for each segment is the RMSE for the linear fit residuals for that segment.

===

## ee.Algorithms.TemporalSegmentation.VCT
Vegetation Change Tracker, an automated approach for reconstructing recent forest disturbance history using dense Landsat time series stacks. The output is a 2D array per pixel containing 6 rows x N years. The output rows contain: input years, VCT landcover mask, magnitude in term of the UD composite, magnitude of distubance in B4, magnitude of distubance in NDVI, magnitude of distubance in dNBR. See: Huang, C., Goward, S.N., Masek, J.G., Thomas, N., Zhu, Z. and Vogelmann, J.E., 2010. An automated approach for reconstructing recent forest disturbance history using dense Landsat time series stacks. Remote Sensing of Environment, 114(1), pp.183-198.

===

## ee.Algorithms.TemporalSegmentation.Verdet
Vegetation Regeneration and Disturbance Estimates through Time, forest change detection algorithm. This algorithm generates a yearly clear-sky composite from satellite imagery, calculates a spectral vegetation index for each pixel in that composite, spatially segments the vegetation index image into patches, temporally divides the time series into differently sloped segments, and then labels those segments as disturbed, stable, or regenerating. Segmentation at both the spatial and temporal steps are performed using total variation regularization. The output consists of a 1D array per pixel containing the slope of fitted trend lines. Negative values indicate disturbance and positive values regeneration. See: Hughes, M.J., Kaylor, S.D. and Hayes, D.J., 2017.  Patch-based forest change detection from Landsat time series. Forests, 8(5), p.166.

===

## ee.Algorithms.Terrain
Calculates slope, aspect, and a simple hillshade from a terrain DEM. Expects an image containing either a single band of elevation, measured in meters, or if there's more than one band, one named 'elevation'. Adds output bands named 'slope' and 'aspect' measured in degrees plus an unsigned byte output band named 'hillshade' for visualization. All other bands and metadata are copied from the input image. The local gradient is computed using the 4-connected neighbors of each pixel, so missing values will occur around the edges of an image.

===

## ee.Array
Returns an array with the given coordinates.

===

## ee.Array.abs
On an element-wise basis, computes the absolute value of the input.

===

## ee.Array.accum
Accumulates elements of an array along the given axis, by setting each element of the result to the reduction of elements along that axis up to and including the current position. May be used to make a cumulative sum, a monotonically increasing sequence, etc.

===

## ee.Array.acos
On an element-wise basis, computes the arccosine in radians of the input.

===

## ee.Array.add
On an element-wise basis, adds the first value to the second.

===

## ee.Array.and
On an element-wise basis, returns 1 if and only if both values are non-zero.

===

## ee.Array.argmax
Returns the position, as a list of indices in each array axis, of the maximum value in an array, or null if the array is empty. If there are multiple occurrences of the maximum, returns the position of the first.

===

## ee.Array.asin
On an element-wise basis, computes the arcsine in radians of the input.

===

## ee.Array.atan
On an element-wise basis, computes the arctangent in radians of the input.

===

## ee.Array.atan2
On an element-wise basis, calculates the angle formed by the 2D vector [x, y].

===

## ee.Array.bitCount
On an element-wise basis, calculates the number of one-bits in the 64-bit two's complement binary representation of the input.

===

## ee.Array.bitsToArray
Converts the bits of an integer to an Array. The array has as many elements as the position of the highest set bit, or a single 0 for a value of 0.

===

## ee.Array.bitwiseAnd
On an element-wise basis, calculates the bitwise AND of the input values.

===

## ee.Array.bitwiseNot
On an element-wise basis, calculates the bitwise NOT of the input, in the smallest signed integer type that can hold the input.

===

## ee.Array.bitwiseOr
On an element-wise basis, calculates the bitwise OR of the input values.

===

## ee.Array.bitwiseXor
On an element-wise basis, calculates the bitwise XOR of the input values.

===

## ee.Array.byte
On an element-wise basis, casts the input value to an unsigned 8-bit integer.

===

## ee.Array.cat
Concatenates multiple arrays into a single array along the given axis. Each array must have the same dimensionality and the same length on all axes except the concatenation axis.

===

## ee.Array.cbrt
On an element-wise basis, computes the cubic root of the input.

===

## ee.Array.ceil
On an element-wise basis, computes the smallest integer greater than or equal to the input.

===

## ee.Array.cos
On an element-wise basis, computes the cosine of the input in radians.

===

## ee.Array.cosh
On an element-wise basis, computes the hyperbolic cosine of the input.

===

## ee.Array.cut
Cut an array along one or more axes.

===

## ee.Array.digamma
On an element-wise basis, computes the digamma function of the input.

===

## ee.Array.divide
On an element-wise basis, divides the first value by the second, returning 0 for division by 0.

===

## ee.Array.dotProduct
Compute the dot product between two 1-D arrays.

===

## ee.Array.double
On an element-wise basis, casts the input value to a 64-bit float.

===

## ee.Array.eigen
Computes the real eigenvectors and eigenvalues of a square 2D array of A rows and A columns. Returns an array with A rows and A+1 columns, where each row contains an eigenvalue in the first column, and the corresponding eigenvector in the remaining A columns. The rows are sorted by eigenvalue, in descending order. This implementation uses DecompositionFactory.eig() from https://ejml.org.

===

## ee.Array.eq
On an element-wise basis, returns 1 if and only if the first value is equal to the second.

===

## ee.Array.erf
On an element-wise basis, computes the error function of the input.

===

## ee.Array.erfInv
On an element-wise basis, computes the inverse error function of the input.

===

## ee.Array.erfc
On an element-wise basis, computes the complementary error function of the input.

===

## ee.Array.erfcInv
On an element-wise basis, computes the inverse complementary error function of the input.

===

## ee.Array.exp
On an element-wise basis, computes the Euler's number e raised to the power of the input.

===

## ee.Array.first
On an element-wise basis, selects the value of the first value.

===

## ee.Array.firstNonZero
On an element-wise basis, selects the first value if it is non-zero, and the second value otherwise.

===

## ee.Array.float
On an element-wise basis, casts the input value to a 32-bit float.

===

## ee.Array.floor
On an element-wise basis, computes the largest integer less than or equal to the input.

===

## ee.Array.gamma
On an element-wise basis, computes the gamma function of the input.

===

## ee.Array.gammainc
On an element-wise basis, calculates the regularized lower incomplete Gamma function γ(x,a).

===

## ee.Array.get
Extracts the value at the given position from the input array.

===

## ee.Array.gt
On an element-wise basis, returns 1 if and only if the first value is greater than the second.

===

## ee.Array.gte
On an element-wise basis, returns 1 if and only if the first value is greater than or equal to the second.

===

## ee.Array.hypot
On an element-wise basis, calculates the magnitude of the 2D vector [x, y].

===

## ee.Array.identity
Creates a 2D identity matrix of the given size.

===

## ee.Array.int
On an element-wise basis, casts the input value to a signed 32-bit integer.

===

## ee.Array.int16
On an element-wise basis, casts the input value to a signed 16-bit integer.

===

## ee.Array.int32
On an element-wise basis, casts the input value to a signed 32-bit integer.

===

## ee.Array.int64
On an element-wise basis, casts the input value to a signed 64-bit integer.

===

## ee.Array.int8
On an element-wise basis, casts the input value to a signed 8-bit integer.

===

## ee.Array.lanczos
On an element-wise basis, computes the Lanczos approximation of the input.

===

## ee.Array.leftShift
On an element-wise basis, calculates the left shift of v1 by v2 bits.

===

## ee.Array.length
Returns a 1-D ee.Array containing the length of each dimension of the given ee.Array.

===

## ee.Array.log
On an element-wise basis, computes the natural logarithm of the input.

===

## ee.Array.log10
On an element-wise basis, computes the base-10 logarithm of the input.

===

## ee.Array.long
On an element-wise basis, casts the input value to a signed 64-bit integer.

===

## ee.Array.lt
On an element-wise basis, returns 1 if and only if the first value is less than the second.

===

## ee.Array.lte
On an element-wise basis, returns 1 if and only if the first value is less than or equal to the second.

===

## ee.Array.mask
Creates a subarray by slicing out each position in an input array that is parallel to a non-zero element of the given mask array.

===

## ee.Array.matrixCholeskyDecomposition
Calculates the Cholesky decomposition of a matrix. The Cholesky decomposition is a decomposition into the form L * L' where L is a lower triangular matrix. The input must be a symmetric positive-definite matrix. Returns a dictionary with 1 entry named 'L'.

===

## ee.Array.matrixDeterminant
Computes the determinant of the matrix.

===

## ee.Array.matrixDiagonal
Computes the diagonal of the matrix in a single column.

===

## ee.Array.matrixFnorm
Computes the Frobenius norm of the matrix.

===

## ee.Array.matrixInverse
Computes the inverse of the matrix.

===

## ee.Array.matrixLUDecomposition
Calculates the LU matrix decomposition such that P×input=L×U, where L is lower triangular (with unit diagonal terms), U is upper triangular and P is a partial pivot permutation matrix. The input matrix must be square. Returns a dictionary with entries named 'L', 'U' and 'P'.

===

## ee.Array.matrixMultiply
Returns the matrix multiplication A * B.

===

## ee.Array.matrixPseudoInverse
Computes the Moore-Penrose pseudoinverse of the matrix.

===

## ee.Array.matrixQRDecomposition
Calculates the QR-decomposition of a matrix into two matrices Q and R such that input = QR, where Q is orthogonal, and R is upper triangular. Returns a dictionary with entries named 'Q' and 'R'.

===

## ee.Array.matrixSingularValueDecomposition
Calculates the Singular Value Decomposition of the input matrix into U×S×V', such that U and V are orthogonal and S is diagonal. Returns a dictionary with entries named 'U', 'S' and 'V'.

===

## ee.Array.matrixSolve
Solves for x in the matrix equation A * x = B, finding a least-squares solution if A is overdetermined.

===

## ee.Array.matrixToDiag
Computes a square diagonal matrix from a single column matrix.

===

## ee.Array.matrixTrace
Computes the trace of the matrix.

===

## ee.Array.matrixTranspose
Transposes two dimensions of an array.

===

## ee.Array.max
On an element-wise basis, selects the maximum of the first and second values.

===

## ee.Array.min
On an element-wise basis, selects the minimum of the first and second values.

===

## ee.Array.mod
On an element-wise basis, calculates the remainder of the first value divided by the second.

===

## ee.Array.multiply
On an element-wise basis, multiplies the first value by the second.

===

## ee.Array.neq
On an element-wise basis, returns 1 if and only if the first value is not equal to the second.

===

## ee.Array.not
On an element-wise basis, returns 0 if the input is non-zero, and 1 otherwise.

===

## ee.Array.or
On an element-wise basis, returns 1 if and only if either input value is non-zero.

===

## ee.Array.pad
Pad an array to a given length. The pad value will be repeatedly appended to the array to extend it to given length along each axis. If the array is already as large or larger than a given length, it will remain unchanged along that axis.

===

## ee.Array.pow
On an element-wise basis, raises the first value to the power of the second.

===

## ee.Array.project
Projects an array to a lower dimensional space by specifying the axes to retain. Dropped axes must be at most length 1.

===

## ee.Array.reduce
Apply a reducer to an array by collapsing all the input values along each specified axis into a single output value computed by the reducer. The output always has the same dimensionality as the input, and the individual axes are affected as follows: The axes specified in the 'axes' parameter have their length reduced to 1 (by applying the reducer). If the reducer has multiple inputs or multiple outputs, the axis specified in 'fieldAxis' will be used to provide the reducer's inputs and store the reducer's outputs. All other axes are unaffected (independent reductions are performed).

===

## ee.Array.repeat
Repeats the array along the given axis. The result will have the shape of the input, except length along the repeated axis will be multiplied by the given number of copies.

===

## ee.Array.reshape
Reshapes an array to a new list of dimension lengths.

===

## ee.Array.rightShift
On an element-wise basis, calculates the signed right shift of v1 by v2 bits.

===

## ee.Array.round
On an element-wise basis, computes the integer nearest to the input.

===

## ee.Array.short
On an element-wise basis, casts the input value to a signed 16-bit integer.

===

## ee.Array.signum
On an element-wise basis, computes the signum function (sign) of the input; 0 if the input is 0, 1 if the input is greater than 0, -1 if the input is less than 0.

===

## ee.Array.sin
On an element-wise basis, computes the sine of the input in radians.

===

## ee.Array.sinh
On an element-wise basis, computes the hyperbolic sine of the input.

===

## ee.Array.slice
Creates a subarray by slicing out each position along the given axis from the 'start' (inclusive) to 'end' (exclusive) by increments of 'step'. The result will have as many dimensions as the input, and the same length in all directions except the slicing axis, where the length will be the number of positions from 'start' to 'end' by 'step' that are in range of the input array's length along 'axis'. This means the result can be length 0 along the given axis if start=end, or if the start or end values are entirely out of range.

===

## ee.Array.sort
Sorts elements of the array along one axis.

===

## ee.Array.sqrt
On an element-wise basis, computes the square root of the input.

===

## ee.Array.subtract
On an element-wise basis, subtracts the second value from the first.

===

## ee.Array.tan
On an element-wise basis, computes the tangent of the input in radians.

===

## ee.Array.tanh
On an element-wise basis, computes the hyperbolic tangent of the input.

===

## ee.Array.toByte
On an element-wise basis, casts the input value to an unsigned 8-bit integer.

===

## ee.Array.toDouble
On an element-wise basis, casts the input value to a 64-bit float.

===

## ee.Array.toFloat
On an element-wise basis, casts the input value to a 32-bit float.

===

## ee.Array.toInt
On an element-wise basis, casts the input value to a signed 32-bit integer.

===

## ee.Array.toInt16
On an element-wise basis, casts the input value to a signed 16-bit integer.

===

## ee.Array.toInt32
On an element-wise basis, casts the input value to a signed 32-bit integer.

===

## ee.Array.toInt64
On an element-wise basis, casts the input value to a signed 64-bit integer.

===

## ee.Array.toInt8
On an element-wise basis, casts the input value to a signed 8-bit integer.

===

## ee.Array.toList
Turns an Array into a list of lists of numbers.

===

## ee.Array.toLong
On an element-wise basis, casts the input value to a signed 64-bit integer.

===

## ee.Array.toShort
On an element-wise basis, casts the input value to a signed 16-bit integer.

===

## ee.Array.toUint16
On an element-wise basis, casts the input value to an unsigned 16-bit integer.

===

## ee.Array.toUint32
On an element-wise basis, casts the input value to an unsigned 32-bit integer.

===

## ee.Array.toUint8
On an element-wise basis, casts the input value to an unsigned 8-bit integer.

===

## ee.Array.transpose
Transposes two dimensions of an array.

===

## ee.Array.trigamma
On an element-wise basis, computes the trigamma function of the input.

===

## ee.Array.uint16
On an element-wise basis, casts the input value to an unsigned 16-bit integer.

===

## ee.Array.uint32
On an element-wise basis, casts the input value to an unsigned 32-bit integer.

===

## ee.Array.uint8
On an element-wise basis, casts the input value to an unsigned 8-bit integer.

===

## ee.Blob
Loads a Blob from a Google Cloud Storage URL.

===

## ee.Blob.string
Returns the contents of the blob as a String.

===

## ee.Blob.url
Returns the Blob's Google Cloud Storage URL. The bucket metadata must be accessible (requires the storage.buckets.get permission which is provided by the role "Storage Legacy Bucket Reader" among others, see https://cloud.google.com/storage/docs/access-control/iam-roles) and the bucket must be located in the US multi-region, a dual-region including US-CENTRAL1, or theUS-CENTRAL1 region.

===

## ee.Classifier.amnhMaxent
Creates a Maximum Entropy classifier. Maxent is used to model species distribution probabilities using environmental data for locations of known presence and for a large number of 'background' locations. For more information and to cite, see: https://biodiversityinformatics.amnh.org/open_source/maxent/ and the reference publication: Phillips, et. al., 2004 A maximum entropy approach to species distribution modeling, Proceedings of the Twenty-First International Conference on Machine Learning. The output is a single band named 'probability', containing the modeled probability, and an additional band named 'clamp' when the 'writeClampGrid' argument is true.

===

## ee.Classifier.confusionMatrix
Computes a 2D confusion matrix for a classifier based on its training data (e.g., resubstitution error). Axis 0 of the matrix corresponds to the input classes and axis 1 corresponds to the output classes. The rows and columns start at class 0 and increase sequentially up to the maximum class value, so some rows or columns might be empty if the input classes aren't 0-based or sequential.

===

## ee.Classifier.decisionTree
Creates a classifier that applies the given decision tree.

===

## ee.Classifier.decisionTreeEnsemble
Creates a classifier that applies the given decision trees.

===

## ee.Classifier.explain
Describe the results of a trained classifier.

===

## ee.Classifier.libsvm
Creates an empty Support Vector Machine classifier.

===

## ee.Classifier.load
Creates a Classifier.

===

## ee.Classifier.minimumDistance
Creates a minimum distance classifier for the given distance metric. In CLASSIFICATION mode, the nearest class is returned. In REGRESSION mode, the distance to the nearest class center is returned. In RAW mode, the distance to every class center is returned.

===

## ee.Classifier.mode
Returns the classifier mode: CLASSIFICATION , REGRESSION , PROBABILITY , MULTIPROBABILITY , RAW , or RAW_REGRESSION .

===

## ee.Classifier.schema
Returns the names of the inputs used by this classifier or null if this classifier has not had any training data added yet.

===

## ee.Classifier.setOutputMode
Sets a classifier's output format. Refer to https://developers.google.com/earth-engine/guides/classification for a list of supported modes for each classifier.

===

## ee.Classifier.smileCart
Creates an empty CART classifier. See: "Classification and Regression Trees," L. Breiman, J. Friedman, R. Olshen, C. Stone Chapman and Hall, 1984.

===

## ee.Classifier.smileGradientTreeBoost
Creates an empty Gradient Tree Boost classifier.

===

## ee.Classifier.smileKNN
Creates an empty k-NN classifier. The k-nearest neighbor algorithm (k-NN) is a method for classifying objects by a majority vote of its neighbors, with the object being assigned to the class most common amongst its k nearest neighbors (k is a positive integer, typically small, typically odd).

===

## ee.Classifier.smileNaiveBayes
Creates an empty Naive Bayes classifier. This classifier assumes that the feature vector consists of positive integers, and negative inputs are discarded.

===

## ee.Classifier.smileRandomForest
Creates an empty Random Forest classifier.

===

## ee.Classifier.spectralRegion
Creates a classifier that tests if its inputs lie within a polygon defined by a set of coordinates in an arbitrary 2D coordinate system. Each input to be classified must have 2 values (e.g., images must have 2 bands). The result will be 1 wherever the input values are contained within the given polygon and 0 otherwise.

===

## ee.Classifier.train
Trains the classifier on a collection of features, using the specified numeric properties of each feature as training data. The geometry of the features is ignored.

===

## ee.Clusterer.schema
Returns the names of the inputs used by this Clusterer, or null if this Clusterer has not had any training data added yet.

===

## ee.Clusterer.train
Trains the Clusterer on a collection of features using the specified numeric properties of each feature as training data. The geometry of the features is ignored.

===

## ee.Clusterer.wekaCascadeKMeans
Cascade simple k-means selects the best k according to the Calinski-Harabasz criterion. For more information see: Calinski, T. and J. Harabasz. 1974. A dendrite method for cluster analysis. Commun. Stat. 3: 1-27.

===

## ee.Clusterer.wekaCobweb
Implementation of the Cobweb clustering algorithm. For more information see: D. Fisher (1987). Knowledge acquisition via incremental conceptual clustering. Machine Learning. 2(2):139-172. and J. H. Gennari, P. Langley, D. Fisher (1990). Models of incremental concept formation. Artificial Intelligence. 40:11-61.

===

## ee.Clusterer.wekaKMeans
Cluster data using the k-means algorithm. Can use either the Euclidean distance (default) or the Manhattan distance. If the Manhattan distance is used, then centroids are computed as the component-wise median rather than mean. For more information see: D. Arthur, S. Vassilvitskii: k-means++: the advantages of careful seeding. In: Proceedings of the eighteenth annual ACM-SIAM symposium on Discrete algorithms, 1027-1035, 2007.

===

## ee.Clusterer.wekaLVQ
A Clusterer that implements the Learning Vector Quantization algorithm. For more details, see: T. Kohonen, "Learning Vector Quantization", The Handbook of Brain Theory and Neural Networks, 2nd Edition, MIT Press, 2003, pp. 631-634.

===

## ee.Clusterer.wekaXMeans
X-Means is K-Means with an efficient estimation of the number of clusters. For more information see: Dan Pelleg, Andrew W. Moore: X-means: Extending K-means with Efficient Estimation of the Number of Clusters. In: Seventeenth International Conference on Machine Learning, 727-734, 2000.

===

## ee.ConfusionMatrix
Creates a confusion matrix. Axis 0 (the rows) of the matrix correspond to the actual values, and Axis 1 (the columns) to the predicted values.

===

## ee.ConfusionMatrix.accuracy
Computes the overall accuracy of a confusion matrix defined as correct / total.

===

## ee.ConfusionMatrix.array
Returns a confusion matrix as an Array.

===

## ee.ConfusionMatrix.consumersAccuracy
Computes the consumer's accuracy (reliability) of a confusion matrix defined as (correct / total) for each row.

===

## ee.ConfusionMatrix.fscore
Computes the Fβ-score for the confusion matrix.

===

## ee.ConfusionMatrix.kappa
Computes the Kappa statistic for the confusion matrix.

===

## ee.ConfusionMatrix.order
Returns the name and order of the rows and columns of the matrix.

===

## ee.ConfusionMatrix.producersAccuracy
Computes the producer's accuracy of a confusion matrix defined as (correct / total) for each column.

===

## ee.Date
Constructs a new Date object.

===

## ee.Date.advance
Create a new Date by adding the specified units to the given Date.

===

## ee.Date.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Date.difference
Returns the difference between two Dates in the specified units; the result is floating-point and based on the average length of the unit.

===

## ee.Date.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Date.format
Convert a date to string.

===

## ee.Date.fromYMD
Returns a Date given year, month, day.

===

## ee.Date.get
Returns the specified unit of this date.

===

## ee.Date.getFraction
Returns this date's elapsed fraction of the specified unit (between 0 and 1).

===

## ee.Date.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Date.getRange
Returns a DateRange covering the unit of the specified type that contains this date, e.g., Date('2013-3-15').getRange('year') returns DateRange('2013-1-1', '2014-1-1').

===

## ee.Date.getRelative
Returns the specified (0-based) unit of this date relative to a larger unit, e.g., getRelative('day', 'year') returns a value between 0 and 365.

===

## ee.Date.millis
The number of milliseconds since 1970-01-01T00:00:00Z.

===

## ee.Date.parse
Parse a date string, given a string describing its format.

===

## ee.Date.serialize
Returns the serialized representation of this object.

===

## ee.Date.unitRatio
Returns the ratio of the length of one unit to the length of another, e.g., unitRatio('day', 'minute') returns 1440. Valid units are 'year', 'month', 'week', 'day', 'hour', 'minute', and 'second'.

===

## ee.Date.update
Create a new Date by setting one or more of the units of the given Date to a new value. If a timeZone is given the new value(s) is interpreted in that zone.

===

## ee.DateRange
Creates a DateRange with the given start (inclusive) and end (exclusive), which may be Dates, numbers (interpreted as milliseconds since 1970-01-01T00:00:00Z), or strings (such as '1996-01-01T08:00'). If 'end' is not specified, a 1-millisecond range starting at 'start' is created.

===

## ee.DateRange.contains
Returns true if the given Date or DateRange is within this DateRange.

===

## ee.DateRange.end
Returns the (exclusive) end of this DateRange.

===

## ee.DateRange.intersection
Returns a DateRange that contains all points in the intersection of this DateRange and another.

===

## ee.DateRange.intersects
Returns true if the given DateRange has at least one point in common with this DateRange.

===

## ee.DateRange.isEmpty
Returns true if this DateRange contains no dates (i.e. start >= end).

===

## ee.DateRange.isUnbounded
Returns true if this DateRange contains all dates.

===

## ee.DateRange.start
Returns the (inclusive) start of this DateRange.

===

## ee.DateRange.unbounded
Returns a DateRange that includes all possible dates. No arguments.

===

## ee.DateRange.union
Returns a DateRange that contains all points in the union of this DateRange and another.

===

## ee.Dictionary
Constructs a new Dictionary.

===

## ee.Dictionary.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Dictionary.combine
Combines two dictionaries. In the case of duplicate names, the output will contain the value of the second dictionary unless overwrite is false. Null values in both dictionaries are ignored / removed.

===

## ee.Dictionary.contains
Returns true if the dictionary contains the given key.

===

## ee.Dictionary.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Dictionary.fromLists
Construct a dictionary from two parallel lists of keys and values.

===

## ee.Dictionary.get
Extracts a named value from a dictionary. If the dictionary does not contain the given key, then defaultValue is returned, unless it is null.

===

## ee.Dictionary.getArray
Extracts a named array value from a dictionary.

===

## ee.Dictionary.getGeometry
Extracts a named geometry value from a dictionary.

===

## ee.Dictionary.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Dictionary.getNumber
Extracts a named number value from a dictionary.

===

## ee.Dictionary.getString
Extracts a named string value from a dictionary.

===

## ee.Dictionary.keys
Retrieve the keys of a dictionary as a list. The keys will be sorted in natural order.

===

## ee.Dictionary.map
Map an algorithm over a dictionary. The algorithm is expected to take 2 arguments, a key from the existing dictionary and the value it corresponds to, and return a new value for the given key. If the algorithm returns null, the key is dropped.

===

## ee.Dictionary.remove
Returns a dictionary with the specified keys removed.

===

## ee.Dictionary.rename
Rename elements in a dictionary.

===

## ee.Dictionary.select
Returns a dictionary with only the specified keys.

===

## ee.Dictionary.serialize
Returns the serialized representation of this object.

===

## ee.Dictionary.set
Set a value in a dictionary.

===

## ee.Dictionary.size
Returns the number of entries in a dictionary.

===

## ee.Dictionary.toArray
Returns numeric values of a dictionary as an array. If no keys are specified, all values are returned in the natural ordering of the dictionary's keys. The default 'axis' is 0.

===

## ee.Dictionary.toImage
Creates an image of constants from values in a dictionary. The bands of the image are ordered and named according to the names argument. If no names are specified, the bands are sorted alpha-numerically.

===

## ee.Dictionary.values
Returns the values of a dictionary as a list. If no keys are specified, all values are returned in the natural ordering of the dictionary's keys.

===

## ee.ErrorMargin
Returns an ErrorMargin of the given type with the given value.

===

## ee.Feature
Features can be constructed from one of the following arguments plus an optional dictionary of properties: - An ee.Geometry. - A GeoJSON Geometry. - A GeoJSON Feature. - A computed object: reinterpreted as a geometry if properties are specified, and as a feature if they aren't.

===

## ee.Feature.area
Returns the area of the feature's default geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Feature.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Feature.bounds
Returns a feature containing the bounding box of the geometry of a given feature.

===

## ee.Feature.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Feature.centroid
Returns a feature containing the point at the center of the highest-dimension components of the geometry of a feature. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Feature.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Feature.closestPoints
Returns a dictionary containing up to two entries representing a point on each input feature's geometry that is closest to the geometry of the other input. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Feature.containedIn
Returns true if and only if the geometry of one feature is contained in the geometry of another.

===

## ee.Feature.contains
Returns true if and only if the geometry of one feature contains the geometry of another.

===

## ee.Feature.convexHull
Returns the feature with the geometry replaced by the convex hull of the original geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Feature.copyProperties
Copies metadata properties from one element to another.

===

## ee.Feature.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Feature.difference
Returns a feature with the properties of the 'left' feature, and the geometry that results from subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Feature.disjoint
Returns true if and only if the feature geometries are disjoint.

===

## ee.Feature.dissolve
Returns a feature containing the union of the geometry of a feature. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Feature.distance
Returns the minimum distance between the geometries of two features.

===

## ee.Feature.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Feature.geometry
Returns the geometry of a given feature in a given projection.

===

## ee.Feature.get
Extract a property from a feature.

===

## ee.Feature.getArray
Extract a property from a feature.

===

## ee.Feature.getInfo
An imperative function that returns information about this feature via an AJAX call. Returns a description of the feature.

===

## ee.Feature.getMapId
An imperative function that returns a map ID and optional token, suitable for generating a Map overlay. Returns an object which may be passed to ee.data.getTileUrl or ui.Map.addLayer, including an additional 'image' field, containing a Collection.draw image wrapping a FeatureCollection containing this feature. Undefined if a callback was specified.

===

## ee.Feature.getNumber
Extract a property from a feature.

===

## ee.Feature.getString
Extract a property from a feature.

===

## ee.Feature.hersDescriptor
Creates a dictionary of Histogram Error Ring Statistic (HERS) descriptor arrays from square array properties of an element. The HERS radius is taken to be the array's (side_length - 1) / 2.

===

## ee.Feature.id
Returns the ID of a given element within a collection. Objects outside collections are not guaranteed to have IDs.

===

## ee.Feature.intersection
Returns a feature containing the intersection of the geometries of two features, with the properties of the left feature.

===

## ee.Feature.intersects
Returns true if and only if the feature geometries intersect.

===

## ee.Feature.length
Returns the length of the linear parts of the geometry of a given feature. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Feature.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry of a given feature. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Feature.propertyNames
Returns the names of properties on this element.

===

## ee.Feature.select
Selects properties from a feature by name or RE2-compatible regex and optionally renames them.

===

## ee.Feature.serialize
Returns the serialized representation of this object.

===

## ee.Feature.set
Overrides one or more metadata properties of an Element. Returns the element with the specified properties overridden.

===

## ee.Feature.setGeometry
Returns the feature with the geometry replaced by the specified geometry.

===

## ee.Feature.simplify
Simplifies the geometry of a feature to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Feature.symmetricDifference
Returns a feature containing the symmetric difference between geometries of two features.

===

## ee.Feature.toArray
Creates an array from the given properties of an object, which must all be numbers.

===

## ee.Feature.toDictionary
Extract properties from a feature as a dictionary.

===

## ee.Feature.transform
Transforms the geometry of a feature to a specific projection.

===

## ee.Feature.union
Returns a feature containing the union of the geometries of two features.

===

## ee.Feature.withinDistance
Returns true if and only if the geometries of two features are within a specified distance.

===

## ee.FeatureCollection
FeatureCollections can be constructed from the following arguments: - A string: assumed to be the name of a collection. - A single geometry. - A single feature. - A list of features. - A GeoJSON FeatureCollection - A computed object: reinterpreted as a collection.

===

## ee.FeatureCollection.aggregate_array
Aggregates over a given property of the objects in a collection, calculating a list of all the values of the selected property.

===

## ee.FeatureCollection.aggregate_count
Aggregates over a given property of the objects in a collection, calculating the number of non-null values of the property.

===

## ee.FeatureCollection.aggregate_count_distinct
Aggregates over a given property of the objects in a collection, calculating the number of distinct values for the selected property.

===

## ee.FeatureCollection.aggregate_first
Aggregates over a given property of the objects in a collection, calculating the property value of the first object in the collection.

===

## ee.FeatureCollection.aggregate_histogram
Aggregates over a given property of the objects in a collection, calculating a histogram of the selected property.

===

## ee.FeatureCollection.aggregate_max
Aggregates over a given property of the objects in a collection, calculating the maximum of the values of the selected property.

===

## ee.FeatureCollection.aggregate_mean
Aggregates over a given property of the objects in a collection, calculating the mean of the selected property.

===

## ee.FeatureCollection.aggregate_min
Aggregates over a given property of the objects in a collection, calculating the minimum of the values of the selected property.

===

## ee.FeatureCollection.aggregate_product
Aggregates over a given property of the objects in a collection, calculating the product of the values of the selected property.

===

## ee.FeatureCollection.aggregate_sample_sd
Aggregates over a given property of the objects in a collection, calculating the sample std. deviation of the values of the selected property.

===

## ee.FeatureCollection.aggregate_sample_var
Aggregates over a given property of the objects in a collection, calculating the sample variance of the values of the selected property.

===

## ee.FeatureCollection.aggregate_stats
Aggregates over a given property of the objects in a collection, calculating the sum, min, max, mean, sample standard deviation, sample variance, total standard deviation and total variance of the selected property.

===

## ee.FeatureCollection.aggregate_sum
Aggregates over a given property of the objects in a collection, calculating the sum of the values of the selected property.

===

## ee.FeatureCollection.aggregate_total_sd
Aggregates over a given property of the objects in a collection, calculating the total std. deviation of the values of the selected property.

===

## ee.FeatureCollection.aggregate_total_var
Aggregates over a given property of the objects in a collection, calculating the total variance of the values of the selected property.

===

## ee.FeatureCollection.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.FeatureCollection.bounds
Constructs a bounding box around the geometries in a collection.

===

## ee.FeatureCollection.classify
Classifies each feature in a collection.

===

## ee.FeatureCollection.cluster
Clusters each feature in a collection, adding a new column to each feature containing the cluster number to which it has been assigned.

===

## ee.FeatureCollection.copyProperties
Copies metadata properties from one element to another.

===

## ee.FeatureCollection.distance
Produces a DOUBLE image where each pixel is the distance in meters from the pixel center to the nearest Point, LineString, or polygonal boundary in the collection. Note distance is also measured within interiors of polygons. Pixels that are not within 'searchRadius' meters of a geometry will be masked out. Distances are computed on a sphere, so there is a small error proportional to the latitude difference between each pixel and the nearest geometry.

===

## ee.FeatureCollection.distinct
Removes duplicates from a collection. Note that duplicates are determined using a strong hash over the serialized form of the selected properties.

===

## ee.FeatureCollection.draw
Paints a vector collection for visualization. Not intended for use as input to other algorithms.

===

## ee.FeatureCollection.errorMatrix
Computes a 2D error matrix for a collection by comparing two columns of a collection: one containing the actual values, and one containing predicted values. The values are expected to be small contiguous integers, starting from 0. Axis 0 (the rows) of the matrix correspond to the actual values, and Axis 1 (the columns) to the predicted values.

===

## ee.FeatureCollection.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.FeatureCollection.filter
Apply a filter to this collection. Returns the filtered collection.

===

## ee.FeatureCollection.filterBounds
Shortcut to filter a collection by intersection with geometry. Items in the collection with a footprint that fails to intersect the given geometry will be excluded. This is equivalent to this.filter(ee.Filter.bounds(...)). Caution: providing a large or complex collection as the geometry argument can result in poor performance. Collating the geometry of collections does not scale well; use the smallest collection (or geometry) that is required to achieve the desired outcome. Returns the filtered collection.

===

## ee.FeatureCollection.filterDate
Shortcut to filter a collection by a date range. The start and end may be Dates, numbers (interpreted as milliseconds since 1970-01-01T00:00:00Z), or strings (such as '1996-01-01T08:00'). Based on 'system:time_start'. This is equivalent to this.filter(ee.Filter.date(...)); see the ee.Filter type for other date filtering options. Returns the filtered collection.

===

## ee.FeatureCollection.first
Returns the first entry from a given collection.

===

## ee.FeatureCollection.flatten
Flattens collections of collections.

===

## ee.FeatureCollection.geometry
Extracts and merges the geometries of a collection. Requires that all the geometries in the collection share the projection and edge interpretation. Caution: providing a large or complex collection as input can result in poor performance. Collating the geometry of collections does not scale well; use the smallest collection that is required to achieve the desired outcome. Note: If only a bounding box around the collection is needed, consider using Collection.bounds instead.

===

## ee.FeatureCollection.get
Extract a property from a feature.

===

## ee.FeatureCollection.getArray
Extract a property from a feature.

===

## ee.FeatureCollection.getDownloadURL
Gets a download URL. When the URL is accessed, the FeatureCollection is downloaded in one of several formats. Returns a download URL or undefined if a callback was specified.

===

## ee.FeatureCollection.getInfo
An imperative function that returns all the known information about this collection via an AJAX call. Returns a collection description whose fields include: - features: a list containing metadata about the features in the collection. - properties: an optional dictionary containing the collection's metadata properties.

===

## ee.FeatureCollection.getMapId
An imperative function that returns a map ID and optional token, suitable for generating a Map overlay. Returns an object which may be passed to ee.data.getTileUrl or ui.Map.addLayer, including an additional 'image' field, containing a Collection.draw image wrapping a FeatureCollection containing this feature. Undefined if a callback was specified.

===

## ee.FeatureCollection.getNumber
Extract a property from a feature.

===

## ee.FeatureCollection.getString
Extract a property from a feature.

===

## ee.FeatureCollection.inverseDistance
Returns an inverse-distance weighted estimate of the value at each pixel.

===

## ee.FeatureCollection.iterate
Applies a user-supplied function to each element of a collection. The user-supplied function is given two arguments: the current element, and the value returned by the previous call to iterate() or the first argument, for the first iteration. The result is the value returned by the final call to the user-supplied function. Returns the result of the Collection.iterate() call.

===

## ee.FeatureCollection.kriging
Returns the results of sampling a Kriging estimator at each pixel.

===

## ee.FeatureCollection.limit
Limit a collection to the specified number of elements, optionally sorting them by a specified property first. Returns the limited collection.

===

## ee.FeatureCollection.loadBigQueryTable
Reads data from a BigQuery table and presents the results as a FeatureCollection.

===

## ee.FeatureCollection.makeArray
Add a 1-D Array to each feature in a collection by combining a list of properties for each feature into a 1-D Array. All of the properties must be numeric values. If a feature doesn't contain all of the named properties, or any of them aren't numeric, the feature will be dropped from the resulting collection.

===

## ee.FeatureCollection.map
Maps an algorithm over a collection. Returns the mapped collection.

===

## ee.FeatureCollection.merge
Merges two collections into one. The result has all the elements that were in either collection. Elements from the first collection will have IDs prefixed with "1 " and elements from the second collection will have IDs prefixed with "2 ". Note: If many collections need to be merged, consider placing them all in a collection and using FeatureCollection.flatten() instead. Repeated use of FeatureCollection.merge() will result in increasingly long element IDs and reduced performance.

===

## ee.FeatureCollection.propertyNames
Returns the names of properties on this element.

===

## ee.FeatureCollection.randomColumn
Adds a column of deterministic pseudorandom numbers to a collection. The outputs are double-precision floating point numbers. When using the 'uniform' distribution (default), outputs are in the range of [0, 1). Using the 'normal' distribution, outputs have μ=0, σ=1, but have no explicit limits.

===

## ee.FeatureCollection.randomPoints
Generates points that are uniformly random in the given geometry. If the geometry is two-dimensional (polygon or multi-polygon) then the returned points are uniformly distributed on the given region of the sphere. If the geometry is one-dimensional (linestrings), the returned points are interpolated uniformly along the geometry's edges. If the geometry has dimension zero (points), the returned points are sampled uniformly from the input points. If a multi-geometry of mixed dimension is given, points are sampled from the component geometries with the highest dimension.

===

## ee.FeatureCollection.reduceColumns
Apply a reducer to each element of a collection, using the given selectors to determine the inputs. Returns a dictionary of results, keyed with the output names.

===

## ee.FeatureCollection.reduceToImage
Creates an image from a feature collection by applying a reducer over the selected properties of all the features that intersect each pixel.

===

## ee.FeatureCollection.remap
Remaps the value of a specific property in a collection. Takes two parallel lists and maps values found in one to values in the other. Any element with a value that is not specified in the first list is dropped from the output collection.

===

## ee.FeatureCollection.runBigQuery
Runs a BigQuery query, fetches the results and presents the them as a FeatureCollection.

===

## ee.FeatureCollection.select
Select properties from each Feature in a collection.  It is also possible to call this function with only string arguments; they will be all be interpreted as propertySelectors (varargs). Returns the feature collection with selected properties.

===

## ee.FeatureCollection.serialize
Returns the serialized representation of this object.

===

## ee.FeatureCollection.set
Overrides one or more metadata properties of an Element. Returns the element with the specified properties overridden.

===

## ee.FeatureCollection.size
Returns the number of elements in the collection.

===

## ee.FeatureCollection.sort
Sort a collection by the specified property. Returns the sorted collection.

===

## ee.FeatureCollection.style
Draw a vector collection for visualization using a simple style language.

===

## ee.FeatureCollection.toDictionary
Extract properties from a feature as a dictionary.

===

## ee.FeatureCollection.toList
Returns the elements of a collection as a list.

===

## ee.FeatureCollection.union
Merges all geometries in a given collection into one and returns a collection containing a single feature with only an ID of 'union_result' and a geometry.

===

## ee.Filter
Constructs a new filter. This constructor accepts the following args: - Another filter. - A list of filters (which are implicitly ANDed together). - A ComputedObject returning a filter. Users shouldn't be making these; they're produced by the generator functions below.

===

## ee.Filter.and
Combine two or more filters using boolean AND. Returns the constructed filter.

===

## ee.Filter.area
Returns a filter that passes if the specified geometry has an area within the given range (inclusive).

===

## ee.Filter.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Filter.bounds
Creates a filter that passes if the object's geometry intersects the given geometry. Caution: providing a large or complex collection as the geometry argument can result in poor performance. Collating the geometry of collections does not scale well; use the smallest collection (or geometry) that is required to achieve the desired outcome. Returns the constructed filter.

===

## ee.Filter.calendarRange
Returns a filter that passes if the object's timestamp falls within the given range of a calendar field. The month , day_of_year , day_of_month , and day_of_week are 1-based. Times are assumed to be in UTC. Weeks are assumed to begin on Monday as day 1. If end < start then this tests for value >= start OR value <= end , to allow for wrapping.

===

## ee.Filter.contains
Creates a unary or binary filter that passes if the left geometry contains the right geometry (empty geometries are not contained in anything).

===

## ee.Filter.date
Filter a collection by date range. The start and end may be Dates, numbers (interpreted as milliseconds since 1970-01-01T00:00:00Z), or strings (such as '1996-01-01T08:00'). Based on 'system:time_start' property. Returns the constructed filter.

===

## ee.Filter.dateRangeContains
Creates a unary or binary filter that passes if the left operand, a date range, contains the right operand, a date.

===

## ee.Filter.dayOfYear
Returns a filter that passes if the object's timestamp falls within the given day-of-year range.

===

## ee.Filter.disjoint
Creates a unary or binary filter that passes unless the left geometry intersects the right geometry.

===

## ee.Filter.eq
Filter to metadata equal to the given value. Returns the constructed filter.

===

## ee.Filter.equals
Creates a unary or binary filter that passes if the two operands are equals.

===

## ee.Filter.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Filter.expression
Constructs a filter tree from a string.

===

## ee.Filter.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Filter.greaterThan
Creates a unary or binary filter that passes if the left operand is greater than the right operand.

===

## ee.Filter.greaterThanOrEquals
Creates a unary or binary filter that passes unless the left operand is less than the right operand.

===

## ee.Filter.gt
Filter on metadata greater than the given value. Returns the constructed filter.

===

## ee.Filter.gte
Filter on metadata greater than or equal to the given value. Returns the constructed filter.

===

## ee.Filter.hasType
Creates a unary or binary filter that passes if the left operand has the type, or is a subtype of the type named in the right operand.

===

## ee.Filter.inList
Filter on metadata contained in a list. Returns the constructed filter.

===

## ee.Filter.intersects
Creates a unary or binary filter that passes if the left geometry intersects the right geometry.

===

## ee.Filter.isContained
Creates a unary or binary filter that passes if the right geometry contains the left geometry (empty geometries are not contained in anything).

===

## ee.Filter.lessThan
Creates a unary or binary filter that passes if the left operand is less than the right operand.

===

## ee.Filter.lessThanOrEquals
Creates a unary or binary filter that passes unless the left operand is greater than the right operand.

===

## ee.Filter.listContains
Creates a unary or binary filter that passes if the left operand, a list, contains the right operand.

===

## ee.Filter.lt
Filter to metadata less than the given value. Returns the constructed filter.

===

## ee.Filter.lte
Filter on metadata less than or equal to the given value. Returns the constructed filter.

===

## ee.Filter.maxDifference
Creates a unary or binary filter that passes if the left and right operands, both numbers, are within a given maximum difference. If used as a join condition, this numeric difference is used as a join measure.

===

## ee.Filter.neq
Filter to metadata not equal to the given value. Returns the constructed filter.

===

## ee.Filter.not
Returns the opposite of the input filter, i.e. the resulting filter will match if and only if the input filter doesn't match.

===

## ee.Filter.notEquals
Creates a unary or binary filter that passes unless the two operands are equals.

===

## ee.Filter.notNull
Returns a filter that passes if all the named properties are not null.

===

## ee.Filter.or
Combine two or more filters using boolean OR. Returns the constructed filter.

===

## ee.Filter.rangeContains
Returns a filter that passes if the value of the selected field is in the specified range (inclusive).

===

## ee.Filter.serialize
Returns the serialized representation of this object.

===

## ee.Filter.stringContains
Creates a unary or binary filter that passes if the left operand, a string, contains the right operand, also a string.

===

## ee.Filter.stringEndsWith
Creates a unary or binary filter that passes if the left operand, a string, ends with the right operand, also a string.

===

## ee.Filter.stringStartsWith
Creates a unary or binary filter that passes if the left operand, a string, starts with the right operand, also a string.

===

## ee.Filter.withinDistance
Creates a unary or binary filter that passes if the left geometry is within a specified distance of the right geometry. If used as a join condition, this distance is used as a join measure.

===

## ee.Geometry
Creates a geometry.

===

## ee.Geometry.BBox
Constructs a rectangle whose edges are lines of latitude and longitude. The result is a planar rectangle in EPSG:4326. If (east - west) ≥ 360 then the longitude range will be normalized to -180 to +180; otherwise they will be treated as designating points on a circle (e.g. east may be numerically less than west).

===

## ee.Geometry.BBox.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.BBox.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.BBox.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.BBox.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.BBox.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.BBox.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.BBox.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.BBox.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.BBox.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.BBox.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.BBox.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.BBox.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.BBox.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.BBox.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.BBox.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.BBox.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.BBox.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.BBox.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.BBox.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.BBox.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.BBox.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.BBox.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.BBox.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.BBox.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.BBox.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.BBox.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.BBox.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.BBox.projection
Returns the projection of the geometry.

===

## ee.Geometry.BBox.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.BBox.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.BBox.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.BBox.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.BBox.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.BBox.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.BBox.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.BBox.union
Returns the union of the two geometries.

===

## ee.Geometry.BBox.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.LineString
Constructs an ee.Geometry describing a LineString. For convenience, varargs may be used when all arguments are numbers. This allows creating geodesic EPSG:4326 LineStrings given an even number of arguments, e.g. ee.Geometry.LineString(aLng, aLat, bLng, bLat, ...).

===

## ee.Geometry.LineString.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.LineString.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.LineString.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.LineString.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.LineString.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.LineString.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.LineString.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.LineString.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.LineString.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.LineString.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.LineString.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.LineString.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.LineString.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.LineString.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.LineString.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.LineString.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.LineString.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.LineString.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.LineString.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.LineString.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.LineString.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.LineString.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.LineString.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.LineString.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.LineString.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.LineString.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.LineString.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.LineString.projection
Returns the projection of the geometry.

===

## ee.Geometry.LineString.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.LineString.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.LineString.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.LineString.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.LineString.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.LineString.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.LineString.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.LineString.union
Returns the union of the two geometries.

===

## ee.Geometry.LineString.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.LinearRing
Constructs an ee.Geometry describing a LinearRing. If the last point is not equal to the first, a duplicate of the first point will be added at the end. For convenience, varargs may be used when all arguments are numbers. This allows creating geodesic EPSG:4326 LinearRings given an even number of arguments, e.g. ee.Geometry.LinearRing(aLng, aLat, bLng, bLat, ..., aLng, aLat).

===

## ee.Geometry.LinearRing.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.LinearRing.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.LinearRing.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.LinearRing.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.LinearRing.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.LinearRing.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.LinearRing.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.LinearRing.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.LinearRing.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.LinearRing.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.LinearRing.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.LinearRing.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.LinearRing.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.LinearRing.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.LinearRing.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.LinearRing.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.LinearRing.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.LinearRing.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.LinearRing.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.LinearRing.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.LinearRing.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.LinearRing.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.LinearRing.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.LinearRing.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.LinearRing.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.LinearRing.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.LinearRing.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.LinearRing.projection
Returns the projection of the geometry.

===

## ee.Geometry.LinearRing.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.LinearRing.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.LinearRing.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.LinearRing.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.LinearRing.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.LinearRing.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.LinearRing.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.LinearRing.union
Returns the union of the two geometries.

===

## ee.Geometry.LinearRing.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.MultiLineString
Constructs an ee.Geometry describing a MultiLineString. For convenience, varargs may be used when all arguments are numbers. This allows creating geodesic EPSG:4326 MultiLineStrings with a single LineString, given an even number of arguments, e.g. ee.Geometry.MultiLineString(aLng, aLat, bLng, bLat, ...).

===

## ee.Geometry.MultiLineString.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.MultiLineString.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.MultiLineString.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.MultiLineString.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.MultiLineString.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.MultiLineString.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.MultiLineString.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.MultiLineString.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.MultiLineString.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.MultiLineString.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.MultiLineString.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.MultiLineString.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.MultiLineString.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.MultiLineString.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.MultiLineString.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.MultiLineString.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.MultiLineString.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.MultiLineString.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.MultiLineString.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.MultiLineString.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.MultiLineString.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.MultiLineString.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.MultiLineString.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.MultiLineString.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.MultiLineString.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.MultiLineString.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.MultiLineString.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.MultiLineString.projection
Returns the projection of the geometry.

===

## ee.Geometry.MultiLineString.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.MultiLineString.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.MultiLineString.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.MultiLineString.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.MultiLineString.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.MultiLineString.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.MultiLineString.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.MultiLineString.union
Returns the union of the two geometries.

===

## ee.Geometry.MultiLineString.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.MultiPoint
Constructs an ee.Geometry describing a MultiPoint. For convenience, varargs may be used when all arguments are numbers. This allows creating EPSG:4326 MultiPoints given an even number of arguments, e.g. ee.Geometry.MultiPoint(aLng, aLat, bLng, bLat, ...).

===

## ee.Geometry.MultiPoint.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.MultiPoint.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.MultiPoint.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.MultiPoint.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.MultiPoint.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.MultiPoint.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.MultiPoint.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.MultiPoint.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.MultiPoint.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.MultiPoint.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.MultiPoint.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.MultiPoint.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.MultiPoint.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.MultiPoint.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.MultiPoint.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.MultiPoint.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.MultiPoint.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.MultiPoint.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.MultiPoint.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.MultiPoint.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.MultiPoint.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.MultiPoint.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.MultiPoint.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.MultiPoint.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.MultiPoint.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.MultiPoint.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.MultiPoint.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.MultiPoint.projection
Returns the projection of the geometry.

===

## ee.Geometry.MultiPoint.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.MultiPoint.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.MultiPoint.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.MultiPoint.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.MultiPoint.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.MultiPoint.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.MultiPoint.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.MultiPoint.union
Returns the union of the two geometries.

===

## ee.Geometry.MultiPoint.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.MultiPolygon
Constructs an ee.Geometry describing a MultiPolygon. For convenience, varargs may be used when all arguments are numbers. This allows creating geodesic EPSG:4326 MultiPolygons with a single Polygon with a single LinearRing given an even number of arguments, e.g. ee.Geometry.MultiPolygon(aLng, aLat, bLng, bLat, ..., aLng, aLat).

===

## ee.Geometry.MultiPolygon.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.MultiPolygon.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.MultiPolygon.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.MultiPolygon.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.MultiPolygon.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.MultiPolygon.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.MultiPolygon.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.MultiPolygon.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.MultiPolygon.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.MultiPolygon.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.MultiPolygon.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.MultiPolygon.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.MultiPolygon.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.MultiPolygon.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.MultiPolygon.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.MultiPolygon.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.MultiPolygon.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.MultiPolygon.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.MultiPolygon.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.MultiPolygon.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.MultiPolygon.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.MultiPolygon.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.MultiPolygon.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.MultiPolygon.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.MultiPolygon.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.MultiPolygon.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.MultiPolygon.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.MultiPolygon.projection
Returns the projection of the geometry.

===

## ee.Geometry.MultiPolygon.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.MultiPolygon.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.MultiPolygon.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.MultiPolygon.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.MultiPolygon.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.MultiPolygon.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.MultiPolygon.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.MultiPolygon.union
Returns the union of the two geometries.

===

## ee.Geometry.MultiPolygon.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.Point
Constructs an ee.Geometry describing a point. For convenience, varargs may be used when all arguments are numbers. This allows creating EPSG:4326 points, e.g. ee.Geometry.Point(lng, lat).

===

## ee.Geometry.Point.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.Point.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.Point.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.Point.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.Point.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.Point.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.Point.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.Point.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.Point.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.Point.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.Point.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.Point.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.Point.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.Point.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.Point.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.Point.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.Point.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.Point.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.Point.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.Point.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.Point.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.Point.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.Point.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.Point.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.Point.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.Point.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.Point.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.Point.projection
Returns the projection of the geometry.

===

## ee.Geometry.Point.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.Point.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.Point.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.Point.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.Point.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.Point.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.Point.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.Point.union
Returns the union of the two geometries.

===

## ee.Geometry.Point.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.Polygon
Constructs an ee.Geometry describing a polygon. For convenience, varargs may be used when all arguments are numbers. This allows creating geodesic EPSG:4326 Polygons with a single LinearRing given an even number of arguments, e.g. ee.Geometry.Polygon(aLng, aLat, bLng, bLat, ..., aLng, aLat).

===

## ee.Geometry.Polygon.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.Polygon.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.Polygon.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.Polygon.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.Polygon.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.Polygon.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.Polygon.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.Polygon.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.Polygon.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.Polygon.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.Polygon.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.Polygon.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.Polygon.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.Polygon.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.Polygon.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.Polygon.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.Polygon.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.Polygon.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.Polygon.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.Polygon.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.Polygon.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.Polygon.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.Polygon.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.Polygon.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.Polygon.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.Polygon.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.Polygon.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.Polygon.projection
Returns the projection of the geometry.

===

## ee.Geometry.Polygon.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.Polygon.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.Polygon.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.Polygon.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.Polygon.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.Polygon.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.Polygon.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.Polygon.union
Returns the union of the two geometries.

===

## ee.Geometry.Polygon.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.Rectangle
Constructs an ee.Geometry describing a rectangular polygon. For convenience, varargs may be used when all arguments are numbers. This allows creating EPSG:4326 Polygons given exactly four coordinates, e.g. ee.Geometry.Rectangle(minLng, minLat, maxLng, maxLat).

===

## ee.Geometry.Rectangle.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.Rectangle.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.Rectangle.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.Rectangle.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.Rectangle.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.Rectangle.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.Rectangle.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.Rectangle.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.Rectangle.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.Rectangle.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.Rectangle.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.Rectangle.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.Rectangle.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.Rectangle.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.Rectangle.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.Rectangle.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.Rectangle.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.Rectangle.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.Rectangle.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.Rectangle.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.Rectangle.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.Rectangle.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.Rectangle.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.Rectangle.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.Rectangle.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.Rectangle.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.Rectangle.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.Rectangle.projection
Returns the projection of the geometry.

===

## ee.Geometry.Rectangle.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.Rectangle.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.Rectangle.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.Rectangle.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.Rectangle.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.Rectangle.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.Rectangle.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.Rectangle.union
Returns the union of the two geometries.

===

## ee.Geometry.Rectangle.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Geometry.area
Returns the area of the geometry. Area of points and line strings is 0 and the area of multi geometries is the sum of the areas of their components (intersecting areas are counted multiple times).

===

## ee.Geometry.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Geometry.bounds
Returns the bounding rectangle of the geometry.

===

## ee.Geometry.buffer
Returns the input buffered by a given distance. If the distance is positive, the geometry is expanded, and if the distance is negative, the geometry is contracted.

===

## ee.Geometry.centroid
Returns a point at the center of the highest-dimension components of the geometry. Lower-dimensional components are ignored, so the centroid of a geometry containing two polygons, three lines and a point is equivalent to the centroid of a geometry containing just the two polygons.

===

## ee.Geometry.closestPoint
Returns the point on the right input that is nearest to the left input. If either input is empty, null is returned. If both inputs are unbounded, an arbitrary point is returned. If one input is unbounded, an arbitrary point in the bounded input is returned.

===

## ee.Geometry.closestPoints
Returns a dictionary containing up to two entries representing a point on each input geometry that is closest to the other input geometry. If either geometry is empty, an empty dictionary is returned. If both geometries are unbounded, the dictionary has an arbitrary point for both 'left' and 'right'. If one geometry is unbounded, the dictionary has an arbitrary point contained in the bounded geometry for both 'left' and 'right'.

===

## ee.Geometry.containedIn
Returns true if and only if one geometry is contained in the other.

===

## ee.Geometry.contains
Returns true if and only if one geometry contains the other.

===

## ee.Geometry.convexHull
Returns the convex hull of the given geometry. The convex hull of a single point is the point itself, the convex hull of collinear points is a line, and the convex hull of everything else is a polygon. Note that a degenerate polygon with all vertices on the same line will result in a line segment.

===

## ee.Geometry.coordinates
Returns a GeoJSON-style list of the geometry's coordinates.

===

## ee.Geometry.coveringGrid
Returns a collection of features that cover this geometry, where each feature is a rectangle in the grid defined by the given projection.

===

## ee.Geometry.cutLines
Converts LineString, MultiLineString, and LinearRing geometries into a MultiLineString by cutting them into parts no longer than the given distance along their length. All other geometry types will be converted to an empty MultiLineString.

===

## ee.Geometry.difference
Returns the result of subtracting the 'right' geometry from the 'left' geometry.

===

## ee.Geometry.disjoint
Returns true if and only if the geometries are disjoint.

===

## ee.Geometry.dissolve
Returns the union of the geometry. This leaves single geometries untouched, and unions multi geometries.

===

## ee.Geometry.distance
Returns the minimum distance between two geometries.

===

## ee.Geometry.edgesAreGeodesics
Returns true if the geometry edges, if any, are geodesics along a spherical model of the earth; if false, any edges are straight lines in the projection.

===

## ee.Geometry.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Geometry.fromS2CellId
Constructs the Polygon corresponding to an S2 cell id.

===

## ee.Geometry.fromS2CellToken
Constructs the Polygon corresponding to an S2 cell id as a hex string.

===

## ee.Geometry.geodesic
If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.

===

## ee.Geometry.geometries
Returns the list of geometries in a GeometryCollection, or a singleton list of the geometry for single geometries.

===

## ee.Geometry.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Geometry.intersection
Returns the intersection of the two geometries.

===

## ee.Geometry.intersects
Returns true if and only if the geometries intersect.

===

## ee.Geometry.isUnbounded
Returns whether the geometry is unbounded.

===

## ee.Geometry.length
Returns the length of the linear parts of the geometry. Polygonal parts are ignored. The length of multi geometries is the sum of the lengths of their components.

===

## ee.Geometry.perimeter
Returns the length of the perimeter of the polygonal parts of the geometry. The perimeter of multi geometries is the sum of the perimeters of their components.

===

## ee.Geometry.projection
Returns the projection of the geometry.

===

## ee.Geometry.s2Cell
Constructs the Polygon corresponding to an S2 cell id.

===

## ee.Geometry.serialize
Returns the serialized representation of this object.

===

## ee.Geometry.simplify
Simplifies the geometry to within a given error margin. Note that this does not respect the error margin requested by the consumer of this algorithm, unless maxError is explicitly specified to be null. This overrides the default Earth Engine policy for propagating error margins, so regardless of the geometry accuracy requested from the output, the inputs will be requested with the error margin specified in the arguments to this algorithm. This results in consistent rendering at all zoom levels of a rendered vector map, but at lower zoom levels (i.e. zoomed out), the geometry won't be simplified, which may harm performance.

===

## ee.Geometry.symmetricDifference
Returns the symmetric difference between two geometries.

===

## ee.Geometry.toGeoJSON
Returns a GeoJSON representation of the geometry.

===

## ee.Geometry.toGeoJSONString
Returns a GeoJSON string representation of the geometry.

===

## ee.Geometry.transform
Transforms the geometry to a specific projection.

===

## ee.Geometry.type
Returns the GeoJSON type of the geometry.

===

## ee.Geometry.union
Returns the union of the two geometries.

===

## ee.Geometry.withinDistance
Returns true if and only if the geometries are within a specified distance.

===

## ee.Image
An object to represent an Earth Engine image. This constructor accepts a variety of arguments: - A string: an Earth Engine asset id, - A string and a number: an Earth Engine asset id and version, - A number or ee.Array: creates a constant image, - A list: creates an image out of each list element and combines them into a single image, - An ee.Image: returns the argument, - Nothing: results in an empty transparent image.

===

## ee.Image.abs
Computes the absolute value of the input.

===

## ee.Image.acos
Computes the arccosine in radians of the input.

===

## ee.Image.add
Adds the first value to the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.addBands
Returns an image containing all bands copied from the first input and selected bands from the second input, optionally overwriting bands in the first image with the same name. The new image has the metadata and footprint from the first input image.

===

## ee.Image.and
Returns 1 if and only if both values are non-zero for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.arrayAccum
Accumulates elements of each array pixel along the given axis, by setting each element of the result array pixel to the reduction of elements in that pixel along the given axis, up to and including the current position on the axis. May be used to make a cumulative sum, a monotonically increasing sequence, etc.

===

## ee.Image.arrayArgmax
Computes the positional indices of the maximum value in image of array values. If there are multiple occurrences of the maximum, the indices reflect the first.

===

## ee.Image.arrayCat
Creates an array image by concatenating each array pixel along the given axis in each band.

===

## ee.Image.arrayDimensions
Returns the number of dimensions in each array band, and 0 for scalar image bands.

===

## ee.Image.arrayDotProduct
Computes the dot product of each pair of 1-D arrays in the bands of the input images.

===

## ee.Image.arrayFlatten
Converts a single-band image of equal-shape multidimensional pixels to an image of scalar pixels, with one band for each element of the array.

===

## ee.Image.arrayGet
For each band, an output band of the same name is created with the value at the given position extracted from the input multidimensional pixel in that band.

===

## ee.Image.arrayLength
Returns the length of each pixel's array along the given axis.

===

## ee.Image.arrayLengths
Returns a 1D array image with the length of each array axis.

===

## ee.Image.arrayMask
Creates an array image where each array-valued pixel is masked with another array-valued pixel, retaining only the elements where the mask is non-zero. If the mask image has one band it will be applied to all the bands of 'input', otherwise they must have the same number of bands.

===

## ee.Image.arrayPad
Pads the array values in each pixel to be a fixed length. The pad value will be appended to each array to extend it to given length along each axis. All bands of the image must be array-valued and have the same dimensions.

===

## ee.Image.arrayProject
Projects the array in each pixel to a lower dimensional space by specifying the axes to retain. Dropped axes must be at most length 1.

===

## ee.Image.arrayReduce
Reduces elements of each array pixel.

===

## ee.Image.arrayRepeat
Repeats each array pixel along the given axis. Each output pixel will have the shape of the input pixel, except length along the repeated axis, which will be multiplied by the number of copies.

===

## ee.Image.arrayReshape
Converts array bands of an image with equally-shaped, possibly multidimensional pixels to an image of arrays with a new shape.

===

## ee.Image.arraySlice
Creates a subarray by slicing out each position along the given axis from the 'start' (inclusive) to 'end' (exclusive) by increments of 'step'. The result will have as many dimensions as the input, and the same length in all directions except the slicing axis, where the length will be the number of positions from 'start' to 'end' by 'step' that are in range of the input array's length along 'axis'. This means the result can be length 0 along the given axis if start=end, or if the start or end values are entirely out of range.

===

## ee.Image.arraySort
Sorts elements of each array pixel along one axis.

===

## ee.Image.arrayTranspose
Transposes two dimensions of each array pixel.

===

## ee.Image.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Image.asin
Computes the arcsine in radians of the input.

===

## ee.Image.atan
Computes the arctangent in radians of the input.

===

## ee.Image.atan2
Calculates the angle formed by the 2D vector [x, y] for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is float.

===

## ee.Image.bandNames
Returns a list containing the names of the bands of an image.

===

## ee.Image.bandTypes
Returns a dictionary of the image's band types.

===

## ee.Image.bitCount
Calculates the number of one-bits in the 64-bit two's complement binary representation of the input.

===

## ee.Image.bitsToArrayImage
Turns the bits of an integer into a 1-D array. The array has a length up to the highest 'on' bit in the input.

===

## ee.Image.bitwiseAnd
Calculates the bitwise AND of the input values for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.bitwiseNot
Calculates the bitwise NOT of the input, in the smallest signed integer type that can hold the input.

===

## ee.Image.bitwiseOr
Calculates the bitwise OR of the input values for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.bitwiseXor
Calculates the bitwise XOR of the input values for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.blend
Overlays one image on top of another. The images are blended together using the masks as opacity. If either of images has only 1 band, it is replicated to match the number of bands in the other image.

===

## ee.Image.byte
Casts the input value to an unsigned 8-bit integer.

===

## ee.Image.cast
Casts some or all bands of an image to the specified types.

===

## ee.Image.cat
Combines the given images into a single image which contains all bands from all of the images. If two or more bands share a name, they are suffixed with an incrementing index. The resulting image will have the metadata from the first input image, only. This function will promote constant values into constant images. Returns the combined image.

===

## ee.Image.cbrt
Computes the cubic root of the input.

===

## ee.Image.ceil
Computes the smallest integer greater than or equal to the input.

===

## ee.Image.changeProj
Tweaks the projection of the input image, moving each pixel from its location in srcProj to the same coordinates in dstProj.

===

## ee.Image.clamp
Clamps the values in all bands of an image to all lie within the specified range.

===

## ee.Image.classify
Classifies an image.

===

## ee.Image.clip
Clips an image to a Geometry or Feature. The output bands correspond exactly to the input bands, except data not covered by the geometry is masked. The output image retains the metadata of the input image. Use clipToCollection to clip an image to a FeatureCollection. Returns the clipped image.

===

## ee.Image.clipToBoundsAndScale
Clips an image to the bounds of a Geometry, and scales the clipped image to a particular size or scale. Caution: providing a large or complex collection as the geometry argument can result in poor performance. Collating the geometry of collections does not scale well; use the smallest collection (or geometry) that is required to achieve the desired outcome.

===

## ee.Image.clipToCollection
Clips an image to a FeatureCollection. The output bands correspond exactly the input bands, except data not covered by the geometry of at least one feature from the collection is masked. The output image retains the metadata of the input image.

===

## ee.Image.cluster
Applies a clusterer to an image. Returns a new image with a single band containing values from 0 to N, indicating the cluster each pixel is assigned to.

===

## ee.Image.connectedComponents
Finds connected components with the same value of the first band of the input and labels them with a globally unique value. Connectedness is specified by the given kernel. Objects larger than maxSize are considered background, and are masked.

===

## ee.Image.connectedPixelCount
Generate an image where each pixel contains the number of 4- or 8-connected neighbors (including itself).

===

## ee.Image.constant
Generates an image containing a constant value everywhere.

===

## ee.Image.convolve
Convolves each band of an image with the given kernel.

===

## ee.Image.copyProperties
Copies metadata properties from one element to another.

===

## ee.Image.cos
Computes the cosine of the input in radians.

===

## ee.Image.cosh
Computes the hyperbolic cosine of the input.

===

## ee.Image.cumulativeCost
Computes a cumulative cost map based on an image containing costs to traverse each pixel and an image containing source locations. Each output band represents the cumulative cost over the corresponding input cost band.

===

## ee.Image.date
Returns the acquisition time of an image as a Date object. This helper function is equivalent to ee.Date(image.get('system:time_start')) .

===

## ee.Image.derivative
Computes the X and Y discrete derivatives for each band in the input image, in pixel coordinates. For each band of the input image, the output image will have two bands named with the suffixes _x and _y , containing the respective derivatives.

===

## ee.Image.digamma
Computes the digamma function of the input.

===

## ee.Image.directionalDistanceTransform
For each zero-valued pixel in the source, get the distance to the nearest non-zero pixels in the given direction. Returns a band of floating point distances called "distance".

===

## ee.Image.displace
Warps an image using an image of displacements.

===

## ee.Image.displacement
Determines displacements required to register an image to a reference image while allowing local, rubber sheet deformations. Displacements are computed in the CRS of the reference image, at a scale dictated by the lowest resolution of the following three projections: input image projection, reference image projection, and requested projection. The displacements are then transformed into the user-specified projection for output.

===

## ee.Image.distance
Computes the distance to the nearest non-zero pixel in each band, using the specified distance kernel.

===

## ee.Image.divide
Divides the first value by the second, returning 0 for division by 0 for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.double
Casts the input value to a 64-bit float.

===

## ee.Image.entropy
Computes the windowed entropy for each band using the specified kernel centered on each input pixel. Entropy is computed as -sum(p * log2(p)), where p is the normalized probability of occurrence of the values encountered in each window.

===

## ee.Image.eq
Returns 1 if and only if the first value is equal to the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.erf
Computes the error function of the input.

===

## ee.Image.erfInv
Computes the inverse error function of the input.

===

## ee.Image.erfc
Computes the complementary error function of the input.

===

## ee.Image.erfcInv
Computes the inverse complementary error function of the input.

===

## ee.Image.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Image.exp
Computes the Euler's number e raised to the power of the input.

===

## ee.Image.expression
Evaluates an arithmetic expression on an image, possibly involving additional images. The bands of the primary input image are available using the built-in function b(), as b(0) or b('band_name'). Variables in the expression are interpreted as additional image parameters which must be supplied in opt_map. The bands of each such image can be accessed like image.band_name or image[0]. Both b() and image[] allow multiple arguments, to specify multiple bands, such as b(1, 'name', 3).  Calling b() with no arguments, or using a variable by itself, returns all bands of the image. If the result of an expression is a single band, it can be assigned a name using the '=' operator (e.g.: x = a + b). Returns the image computed by the provided expression.

===

## ee.Image.fastDistanceTransform
Returns the distance, as determined by the specified distance metric, to the nearest non-zero valued pixel in the input. The output contains values for all pixels within the given neighborhood size, regardless of the input's mask. Note: the default distance metric returns squared distance.

===

## ee.Image.first
Selects the value of the first value for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.firstNonZero
Selects the first value if it is non-zero, and the second value otherwise for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.float
Casts the input value to a 32-bit float.

===

## ee.Image.floor
Computes the largest integer less than or equal to the input.

===

## ee.Image.focalMax
Applies a morphological reducer() filter to each band of an image using a named or custom kernel.

===

## ee.Image.focalMean
Applies a morphological mean filter to each band of an image using a named or custom kernel.

===

## ee.Image.focalMedian
Applies a morphological reducer() filter to each band of an image using a named or custom kernel.

===

## ee.Image.focalMin
Applies a morphological reducer() filter to each band of an image using a named or custom kernel.

===

## ee.Image.focalMode
Applies a morphological reducer() filter to each band of an image using a named or custom kernel.

===

## ee.Image.gamma
Computes the gamma function of the input.

===

## ee.Image.gammainc
Calculates the regularized lower incomplete Gamma function γ(x,a) for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is float.

===

## ee.Image.geometry
Returns the geometry of a given feature in a given projection.

===

## ee.Image.get
Extract a property from a feature.

===

## ee.Image.getArray
Extract a property from a feature.

===

## ee.Image.getDownloadURL
Get a download URL for small chunks of image data in GeoTIFF or NumPy format. Maximum request size is 32 MB, maximum grid dimension is 10000. Use getThumbURL for RGB visualization formats PNG and JPG. Returns returns a download URL, or undefined if a callback was specified.

===

## ee.Image.getInfo
An imperative function that returns information about this image via an AJAX call. Returns a description of the image. Includes: - bands - a list containing metadata about the bands in the collection. - properties - a dictionary containing the image's metadata properties.

===

## ee.Image.getMapId
An imperative function that returns a map ID and optional token, suitable for generating a Map overlay. Returns an object which may be passed to ee.data.getTileUrl or ui.Map.addLayer. Undefined if a callback was specified.

===

## ee.Image.getNumber
Extract a property from a feature.

===

## ee.Image.getString
Extract a property from a feature.

===

## ee.Image.getThumbId
Applies transformations and returns the thumbId. Returns the thumb ID and optional token, or null if a callback is specified.

===

## ee.Image.getThumbURL
Get a thumbnail URL for this image. Returns a thumbnail URL, or undefined if a callback was specified.

===

## ee.Image.glcmTexture
Computes texture metrics from the Gray Level Co-occurrence Matrix around each pixel of every band. The GLCM is a tabulation of how often different combinations of pixel brightness values (grey levels) occur in an image. It counts the number of times a pixel of value X lies next to a pixel of value Y, in a particular direction and distance. and then derives statistics from this tabulation. This implementation computes the 14 GLCM metrics proposed by Haralick, and 4 additional metrics from Conners. Inputs are required to be integer valued. The output consists of 18 bands per input band if directional averaging is on and 18 bands per directional pair in the kernel, if not: ASM: f1, Angular Second Moment; measures the number of repeated pairs CONTRAST: f2, Contrast; measures the local contrast of an image CORR: f3, Correlation; measures the correlation between pairs of pixels VAR: f4, Variance; measures how spread out the distribution of gray-levels is IDM: f5, Inverse Difference Moment; measures the homogeneity SAVG: f6, Sum Average SVAR: f7, Sum Variance SENT: f8, Sum Entropy ENT: f9, Entropy. Measures the randomness of a gray-level distribution DVAR: f10, Difference variance DENT: f11, Difference entropy IMCORR1: f12, Information Measure of Corr. 1 IMCORR2: f13, Information Measure of Corr. 2 MAXCORR: f14, Max Corr. Coefficient. (not computed) DISS: Dissimilarity INERTIA: Inertia SHADE: Cluster Shade PROM: Cluster prominence More information can be found in the two papers: Haralick et. al, 'Textural Features for Image Classification', https://doi.org/10.1109/TSMC.1973.4309314 and Conners, et al, Segmentation of a high-resolution urban scene using texture operators', https://doi.org/10.1016/0734-189X(84)90197-X.

===

## ee.Image.gradient
Calculates the x and y gradient.

===

## ee.Image.gt
Returns 1 if and only if the first value is greater than the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.gte
Returns 1 if and only if the first value is greater than or equal to the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.hersDescriptor
Creates a dictionary of Histogram Error Ring Statistic (HERS) descriptor arrays from square array properties of an element. The HERS radius is taken to be the array's (side_length - 1) / 2.

===

## ee.Image.hersFeature
Computes the Histogram Error Ring Statistic (HERS) for each pixel in each band matching the keys in the descriptor. Only the bands for which HERS could be computed are returned.

===

## ee.Image.hersImage
Computes the Histogram Error Ring Statistic (HERS) for each pair of pixels in each band present in both images. Only the bands for which HERS could be computed are returned.

===

## ee.Image.hsvToRgb
Transforms the image from the HSV color space to the RGB color space. Expects a 3-band image in the range [0, 1], and produces 3 bands: red, green and blue with values in the range [0, 1].

===

## ee.Image.hypot
Calculates the magnitude of the 2D vector [x, y] for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is float.

===

## ee.Image.id
Returns the ID of a given element within a collection. Objects outside collections are not guaranteed to have IDs.

===

## ee.Image.int
Casts the input value to a signed 32-bit integer.

===

## ee.Image.int16
Casts the input value to a signed 16-bit integer.

===

## ee.Image.int32
Casts the input value to a signed 32-bit integer.

===

## ee.Image.int64
Casts the input value to a signed 64-bit integer.

===

## ee.Image.int8
Casts the input value to a signed 8-bit integer.

===

## ee.Image.interpolate
Interpolates each point in the first band of the input image into the piecewise-linear function specified by the x and y arrays. The x values must be strictly increasing. If an input point is less than the first or greater than the last x value, then the output is specified by the "behavior" argument: "extrapolate" specifies the output value is extrapolated from the two nearest points, "clamp" specifies the output value is taken from the nearest point, "input"  specifies the output value is copied from the input, and "mask" specifies the output value is masked.

===

## ee.Image.lanczos
Computes the Lanczos approximation of the input.

===

## ee.Image.leftShift
Calculates the left shift of v1 by v2 bits for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.linkCollection
Links the source image to a matching image from an image collection. Any specified bands or metadata will be added to the source image from the image found in the collection, and if the bands or metadata are already present they will be overwritten. If a matching image is not found, any new or updated bands will be fully masked and any new or updated metadata will be null. The output footprint will be the same as the source image footprint. A match is determined if the source image and an image in the collection have a specific equivalent metadata property. If more than one collection image would match, the collection image selected is arbitrary. By default, images are matched on their 'system:index' metadata property. This linking function is a convenience method for adding bands to a target image based on a specified shared metadata property and is intended to support linking collections that apply different processing/product generation to the same source imagery. For more expressive linking known as 'joining', see https://developers.google.com/earth-engine/guides/joins_intro.

===

## ee.Image.load
Returns the image given its ID.

===

## ee.Image.loadGeoTIFF
Loads a GeoTIFF as an Image.

===

## ee.Image.loadZarrV2Array
Loads a Zarr v2 array as an Image. The array attributes (.zattrs) must contain the field '_ARRAY_DIMENSIONS', which is a list of the names of each dimension (e.g., ['time', 'y', 'x']). There must be at least two dimensions, with the final two representing Y and X respectively (e.g., ['lat', 'lon']). The supported compression codecs are 'blosc', 'gzip', 'lz4', 'zlib', and 'zstd'. The supported blosc meta-compression codecs are 'lz4', lz4hc', 'zlib', and 'zstd' ('blosclz' is not supported).

===

## ee.Image.log
Computes the natural logarithm of the input.

===

## ee.Image.log10
Computes the base-10 logarithm of the input.

===

## ee.Image.long
Casts the input value to a signed 64-bit integer.

===

## ee.Image.lt
Returns 1 if and only if the first value is less than the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.lte
Returns 1 if and only if the first value is less than or equal to the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.mask
Gets or sets an image's mask. The output image retains the metadata and footprint of the input image. Pixels where the mask changes from zero to another value will be filled with zeros, or the values closest to zero within the range of the pixel type. Note: the version that sets a mask will be deprecated. To set a mask from an image on previously unmasked pixels, use Image.updateMask. To unmask previously masked pixels, use Image.unmask.

===

## ee.Image.matrixCholeskyDecomposition
Calculates the Cholesky decomposition of a matrix. The Cholesky decomposition is a decomposition into the form L * L' where L is a lower triangular matrix. The input must be a symmetric positive-definite matrix. Returns an image with 1 band named 'L'.

===

## ee.Image.matrixDeterminant
Computes the determinant of the matrix.

===

## ee.Image.matrixDiagonal
Computes the diagonal of the matrix in a single column.

===

## ee.Image.matrixFnorm
Computes the Frobenius norm of the matrix.

===

## ee.Image.matrixIdentity
Creates an image where each pixel is a 2D identity matrix of the given size.

===

## ee.Image.matrixInverse
Computes the inverse of the matrix.

===

## ee.Image.matrixLUDecomposition
Calculates the LU matrix decomposition such that P×input=L×U, where L is lower triangular (with unit diagonal terms), U is upper triangular and P is a partial pivot permutation matrix. The input matrix must be square. Returns an image with bands named 'L', 'U' and 'P'.

===

## ee.Image.matrixMultiply
Returns the matrix multiplication A * B for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.matrixPseudoInverse
Computes the Moore-Penrose pseudoinverse of the matrix.

===

## ee.Image.matrixQRDecomposition
Calculates the QR-decomposition of a matrix into two matrices Q and R such that input = QR, where Q is orthogonal, and R is upper triangular. Returns an image with bands named 'Q' and 'R'.

===

## ee.Image.matrixSingularValueDecomposition
Calculates the Singular Value Decomposition of the input matrix into U×S×V', such that U and V are orthogonal and S is diagonal. Returns an image with bands named 'U', 'S' and 'V'.

===

## ee.Image.matrixSolve
Solves for x in the matrix equation A * x = B, finding a least-squares solution if A is overdetermined for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.matrixToDiag
Computes a square diagonal matrix from a single column matrix.

===

## ee.Image.matrixTrace
Computes the trace of the matrix.

===

## ee.Image.matrixTranspose
Transposes two dimensions of each array pixel.

===

## ee.Image.max
Selects the maximum of the first and second values for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.medialAxis
Computes the discrete medial axis of the zero valued pixels of the first band of the input. Outputs 4 bands: medial - the medial axis points, scaled by the distance. coverage - the number of points supporting each medial axis point. xlabel - the horizontal distance to the power point for each pixel. ylabel - the vertical distance to the power point for each pixel.

===

## ee.Image.metadata
Generates a constant image of type double from a metadata property.

===

## ee.Image.min
Selects the minimum of the first and second values for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.mod
Calculates the remainder of the first value divided by the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.multiply
Multiplies the first value by the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.neighborhoodToArray
Turns the neighborhood of each pixel in a scalar image into a 2D array. Axes 0 and 1 of the output array correspond to Y and X axes of the image, respectively. The output image will have as many bands as the input; each output band has the same mask as the corresponding input band. The footprint and metadata of the input image are preserved.

===

## ee.Image.neighborhoodToBands
Turns the neighborhood of a pixel into a set of bands. The neighborhood is specified using a Kernel and only non-zero-weight kernel values are used. The weights of the kernel is otherwise ignored. Each input band produces x * y output bands. Each output band is named 'input_x_y' where x and y indicate the pixel's location in the kernel. For example, a 3x3 kernel operating on a 2-band image produces 18 output bands.

===

## ee.Image.neq
Returns 1 if and only if the first value is not equal to the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.normalizedDifference
Computes the normalized difference between two bands. If the bands to use are not specified, uses the first two bands. The normalized difference is computed as (first − second) / (first + second). Note that the returned image band name is 'nd', the input image properties are not retained in the output image, and a negative pixel value in either input band will cause the output pixel to be masked. To avoid masking negative input values, use ee.Image.expression() to compute normalized difference.

===

## ee.Image.not
Returns 0 if the input is non-zero, and 1 otherwise.

===

## ee.Image.or
Returns 1 if and only if either input value is non-zero for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is boolean.

===

## ee.Image.paint
Paints the geometries of a collection onto an image, using the given 'color' value to replace each band's values where any geometry covers the image (or, if a line width is specified, where the perimeters do). This algorithm is most suitable for converting categorical data from feature properties to pixels in an image; if you wish to visualize a collection, consider using FeatureCollection.style instead, which supports RGB colors whereas this algorithm is strictly 'monochrome' (using single numeric values).

===

## ee.Image.pixelArea
Generate an image in which the value of each pixel is the area of that pixel in square meters. The returned image has a single band called "area." No arguments.

===

## ee.Image.pixelCoordinates
Creates a two-band image containing the x and y coordinates of each pixel in the given projection.

===

## ee.Image.pixelLonLat
Creates an image with two bands named 'longitude' and 'latitude', containing the longitude and latitude at each pixel, in degrees. No arguments.

===

## ee.Image.polynomial
Compute a polynomial at each pixel using the given coefficients.

===

## ee.Image.pow
Raises the first value to the power of the second for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is float.

===

## ee.Image.projection
Returns the default projection of an Image. Throws an error if the bands of the image don't all have the same projection.

===

## ee.Image.propertyNames
Returns the names of properties on this element.

===

## ee.Image.random
Generates a random number at each pixel location. When using the 'uniform' distribution, outputs are in the range of [0, 1). Using the 'normal' distribution, the outputs have μ=0, σ=1, but no explicit limits.

===

## ee.Image.randomVisualizer
Creates a visualization image by assigning a random color to each unique value of the pixels of the first band. The first three bands of the output image will contain 8-bit R, G and B values, followed by all bands of the input image.

===

## ee.Image.reduce
Applies a reducer to all of the bands of an image. The reducer must have a single input and will be called at each pixel to reduce the stack of band values. The output image will have one band for each reducer output.

===

## ee.Image.reduceConnectedComponents
Applies a reducer to all of the pixels inside of each 'object'. Pixels are considered to belong to an object if they are connected (8-way) and have the same value in the 'label' band. The label band is only used to identify the connectedness; the rest are provided as inputs to the reducer.

===

## ee.Image.reduceNeighborhood
Applies the given reducer to the neighborhood around each pixel, as determined by the given kernel. If the reducer has a single input, it will be applied separately to each band of the collection; otherwise it must have the same number of inputs as the input image has bands. The reducer output names determine the names of the output bands: reducers with multiple inputs will use the output names directly, while reducers with a single input will prefix the output name with the input band name (e.g., '10_mean', '20_mean'). Reducers with weighted inputs can have the input weight based on the input mask, the kernel value, or the smaller of those two.

===

## ee.Image.reduceRegion
Apply a reducer to all the pixels in a specific region. Either the reducer must have the same number of inputs as the input image has bands, or it must have a single input and will be repeated for each band. Returns a dictionary of the reducer's outputs.

===

## ee.Image.reduceRegions
Apply a reducer over the area of each feature in the given collection. The reducer must have the same number of inputs as the input image has bands. Returns the input features, each augmented with the corresponding reducer outputs.

===

## ee.Image.reduceResolution
Enables reprojection using the given reducer to combine all input pixels corresponding to each output pixel. If the reducer has a single input, it will be applied separately to each band of the collection; otherwise it must have the same number of inputs as the input image has bands. The reducer output names determine the names of the output bands: reducers with multiple inputs will use the output names directly, reducers with a single input and single output will preserve the input band names, and reducers with a single input and multiple outputs will prefix the output name with the input band name (e.g., '10_mean', '10_stdDev', '20_mean', '20_stdDev'). Reducer input weights will be the product of the input mask and the fraction of the output pixel covered by the input pixel.

===

## ee.Image.reduceToVectors
Convert an image to a feature collection by reducing homogeneous regions. Given an image containing a band of labeled segments and zero or more additional bands, runs a reducer over the pixels in each segment producing a feature per segment. Either the reducer must have one fewer inputs than the image has bands, or it must have a single input and will be repeated for each band.

===

## ee.Image.regexpRename
Renames the bands of an image by applying a regular expression replacement to the current band names. Any bands not matched by the regex will be copied over without renaming.

===

## ee.Image.register
Registers an image to a reference image while allowing local, rubber sheet deformations. Displacements are computed in the CRS of the reference image, at a scale dictated by the lowest resolution of the following three projections: input image projection, reference image projection, and requested projection. The displacements then applied to the input image to register it with the reference.

===

## ee.Image.remap
Maps from input values to output values, represented by two parallel lists. Any input values not included in the input list are either set to defaultValue if it is given or masked if it isn't. Note that inputs containing floating point values might sometimes fail to match due to floating point precision errors.

===

## ee.Image.rename
Rename the bands of an image. Returns the renamed image.

===

## ee.Image.reproject
Force an image to be computed in a given projection and resolution.

===

## ee.Image.resample
An algorithm that returns an image identical to its argument, but which uses bilinear or bicubic interpolation (rather than the default nearest-neighbor) to compute pixels in projections other than its native projection or other levels of the same image pyramid. This relies on the input image's default projection being meaningful, and so cannot be used on composites, for example. (Instead, you should resample the images that are used to create the composite.)

===

## ee.Image.rgb
Create a 3-band image specifically for visualization. This uses the first band in each image. Returns the combined image.

===

## ee.Image.rgbToHsv
Transforms the image from the RGB color space to the HSV color space. Expects a 3-band image in the range [0, 1], and produces 3 bands: hue, saturation and value with values in the range [0, 1].

===

## ee.Image.rightShift
Calculates the signed right shift of v1 by v2 bits for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.round
Computes the integer nearest to the input.

===

## ee.Image.rsedTransform
Reverse Squared Euclidean Distance (RSED) computes the 2D maximal height surface created by placing an inverted parabola over each non-zero pixel of the input image, where the pixel's value is the height of the parabola. Viewed as a binary image (zero/not-zero) this is equivalent to buffering each non-zero input pixel by the square root of its value, in pixels.

===

## ee.Image.sample
Samples the pixels of an image, returning them as a FeatureCollection. Each feature will have 1 property per band in the input image. Note that the default behavior is to drop features that intersect masked pixels, which result in null-valued properties (see dropNulls argument).

===

## ee.Image.sampleRectangle
Extracts a rectangular region of pixels from an image into a ND array per band. The arrays are returned in a feature retaining the same properties as the image and a geometry the same as that used to sample the image (or the image footprint if unspecified). Each band is sampled in its input projection, and if no geometry is specified, sampled using its footprint. For scalar bands, the output array is 2D. For array bands the output array is (2+N)D where N is the number of dimensions in the original band. If sampling array bands, all arrays must have the same number of elements. If a band's sampled region is entirely masked and a default array value is specified, the default array value is used in-lieu of sampling the image.

===

## ee.Image.sampleRegions
Converts each pixel of an image (at a given scale) that intersects one or more regions to a Feature, returning them as a FeatureCollection. Each output feature will have one property per band of the input image, as well as any specified properties copied from the input feature. Note that geometries will be snapped to pixel centers.

===

## ee.Image.select
Selects bands from an image. Returns an image with the selected bands.

===

## ee.Image.selfMask
Updates an image's mask at all positions where the existing mask is not zero using the value of the image as the new mask value. The output image retains the metadata and footprint of the input image.

===

## ee.Image.serialize
Returns the serialized representation of this object.

===

## ee.Image.set
Overrides one or more metadata properties of an Element. Returns the element with the specified properties overridden.

===

## ee.Image.setDefaultProjection
Set a default projection to be applied to this image. The projection's resolution may be overridden by later operations.

===

## ee.Image.short
Casts the input value to a signed 16-bit integer.

===

## ee.Image.signum
Computes the signum function (sign) of the input; 0 if the input is 0, 1 if the input is greater than 0, -1 if the input is less than 0.

===

## ee.Image.sin
Computes the sine of the input in radians.

===

## ee.Image.sinh
Computes the hyperbolic sine of the input.

===

## ee.Image.sldStyle
Styles a raster input with the provided OGC SLD styling. Points of note: * OGC SLD 1.0 and OGC SE 1.1 are supported. * The XML document passed in can be complete, or just the   SldRasterSymbolizer element and down. * Exactly one SldRasterSymbolizer is required. * Bands may be selected by their proper Earth Engine names or   using numeric identifiers ("1", "2", ...). Proper   EarthEngine names are tried first. * The Histogram and Normalize contrast stretch mechanisms are   supported. * The type="values", type="intervals" and type="ramp"   attributes for ColorMap element in SLD 1.0 (GeoServer   extensions) are supported. * Opacity is only taken into account when it is 0.0   (transparent). Non-zero opacity values are treated as   completely opaque. * The OverlapBehavior definition is currently ignored. * The ShadedRelief mechanism is not currently supported. * The ImageOutline mechanism is not currently supported. * The Geometry element is ignored. The output image will have histogram_bandname metadata if histogram equalization or normalization is requested.

===

## ee.Image.slice
Selects a contiguous group of bands from an image by position.

===

## ee.Image.spectralDilation
Computes the spectral/spatial dilation of an image by computing the spectral distance of each pixel under a structuring kernel from the centroid of all pixels under the kernel and taking the most distant result. See 'Spatial/spectral endmember extraction by multidimensional morphological operations.' IEEE transactions on geoscience and remote sensing 40.9 (2002): 2025-2041.

===

## ee.Image.spectralDistance
Computes the per-pixel spectral distance between two images. If the images are array based then only the first band of each image is used; otherwise all bands are involved in the distance computation. The two images are therefore expected to contain the same number of bands or have the same 1-dimensional array length.

===

## ee.Image.spectralErosion
Computes the spectral/spatial erosion of an image by computing the spectral distance of each pixel under a structuring kernel from the centroid of all pixels under the kernel and taking the closest result. See 'Spatial/spectral endmember extraction by multidimensional morphological operations.' IEEE transactions on geoscience and remote sensing 40.9 (2002): 2025-2041.

===

## ee.Image.spectralGradient
Computes the spectral gradient over all bands of an image (or the first band if the image is Array typed) by computing the per-pixel difference between the spectral erosion and dilation with a given structuring kernel and distance metric. See: Plaza, Antonio, et al. 'Spatial/spectral endmember extraction by multidimensional morphological operations.' IEEE transactions on geoscience and remote sensing 40.9 (2002): 2025-2041.

===

## ee.Image.sqrt
Computes the square root of the input.

===

## ee.Image.stratifiedSample
Extracts a stratified random sample of points from an image. Extracts the specified number of samples for each distinct value discovered within the 'classBand'. Returns a FeatureCollection of 1 Feature per extracted point, with each feature having 1 property per band in the input image. If there are less than the specified number of samples available for a given class value, then all of the points for that class will be included. Requires that the classBand contain integer values.

===

## ee.Image.subtract
Subtracts the second value from the first for each matched pair of bands in image1 and image2. If either image1 or image2 has only 1 band, then it is used against all the bands in the other image. If the images have the same number of bands, but not the same names, they're used pairwise in the natural order. The output bands are named for the longer of the two inputs, or if they're equal in length, in image1's order. The type of the output pixels is the union of the input types.

===

## ee.Image.tan
Computes the tangent of the input in radians.

===

## ee.Image.tanh
Computes the hyperbolic tangent of the input.

===

## ee.Image.toArray
Concatenates pixels from each band into a single array per pixel. The result will be masked if any input bands are masked.

===

## ee.Image.toByte
Casts the input value to an unsigned 8-bit integer.

===

## ee.Image.toDictionary
Extract properties from a feature as a dictionary.

===

## ee.Image.toDouble
Casts the input value to a 64-bit float.

===

## ee.Image.toFloat
Casts the input value to a 32-bit float.

===

## ee.Image.toInt
Casts the input value to a signed 32-bit integer.

===

## ee.Image.toInt16
Casts the input value to a signed 16-bit integer.

===

## ee.Image.toInt32
Casts the input value to a signed 32-bit integer.

===

## ee.Image.toInt64
Casts the input value to a signed 64-bit integer.

===

## ee.Image.toInt8
Casts the input value to a signed 8-bit integer.

===

## ee.Image.toLong
Casts the input value to a signed 64-bit integer.

===

## ee.Image.toShort
Casts the input value to a signed 16-bit integer.

===

## ee.Image.toUint16
Casts the input value to an unsigned 16-bit integer.

===

## ee.Image.toUint32
Casts the input value to an unsigned 32-bit integer.

===

## ee.Image.toUint8
Casts the input value to an unsigned 8-bit integer.

===

## ee.Image.translate
Translate the input image.

===

## ee.Image.trigamma
Computes the trigamma function of the input.

===

## ee.Image.uint16
Casts the input value to an unsigned 16-bit integer.

===

## ee.Image.uint32
Casts the input value to an unsigned 32-bit integer.

===

## ee.Image.uint8
Casts the input value to an unsigned 8-bit integer.

===

## ee.Image.unitScale
Scales the input so that the range of input values [low, high] becomes [0, 1]. Values outside the range are NOT clamped. This algorithm always produces floating point pixels.

===

## ee.Image.unmask
Replaces mask and value of the input image with the mask and value of another image at all positions where the input mask is zero. The output image retains the metadata of the input image. By default, the output image also retains the footprint of the input, but setting sameFootprint to false allows to extend the footprint.

===

## ee.Image.unmix
Unmix each pixel with the given endmembers, by computing the pseudo-inverse and multiplying it through each pixel. Returns an image of doubles with the same number of bands as endmembers.

===

## ee.Image.updateMask
Updates an image's mask at all positions where the existing mask is not zero. The output image retains the metadata and footprint of the input image.

===

## ee.Image.visualize
Produces an RGB or grayscale visualization of an image. Each of the gain, bias, min, max and gamma arguments can take either a single value, which will be applied to all bands, or a list of values the same length as bands.

===

## ee.Image.where
Performs conditional replacement of values. For each pixel in each band of 'input', if the corresponding pixel in 'test' is nonzero, output the corresponding pixel in value, otherwise output the input pixel. If at a given pixel, either test or value is masked, the input value is used. If the input is masked, nothing is done. The output bands have the same names as the input bands. The output type of each band is the larger of the input and value types. The output image retains the metadata and footprint of the input image.

===

## ee.Image.zeroCrossing
Finds zero-crossings on each band of an image.

===

## ee.ImageCollection
ImageCollections can be constructed from the following arguments: - A string: assumed to be the name of a collection, - A list of images, or anything that can be used to construct an image. - A single image. - A computed object - reinterpreted as a collection.

===

## ee.ImageCollection.aggregate_array
Aggregates over a given property of the objects in a collection, calculating a list of all the values of the selected property.

===

## ee.ImageCollection.aggregate_count
Aggregates over a given property of the objects in a collection, calculating the number of non-null values of the property.

===

## ee.ImageCollection.aggregate_count_distinct
Aggregates over a given property of the objects in a collection, calculating the number of distinct values for the selected property.

===

## ee.ImageCollection.aggregate_first
Aggregates over a given property of the objects in a collection, calculating the property value of the first object in the collection.

===

## ee.ImageCollection.aggregate_histogram
Aggregates over a given property of the objects in a collection, calculating a histogram of the selected property.

===

## ee.ImageCollection.aggregate_max
Aggregates over a given property of the objects in a collection, calculating the maximum of the values of the selected property.

===

## ee.ImageCollection.aggregate_mean
Aggregates over a given property of the objects in a collection, calculating the mean of the selected property.

===

## ee.ImageCollection.aggregate_min
Aggregates over a given property of the objects in a collection, calculating the minimum of the values of the selected property.

===

## ee.ImageCollection.aggregate_product
Aggregates over a given property of the objects in a collection, calculating the product of the values of the selected property.

===

## ee.ImageCollection.aggregate_sample_sd
Aggregates over a given property of the objects in a collection, calculating the sample std. deviation of the values of the selected property.

===

## ee.ImageCollection.aggregate_sample_var
Aggregates over a given property of the objects in a collection, calculating the sample variance of the values of the selected property.

===

## ee.ImageCollection.aggregate_stats
Aggregates over a given property of the objects in a collection, calculating the sum, min, max, mean, sample standard deviation, sample variance, total standard deviation and total variance of the selected property.

===

## ee.ImageCollection.aggregate_sum
Aggregates over a given property of the objects in a collection, calculating the sum of the values of the selected property.

===

## ee.ImageCollection.aggregate_total_sd
Aggregates over a given property of the objects in a collection, calculating the total std. deviation of the values of the selected property.

===

## ee.ImageCollection.aggregate_total_var
Aggregates over a given property of the objects in a collection, calculating the total variance of the values of the selected property.

===

## ee.ImageCollection.and
Reduces an image collection by setting each pixel to 1 if and only if all the non-masked values at that pixel are non-zero across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.ImageCollection.bounds
Constructs a bounding box around the geometries in a collection.

===

## ee.ImageCollection.cast
Casts some or all bands of each image in an ImageCollection to the specified types.

===

## ee.ImageCollection.combine
Makes a new collection that is a copy of the images in primary, adding all the bands from the image in secondary with a matching ID. If there are no matching IDs, the resulting collection will be empty. This is equivalent to an inner join on ID with merging of the bands of the result. Note that this algorithm assumes that for a matching pair of inputs, both have the same footprint and metadata.

===

## ee.ImageCollection.copyProperties
Copies metadata properties from one element to another.

===

## ee.ImageCollection.count
Reduces an image collection by calculating the number of images with a valid mask at each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.distance
Produces a DOUBLE image where each pixel is the distance in meters from the pixel center to the nearest Point, LineString, or polygonal boundary in the collection. Note distance is also measured within interiors of polygons. Pixels that are not within 'searchRadius' meters of a geometry will be masked out. Distances are computed on a sphere, so there is a small error proportional to the latitude difference between each pixel and the nearest geometry.

===

## ee.ImageCollection.distinct
Removes duplicates from a collection. Note that duplicates are determined using a strong hash over the serialized form of the selected properties.

===

## ee.ImageCollection.draw
Paints a vector collection for visualization. Not intended for use as input to other algorithms.

===

## ee.ImageCollection.errorMatrix
Computes a 2D error matrix for a collection by comparing two columns of a collection: one containing the actual values, and one containing predicted values. The values are expected to be small contiguous integers, starting from 0. Axis 0 (the rows) of the matrix correspond to the actual values, and Axis 1 (the columns) to the predicted values.

===

## ee.ImageCollection.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.ImageCollection.filter
Apply a filter to this collection. Returns the filtered collection.

===

## ee.ImageCollection.filterBounds
Shortcut to filter a collection by intersection with geometry. Items in the collection with a footprint that fails to intersect the given geometry will be excluded. This is equivalent to this.filter(ee.Filter.bounds(...)). Caution: providing a large or complex collection as the geometry argument can result in poor performance. Collating the geometry of collections does not scale well; use the smallest collection (or geometry) that is required to achieve the desired outcome. Returns the filtered collection.

===

## ee.ImageCollection.filterDate
Shortcut to filter a collection by a date range. The start and end may be Dates, numbers (interpreted as milliseconds since 1970-01-01T00:00:00Z), or strings (such as '1996-01-01T08:00'). Based on 'system:time_start'. This is equivalent to this.filter(ee.Filter.date(...)); see the ee.Filter type for other date filtering options. Returns the filtered collection.

===

## ee.ImageCollection.first
Returns the first entry from a given collection.

===

## ee.ImageCollection.flatten
Flattens collections of collections.

===

## ee.ImageCollection.formaTrend
Computes the long and short term trends of a time series or optionally, the trends of the ratio of the time series and a covariate. The long term trend is estimated from the linear term of a regression on the full time series. The short term trend is computed as the windowed minimum over the time series. The time series and covariate series are expected to contain a single band each, and the time series is expected to be evenly spaced in time. The output is 4 float bands: the long and short term trends, the t-test of the long term trend against the time series, and the Bruce Hansen test of parameter stability.

===

## ee.ImageCollection.fromImages
Returns the image collection containing the given images.

===

## ee.ImageCollection.geometry
Extracts and merges the geometries of a collection. Requires that all the geometries in the collection share the projection and edge interpretation. Caution: providing a large or complex collection as input can result in poor performance. Collating the geometry of collections does not scale well; use the smallest collection that is required to achieve the desired outcome. Note: If only a bounding box around the collection is needed, consider using Collection.bounds instead.

===

## ee.ImageCollection.get
Extract a property from a feature.

===

## ee.ImageCollection.getArray
Extract a property from a feature.

===

## ee.ImageCollection.getFilmstripThumbURL
Get the URL of a tiled thumbnail for this ImageCollection. Returns a thumbnail URL, or undefined if a callback was specified.

===

## ee.ImageCollection.getInfo
An imperative function that returns all the known information about this collection via an AJAX call. Returns a collection description whose fields include: - features: a list containing metadata about the images in the collection. - bands: a dictionary describing the bands of the images in this collection. - properties: an optional dictionary containing the collection's metadata properties.

===

## ee.ImageCollection.getMapId
An imperative function that returns a map ID via a synchronous AJAX call. This mosaics the collection to a single image and return a map ID suitable for building a Google Maps overlay. Returns returns a map ID and optional token, which may be passed to ee.data.getTileUrl or ui.Map.addLayer. Undefined if a callback was specified.

===

## ee.ImageCollection.getNumber
Extract a property from a feature.

===

## ee.ImageCollection.getRegion
Output an array of values for each [pixel, band, image] tuple in an ImageCollection. The output contains rows of id, lon, lat, time, and all bands for each image that intersects each pixel in the given region. Attempting to extract more than 1048576 values will result in an error.

===

## ee.ImageCollection.getString
Extract a property from a feature.

===

## ee.ImageCollection.getVideoThumbURL
Get the URL of an animated thumbnail for this ImageCollection. Returns a thumbnail URL, or undefined if a callback was specified.

===

## ee.ImageCollection.iterate
Applies a user-supplied function to each element of a collection. The user-supplied function is given two arguments: the current element, and the value returned by the previous call to iterate() or the first argument, for the first iteration. The result is the value returned by the final call to the user-supplied function. Returns the result of the Collection.iterate() call.

===

## ee.ImageCollection.limit
Limit a collection to the specified number of elements, optionally sorting them by a specified property first. Returns the limited collection.

===

## ee.ImageCollection.linkCollection
Links images in this collection to matching images from imageCollection . For each source image in this collection, any specified bands or metadata will be added to the source image from the matching image found in imageCollection . If bands or metadata are already present, they will be overwritten. If matching images are not found, any new or updated bands will be fully masked and any new or updated metadata will be null. The output footprint will be the same as the source image footprint. Matches are determined if a source image and an image in imageCollection have a specific equivalent metadata property. If more than one collection image would match, the collection image selected is arbitrary. By default, images are matched on their 'system:index' metadata property. This linking function is a convenience method for adding bands to target images based on a specified shared metadata property and is intended to support linking collections that apply different processing/product generation to the same source imagery. For more expressive linking known as 'joining', see https://developers.google.com/earth-engine/guides/joins_intro. Returns the linked image collection.

===

## ee.ImageCollection.load
Returns the image collection given its ID.

===

## ee.ImageCollection.loadZarrV2Array
Loads a Zarr v2 array with 3 or more dimensions (i.e., 1 or more non-spatial dimensions) as an ImageCollection by slicing along a specified non-spatial axis. The array attributes (.zattrs) must contain the field '_ARRAY_DIMENSIONS', which is a list of the names of each dimension (e.g., ['time', 'y', 'x']). There must be at least two dimensions, with the final two representing Y and X respectively (e.g., ['lat', 'lon']). The supported compression codecs are 'blosc', 'gzip', 'lz4', 'zlib', and 'zstd'. The supported blosc meta-compression codecs are 'lz4', lz4hc', 'zlib', and 'zstd' ('blosclz' is not supported).

===

## ee.ImageCollection.map
Maps an algorithm over a collection. Returns the mapped collection.

===

## ee.ImageCollection.max
Reduces an image collection by calculating the maximum value of each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.mean
Reduces an image collection by calculating the mean of all values at each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.median
Reduces an image collection by calculating the median of all values at each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.merge
Merges two image collections into one. The result has all the images that were in either collection.

===

## ee.ImageCollection.min
Reduces an image collection by calculating the minimum value of each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.mode
Reduces an image collection by calculating the most common value at each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.mosaic
Composites all the images in a collection, using the mask.

===

## ee.ImageCollection.or
Reduces an image collection by setting each pixel to 1 if and only if any of the non-masked values at that pixel are non-zero across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.product
Reduces an image collection by calculating the product of all values at each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.propertyNames
Returns the names of properties on this element.

===

## ee.ImageCollection.qualityMosaic
Composites all the images in a collection, using a quality band as a per-pixel ordering function.

===

## ee.ImageCollection.randomColumn
Adds a column of deterministic pseudorandom numbers to a collection. The outputs are double-precision floating point numbers. When using the 'uniform' distribution (default), outputs are in the range of [0, 1). Using the 'normal' distribution, outputs have μ=0, σ=1, but have no explicit limits.

===

## ee.ImageCollection.reduce
Applies a reducer across all of the images in a collection. If the reducer has a single input, it will be applied separately to each band of the collection; otherwise it must have the same number of inputs as the collection has bands. The reducer output names determine the names of the output bands: reducers with multiple inputs will use the output names directly, while reducers with a single input will prefix the output name with the input band name (e.g., '10_mean', '20_mean').

===

## ee.ImageCollection.reduceColumns
Apply a reducer to each element of a collection, using the given selectors to determine the inputs. Returns a dictionary of results, keyed with the output names.

===

## ee.ImageCollection.reduceToImage
Creates an image from a feature collection by applying a reducer over the selected properties of all the features that intersect each pixel.

===

## ee.ImageCollection.remap
Remaps the value of a specific property in a collection. Takes two parallel lists and maps values found in one to values in the other. Any element with a value that is not specified in the first list is dropped from the output collection.

===

## ee.ImageCollection.select
Select bands from each image in a collection. Returns the image collection with selected bands.

===

## ee.ImageCollection.serialize
Returns the serialized representation of this object.

===

## ee.ImageCollection.set
Overrides one or more metadata properties of an Element. Returns the element with the specified properties overridden.

===

## ee.ImageCollection.size
Returns the number of elements in the collection.

===

## ee.ImageCollection.sort
Sort a collection by the specified property. Returns the sorted collection.

===

## ee.ImageCollection.style
Draw a vector collection for visualization using a simple style language.

===

## ee.ImageCollection.sum
Reduces an image collection by calculating the sum of all values at each pixel across the stack of all matching bands. Bands are matched by name.

===

## ee.ImageCollection.toArray
Converts an image collection into an image of 2D arrays. At each pixel, the images that have valid (unmasked) values in all bands are laid out along the first axis of the array in the order they appear in the image collection. The bands of each image are laid out along the second axis of the array, in the order the bands appear in that image. The array element type will be the union of the types of each band.

===

## ee.ImageCollection.toArrayPerBand
Concatenates multiple images into a single array image. The result will be masked if any input is masked.

===

## ee.ImageCollection.toBands
Converts a collection to a single multi-band image containing all of the bands of every image in the collection. Output bands are named by prefixing the existing band names with the image id from which it came (e.g., 'image1_band1'). Note: The maximum number of bands is 5000.

===

## ee.ImageCollection.toDictionary
Extract properties from a feature as a dictionary.

===

## ee.ImageCollection.toList
Returns the elements of a collection as a list.

===

## ee.ImageCollection.union
Merges all geometries in a given collection into one and returns a collection containing a single feature with only an ID of 'union_result' and a geometry.

===

## ee.Join.apply
Joins two collections.

===

## ee.Join.inner
Returns a join that pairs elements from the primary collection with matching elements from the secondary collection. Each result has a 'primary' property that contains the element from the primary collection, and a 'secondary' property containing the matching element from the secondary collection. If measureKey is specified, the join measure is also attached to the object as a property.

===

## ee.Join.inverted
Returns a join that produces the elements of the primary collection that match no elements of the secondary collection. No properties are added to the results. No arguments.

===

## ee.Join.saveAll
Returns a join that pairs each element from the first collection with a group of matching elements from the second collection. The list of matches is added to each result as an additional property. If measureKey is specified, each match has the value of its join measure attached. Join measures are produced when withinDistance or maxDifference filters are used as the join condition.

===

## ee.Join.saveBest
Returns a join that pairs each element from the first collection with a matching element from the second collection. The match with the best join measure is added to each result as an additional property. Join measures are produced when withinDistance or maxDifference filters are used as the join condition.

===

## ee.Join.saveFirst
Returns a join that pairs each element from the first collection with a matching element from the second collection. The first match is added to the result as an additional property.

===

## ee.Join.simple
Returns a join that produces the elements of the primary collection that match any element of the secondary collection. No properties are added to the results. No arguments.

===

## ee.Kernel.add
Adds two kernels (pointwise) after aligning their centers.

===

## ee.Kernel.chebyshev
Generates a distance kernel based on Chebyshev distance (greatest distance along any dimension).

===

## ee.Kernel.circle
Generates a circle-shaped boolean kernel.

===

## ee.Kernel.compass
Generates a 3x3 Prewitt's Compass edge-detection kernel.

===

## ee.Kernel.cross
Generates a cross-shaped boolean kernel.

===

## ee.Kernel.diamond
Generates a diamond-shaped boolean kernel.

===

## ee.Kernel.euclidean
Generates a distance kernel based on Euclidean (straight-line) distance.

===

## ee.Kernel.fixed
Creates a Kernel.

===

## ee.Kernel.gaussian
Generates a Gaussian kernel from a sampled continuous Gaussian.

===

## ee.Kernel.inverse
Returns a kernel which has each of its weights multiplicatively inverted. Weights with a value of zero are not inverted and remain zero.

===

## ee.Kernel.kirsch
Generates a 3x3 Kirsch's Compass edge-detection kernel.

===

## ee.Kernel.laplacian4
Generates a 3x3 Laplacian-4 edge-detection kernel.

===

## ee.Kernel.laplacian8
Generates a 3x3 Laplacian-8 edge-detection kernel.

===

## ee.Kernel.manhattan
Generates a distance kernel based on rectilinear (city-block) distance.

===

## ee.Kernel.octagon
Generates an octagon-shaped boolean kernel.

===

## ee.Kernel.plus
Generates a plus-shaped boolean kernel.

===

## ee.Kernel.prewitt
Generates a 3x3 Prewitt edge-detection kernel.

===

## ee.Kernel.rectangle
Generates a rectangular-shaped kernel.

===

## ee.Kernel.roberts
Generates a 2x2 Roberts edge-detection kernel.

===

## ee.Kernel.rotate
Creates a Kernel.

===

## ee.Kernel.sobel
Generates a 3x3 Sobel edge-detection kernel.

===

## ee.Kernel.square
Generates a square-shaped boolean kernel.

===

## ee.List
Constructs a new list.

===

## ee.List.add
Appends the element to the end of list.

===

## ee.List.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.List.cat
Concatenates the contents of other onto list.

===

## ee.List.contains
Returns true if list contains element.

===

## ee.List.containsAll
Returns true if list contains all of the elements of other, regardless of order.

===

## ee.List.distinct
Returns a copy of list without duplicate elements.

===

## ee.List.equals
Returns true if list contains the same elements as other, in the same order.

===

## ee.List.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.List.filter
Filters a list to only the elements that match the given filter. To filter list items that aren't images or features, test a property named 'item', e.g., ee.Filter.gt('item', 3).

===

## ee.List.flatten
Flattens any sublists into a single list.

===

## ee.List.frequency
Returns the number of elements in list equal to element.

===

## ee.List.get
Returns the element at the specified position in list. A negative index counts backwards from the end of the list.

===

## ee.List.getArray
Returns the array at the specified position in list. A negative index counts backwards from the end of the list. If the value is not a array, an error will occur.

===

## ee.List.getGeometry
Returns the geometry at the specified position in list. A negative index counts backwards from the end of the list. If the value is not a geometry, an error will occur.

===

## ee.List.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.List.getNumber
Returns the number at the specified position in list. A negative index counts backwards from the end of the list. If the value is not a number, an error will occur.

===

## ee.List.getString
Returns the string at the specified position in list. A negative index counts backwards from the end of the list. If the value is not a string, an error will occur.

===

## ee.List.indexOf
Returns the position of the first occurrence of element in list, or -1 if list does not contain element.

===

## ee.List.indexOfSublist
Returns the starting position of the first occurrence of target within list, or -1 if there is no such occurrence.

===

## ee.List.insert
Inserts element at the specified position in list. A negative index counts backwards from the end of the list.

===

## ee.List.iterate
Iterate an algorithm over a list. The algorithm is expected to take two objects, the current list item, and the result from the previous iteration or the value of first for the first iteration.

===

## ee.List.join
Returns a string containing the elements of the list joined together with the specified separator between elements. Note: The string form of list elements which are not strings, numbers, or booleans is currently not well-defined and subject to change.

===

## ee.List.lastIndexOfSubList
Returns the starting position of the last occurrence of target within list, or -1 if there is no such occurrence.

===

## ee.List.length
Returns the number of elements in list.

===

## ee.List.map
Map an algorithm over a list. The algorithm is expected to take an Object and return an Object.

===

## ee.List.reduce
Apply a reducer to a list. If the reducer takes more than 1 input, then each element in the list is assumed to be a list of inputs. If the reducer returns a single output, it is returned directly, otherwise returns a dictionary containing the named reducer outputs.

===

## ee.List.remove
Removes the first occurrence of the specified element from list, if it is present.

===

## ee.List.removeAll
Removes from list all of the elements that are contained in other list.

===

## ee.List.repeat
Returns a new list containing value repeated count times.

===

## ee.List.replace
Replaces the first occurrence of oldval in list with newval.

===

## ee.List.replaceAll
Replaces all occurrences of oldval in list with newval.

===

## ee.List.reverse
Reverses the order of the elements in list.

===

## ee.List.rotate
Rotates the elements of the list by the specified distance.

===

## ee.List.sequence
Generate a sequence of numbers from start to end (inclusive) in increments of step, or in count equally-spaced increments. If end is not specified it is computed from start + step * count, so at least one of end or count must be specified.

===

## ee.List.serialize
Returns the serialized representation of this object.

===

## ee.List.set
Replaces the value at the specified position in list with element. A negative index counts backwards from the end of the list.

===

## ee.List.shuffle
Randomly permute the specified list. Note that the permutation order will always be the same for any given seed, unless the value for seed is 'false'.

===

## ee.List.size
Returns the number of elements in list.

===

## ee.List.slice
Returns a portion of list between the start index, inclusive, and end index, exclusive. Negative values for start or end count backwards from the end of the list. Values greater than the size of the list are legal but are truncated to the size of list.

===

## ee.List.sort
Sorts the list into ascending order. If the 'keys' argument is provided, then it is sorted first, and the elements of 'list' are placed in the same order.

===

## ee.List.splice
Starting at the start index, removes count elements from list and insert the contents of other at that location. If start is negative, it counts backwards from the end of the list.

===

## ee.List.swap
Swaps the elements at the specified positions. A negative position counts backwards from the end of the list.

===

## ee.List.unzip
Transposes a list of lists, extracting the first element of each inner list into one list the second elements into another, etc., up to the length of the shortest inner list. The remaining items are discarded. The result is a list of lists.

===

## ee.List.zip
Pairs the elements of two lists to create a list of two-element lists. When the input lists are of different sizes, the final list has the same size as the shortest one.

===

## ee.Model.fromVertexAi
Returns an ee.Model from a description of a Vertex AI model endpoint. (See https://cloud.google.com/vertex-ai). Warning: This method is in public preview and may undergo breaking changes.

===

## ee.Model.predictImage
Make predictions from pixel tiles of an image. The predictions are merged as bands with the input image. The model will receive 0s in place of masked pixels. The masks of predicted output bands are the minimum of the masks of the inputs.

===

## ee.Model.predictProperties
Make predictions for each feature in a collection. Predicted properties are merged with the properties of the input feature.

===

## ee.Number
Constructs a new Number.

===

## ee.Number.abs
Computes the absolute value of the input.

===

## ee.Number.acos
Computes the arccosine in radians of the input.

===

## ee.Number.add
Adds the first value to the second.

===

## ee.Number.and
Returns 1 if and only if both values are non-zero.

===

## ee.Number.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.Number.asin
Computes the arcsine in radians of the input.

===

## ee.Number.atan
Computes the arctangent in radians of the input.

===

## ee.Number.atan2
Calculates the angle formed by the 2D vector [x, y].

===

## ee.Number.bitCount
Calculates the number of one-bits in the 64-bit two's complement binary representation of the input.

===

## ee.Number.bitwiseAnd
Calculates the bitwise AND of the input values.

===

## ee.Number.bitwiseNot
Calculates the bitwise NOT of the input, in the smallest signed integer type that can hold the input.

===

## ee.Number.bitwiseOr
Calculates the bitwise OR of the input values.

===

## ee.Number.bitwiseXor
Calculates the bitwise XOR of the input values.

===

## ee.Number.byte
Casts the input value to an unsigned 8-bit integer.

===

## ee.Number.cbrt
Computes the cubic root of the input.

===

## ee.Number.ceil
Computes the smallest integer greater than or equal to the input.

===

## ee.Number.clamp
Clamps the value to lie within the range of min to max.

===

## ee.Number.cos
Computes the cosine of the input in radians.

===

## ee.Number.cosh
Computes the hyperbolic cosine of the input.

===

## ee.Number.digamma
Computes the digamma function of the input.

===

## ee.Number.divide
Divides the first value by the second, returning 0 for division by 0.

===

## ee.Number.double
Casts the input value to a 64-bit float.

===

## ee.Number.eq
Returns 1 if and only if the first value is equal to the second.

===

## ee.Number.erf
Computes the error function of the input.

===

## ee.Number.erfInv
Computes the inverse error function of the input.

===

## ee.Number.erfc
Computes the complementary error function of the input.

===

## ee.Number.erfcInv
Computes the inverse complementary error function of the input.

===

## ee.Number.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.Number.exp
Computes the Euler's number e raised to the power of the input.

===

## ee.Number.expression
Computes a numeric expression.

===

## ee.Number.first
Selects the value of the first value.

===

## ee.Number.firstNonZero
Selects the first value if it is non-zero, and the second value otherwise.

===

## ee.Number.float
Casts the input value to a 32-bit float.

===

## ee.Number.floor
Computes the largest integer less than or equal to the input.

===

## ee.Number.format
Convert a number to a string using printf-style formatting.

===

## ee.Number.gamma
Computes the gamma function of the input.

===

## ee.Number.gammainc
Calculates the regularized lower incomplete Gamma function γ(x,a).

===

## ee.Number.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.Number.gt
Returns 1 if and only if the first value is greater than the second.

===

## ee.Number.gte
Returns 1 if and only if the first value is greater than or equal to the second.

===

## ee.Number.hypot
Calculates the magnitude of the 2D vector [x, y].

===

## ee.Number.int
Casts the input value to a signed 32-bit integer.

===

## ee.Number.int16
Casts the input value to a signed 16-bit integer.

===

## ee.Number.int32
Casts the input value to a signed 32-bit integer.

===

## ee.Number.int64
Casts the input value to a signed 64-bit integer.

===

## ee.Number.int8
Casts the input value to a signed 8-bit integer.

===

## ee.Number.lanczos
Computes the Lanczos approximation of the input.

===

## ee.Number.leftShift
Calculates the left shift of v1 by v2 bits.

===

## ee.Number.log
Computes the natural logarithm of the input.

===

## ee.Number.log10
Computes the base-10 logarithm of the input.

===

## ee.Number.long
Casts the input value to a signed 64-bit integer.

===

## ee.Number.lt
Returns 1 if and only if the first value is less than the second.

===

## ee.Number.lte
Returns 1 if and only if the first value is less than or equal to the second.

===

## ee.Number.max
Selects the maximum of the first and second values.

===

## ee.Number.min
Selects the minimum of the first and second values.

===

## ee.Number.mod
Calculates the remainder of the first value divided by the second.

===

## ee.Number.multiply
Multiplies the first value by the second.

===

## ee.Number.neq
Returns 1 if and only if the first value is not equal to the second.

===

## ee.Number.not
Returns 0 if the input is non-zero, and 1 otherwise.

===

## ee.Number.or
Returns 1 if and only if either input value is non-zero.

===

## ee.Number.parse
Convert a string to a number.

===

## ee.Number.pow
Raises the first value to the power of the second.

===

## ee.Number.rightShift
Calculates the signed right shift of v1 by v2 bits.

===

## ee.Number.round
Computes the integer nearest to the input.

===

## ee.Number.serialize
Returns the serialized representation of this object.

===

## ee.Number.short
Casts the input value to a signed 16-bit integer.

===

## ee.Number.signum
Computes the signum function (sign) of the input; 0 if the input is 0, 1 if the input is greater than 0, -1 if the input is less than 0.

===

## ee.Number.sin
Computes the sine of the input in radians.

===

## ee.Number.sinh
Computes the hyperbolic sine of the input.

===

## ee.Number.sqrt
Computes the square root of the input.

===

## ee.Number.subtract
Subtracts the second value from the first.

===

## ee.Number.tan
Computes the tangent of the input in radians.

===

## ee.Number.tanh
Computes the hyperbolic tangent of the input.

===

## ee.Number.toByte
Casts the input value to an unsigned 8-bit integer.

===

## ee.Number.toDouble
Casts the input value to a 64-bit float.

===

## ee.Number.toFloat
Casts the input value to a 32-bit float.

===

## ee.Number.toInt
Casts the input value to a signed 32-bit integer.

===

## ee.Number.toInt16
Casts the input value to a signed 16-bit integer.

===

## ee.Number.toInt32
Casts the input value to a signed 32-bit integer.

===

## ee.Number.toInt64
Casts the input value to a signed 64-bit integer.

===

## ee.Number.toInt8
Casts the input value to a signed 8-bit integer.

===

## ee.Number.toLong
Casts the input value to a signed 64-bit integer.

===

## ee.Number.toShort
Casts the input value to a signed 16-bit integer.

===

## ee.Number.toUint16
Casts the input value to an unsigned 16-bit integer.

===

## ee.Number.toUint32
Casts the input value to an unsigned 32-bit integer.

===

## ee.Number.toUint8
Casts the input value to an unsigned 8-bit integer.

===

## ee.Number.trigamma
Computes the trigamma function of the input.

===

## ee.Number.uint16
Casts the input value to an unsigned 16-bit integer.

===

## ee.Number.uint32
Casts the input value to an unsigned 32-bit integer.

===

## ee.Number.uint8
Casts the input value to an unsigned 8-bit integer.

===

## ee.Number.unitScale
Scales the input so that the range of input values [min, max] becomes [0, 1]. Values outside the range are NOT clamped.  If min == max, 0 is returned.

===

## ee.PixelType
Returns a PixelType of the given precision with the given limits per element, and an optional dimensionality.

===

## ee.PixelType.dimensions
Returns the number of dimensions for this type. Will be 0 for scalar values and >= 1 for array values.

===

## ee.PixelType.double
Returns the 64-bit floating point pixel type. No arguments.

===

## ee.PixelType.float
Returns the 32-bit floating point pixel type. No arguments.

===

## ee.PixelType.int16
Returns the 16-bit signed integer pixel type. No arguments.

===

## ee.PixelType.int32
Returns the 32-bit signed integer pixel type. No arguments.

===

## ee.PixelType.int64
Returns the 64-bit signed integer pixel type. No arguments.

===

## ee.PixelType.int8
Returns the 8-bit signed integer pixel type. No arguments.

===

## ee.PixelType.maxValue
Returns the maximum value of the PixelType.

===

## ee.PixelType.minValue
Returns the minimum value of the PixelType.

===

## ee.PixelType.precision
Returns the precision of the PixelType. One of 'int', 'float', or 'double'.

===

## ee.PixelType.uint16
Returns the 16-bit unsigned integer pixel type. No arguments.

===

## ee.PixelType.uint32
Returns the 32-bit unsigned integer pixel type. No arguments.

===

## ee.PixelType.uint8
Returns the 8-bit unsigned integer pixel type. No arguments.

===

## ee.Projection
Returns a Projection with the given base coordinate system and the given transform between projected coordinates and the base. If no transform is specified, the identity transform is assumed.

===

## ee.Projection.atScale
Returns the projection scaled such that its units have the given scale in linear meters, as measured at the point of true scale.

===

## ee.Projection.crs
Returns the authority code (e.g., 'EPSG:4326') for the base coordinate system of this projection, or null if the base coordinate system is not found in any available database.

===

## ee.Projection.nominalScale
Returns the linear scale in meters of the units of this projection, as measured at the point of true scale.

===

## ee.Projection.scale
Returns the projection scaled by the given amount in each axis.

===

## ee.Projection.transform
Returns a WKT representation of the transform of this Projection. This is the transform that converts from projected coordinates to the base coordinate system.

===

## ee.Projection.translate
Returns the projection translated by the given amount in each axis.

===

## ee.Projection.wkt
Returns a WKT representation of the base coordinate system of this Projection.

===

## ee.Reducer.allNonZero
Returns a Reducer that returns 1 if all of its inputs are non-zero, 0 otherwise. Where applicable, the output name is "all". No arguments.

===

## ee.Reducer.anyNonZero
Returns a Reducer that returns 1 if any of its inputs are non-zero, 0 otherwise. Where applicable, the output name is "any". No arguments.

===

## ee.Reducer.autoHistogram
Create a reducer that will compute a histogram of the inputs. The output is a Nx2 array of the lower bucket bounds and the counts (or cumulative counts) of each bucket and is suitable for use per-pixel.

===

## ee.Reducer.bitwiseAnd
Returns a Reducer that computes the bitwise-and summation of its inputs. Where applicable, the output name is "bitwiseAnd". No arguments.

===

## ee.Reducer.bitwiseOr
Returns a Reducer that computes the bitwise-or summation of its inputs. Where applicable, the output name is "bitwiseOr". No arguments.

===

## ee.Reducer.centeredCovariance
Creates a reducer that reduces some number of 1-D arrays of the same length N to a covariance matrix of shape NxN. WARNING: this reducer requires that the data has been mean centered. No arguments.

===

## ee.Reducer.circularMean
Returns a Reducer that computes the (weighted) circular mean of its inputs, which are expected to be in radians. Output will be in the range (-π to π). Where applicable, the output name is "circularMean". No arguments.

===

## ee.Reducer.circularStddev
Returns a Reducer that computes the (weighted) circular standard deviation of its inputs, which are expected to be in radians, using the sqrt(-2 * ln(R)) formula. Where applicable, the output name is "circularStdDev". No arguments.

===

## ee.Reducer.circularVariance
Returns a Reducer that computes the (weighted) circular variance of its inputs, which are expected to be in radians. Where applicable, the output name is "circularVariance". No arguments.

===

## ee.Reducer.combine
Creates a Reducer that runs two reducers in parallel. The combined reducer's outputs will be those of reducer1 followed by those of reducer2, where the output names of reducer2 are prefixed with the given string.

If sharedInputs is true, the reducers must have the same number of inputs, and the combined reducer's will match them; if it is false, the inputs of the combined reducer will be those of reducer1 followed by those of reducer2.

===

## ee.Reducer.count
Returns a Reducer that computes the number of non-null inputs. Where applicable, the output name is "count". No arguments.

===

## ee.Reducer.countDistinct
Returns a Reducer that computes the number of distinct inputs. Where applicable, the output name is "count". No arguments.

===

## ee.Reducer.countDistinctNonNull
Returns a Reducer that computes the number of distinct inputs, ignoring nulls. Where applicable, the output name is "count". No arguments.

===

## ee.Reducer.countEvery
Returns a Reducer that computes the number of inputs. Where applicable, the output name is "count". No arguments.

===

## ee.Reducer.countRuns
Returns a Reducer that computes the number of runs of distinct, non-null inputs. Where applicable, the output name is "count". No arguments.

===

## ee.Reducer.covariance
Creates a reducer that reduces some number of 1-D arrays of the same length N to a covariance matrix of shape NxN. This reducer uses the one-pass covariance formula from Sandia National Laboratories Technical Report SAND2008-6212, which can lose accuracy if the values span a large range. No arguments.

===

## ee.Reducer.disaggregate
Separates aggregate inputs (Arrays, Lists, or Dictionaries) into individual items that are then each passed to the specified reducer. When used on dictionaries, the dictionary keys are ignored. Non-aggregated inputs (e.g., Numbers or Strings) are passed to the underlying reducer directly.

===

## ee.Reducer.first
Returns a Reducer that returns the first of its inputs. Where applicable, the output name is "first". No arguments.

===

## ee.Reducer.firstNonNull
Returns a Reducer that returns the first of its non-null inputs. Where applicable, the output name is "first". No arguments.

===

## ee.Reducer.fixed2DHistogram
Creates a reducer that will compute a 2D histogram of the inputs using a fixed number of fixed-width bins. Values outside of the [min, max) range on either axis are ignored. The output is a 2D array of counts, and 2 1-D arrays of bucket lower edges for the xAxis and the yAxis. This reducer is suitable for use per-pixel, however it is always unweighted. The maximum count for any bucket is 2^31 - 1.

===

## ee.Reducer.fixedHistogram
Creates a reducer that will compute a histogram of the inputs using a fixed number of fixed width bins. Values outside of the [min, max) range are ignored. The output is a Nx2 array of bucket lower edges and counts (or cumulative counts) and is suitable for use per-pixel.

===

## ee.Reducer.forEach
Creates a Reducer by combining a copy of the given reducer for each output name in the given list. If the reducer has a single output, the output names are used as-is; otherwise they are prefixed to the original output names.

===

## ee.Reducer.forEachBand
Creates a Reducer by combining a copy of the given reducer for each band in the given image using the band names as output names.

===

## ee.Reducer.forEachElement
Separately reduces each position in array inputs of equal shape, producing an array output of the same shape. For example, with the 'sum' reducer applied to 5 arrays with shape 2x2, the output will be a 2x2 array, where each position is the sum of the 5 values at that position.

===

## ee.Reducer.frequencyHistogram
Returns a Reducer that returns a (weighted) frequency table of its inputs. Where applicable, the output name is "histogram". No arguments.

===

## ee.Reducer.geometricMedian
Creates a reducer that computes the geometric median across a set of inputs.

===

## ee.Reducer.getOutputs
Returns a list of the output names of the given reducer.

===

## ee.Reducer.group
Groups reducer records by the value of a given input and reduces each group with the given reducer.

===

## ee.Reducer.histogram
Create a reducer that will compute a histogram of the inputs.

===

## ee.Reducer.intervalMean
Creates a Reducer to compute the mean of all inputs in the specified percentile range. For small numbers of inputs (up to maxRaw) the mean will be computed directly; for larger numbers of inputs the mean will be derived from a histogram.

===

## ee.Reducer.kendallsCorrelation
Creates a reducer that computes the Kendall's Tau-b rank correlation. A positive tau value indicates an increasing trend; negative value indicates a decreasing trend. See https://commons.apache.org/proper/commons-math/javadocs/api-3.6.1/org/apache/commons/math3/stat/correlation/KendallsCorrelation.html for details.

===

## ee.Reducer.kurtosis
Returns a Reducer that Computes the kurtosis of its inputs. Where applicable, the output name is "kurtosis". No arguments.

===

## ee.Reducer.last
Returns a Reducer that returns the last of its inputs. Where applicable, the output name is "last". No arguments.

===

## ee.Reducer.lastNonNull
Returns a Reducer that returns the last of its non-null inputs. Where applicable, the output name is "last". No arguments.

===

## ee.Reducer.linearFit
Returns a Reducer that computes the slope and offset for a (weighted) linear regression of 2 inputs. The inputs are expected to be x data followed by y data. Where applicable, the outputs are named: "scale", "offset". No arguments.

===

## ee.Reducer.linearRegression
Creates a reducer that computes a linear least squares regression with numX independent variables and numY dependent variables.

Each input tuple will have values for the independent variables followed by the dependent variables.

The first output is a coefficients array with dimensions (numX, numY); each column contains the coefficients for the corresponding dependent variable. The second output is a vector of the root mean square of the residuals of each dependent variable. Both outputs are null if the system is underdetermined, e.g., the number of inputs is less than or equal to numX.

===

## ee.Reducer.max
Creates a reducer that outputs the maximum value of its (first) input. If numInputs is greater than one, also outputs the corresponding values of the additional inputs.

===

## ee.Reducer.mean
Returns a Reducer that computes the (weighted) arithmetic mean of its inputs. Where applicable, the output name is "mean". No arguments.

===

## ee.Reducer.median
Create a reducer that will compute the median of the inputs. For small numbers of inputs (up to maxRaw) the median will be computed directly; for larger numbers of inputs the median will be derived from a histogram.

===

## ee.Reducer.min
Creates a reducer that outputs the minimum value of its (first) input. If numInputs is greater than one, also outputs the corresponding values of the additional inputs.

===

## ee.Reducer.minMax
Returns a Reducer that computes the minimum and maximum of its inputs. Where applicable, the outputs are named: "min", "max". No arguments.

===

## ee.Reducer.mode
Create a reducer that will compute the mode of the inputs. For small numbers of inputs (up to maxRaw) the mode will be computed directly; for larger numbers of inputs the mode will be derived from a histogram.

===

## ee.Reducer.pearsonsCorrelation
Creates a two-input reducer that computes Pearson's product-moment correlation coefficient and the 2-sided p-value test for correlation = 0. No arguments.

===

## ee.Reducer.percentile
Create a reducer that will compute the specified percentiles, e.g. given [0, 50, 100] will produce outputs named 'p0', 'p50', and 'p100' with the min, median, and max respectively. For small numbers of inputs (up to maxRaw) the percentiles will be computed directly; for larger numbers of inputs the percentiles will be derived from a histogram.

===

## ee.Reducer.product
Returns a Reducer that computes the product of its inputs. Where applicable, the output name is "product". No arguments.

===

## ee.Reducer.repeat
Creates a Reducer by combining the specified number of copies of the given reducer. Output names are the same as the given reducer, but each is a list of the corresponding output from each of the reducers.

===

## ee.Reducer.ridgeRegression
Creates a reducer that computes a ridge regression with numX independent variables (not including constant) followed by numY dependent variables. Ridge regression is a form of Tikhonov regularization which shrinks the regression coefficients by imposing a penalty on their size. With this implementation of ridge regression there NO NEED to include a constant value for bias. The first output is a coefficients array with dimensions (numX + 1, numY); each column contains the coefficients for the corresponding dependent variable plus the intercept for the dependent variable in the last column. Additional outputs are a vector of the root mean square of the residuals of each dependent variable and a vector of p-values for each dependent variable. Outputs are null if the system is underdetermined, e.g., the number of inputs is less than numX + 1.

===

## ee.Reducer.robustLinearRegression
Creates a reducer that computes a robust least squares regression with numX independent variables and numY dependent variables, using iteratively reweighted least squares with the Talwar cost function. A point is considered an outlier if the RMS of residuals is greater than beta. Each input tuple will have values for the independent variables followed by the dependent variables. The first output is a coefficients array with dimensions (numX, numY); each column contains the coefficients for the corresponding dependent variable. The second is a vector of the root mean square of the residuals of each dependent variable. Both outputs are null if the system is underdetermined, e.g., the number of inputs is less than numX.

===

## ee.Reducer.sampleStdDev
Returns a Reducer that computes the sample standard deviation of its inputs. Where applicable, the output name is "stdDev". No arguments.

===

## ee.Reducer.sampleVariance
Returns a Reducer that computes the sample variance of its inputs. Where applicable, the output name is "variance". No arguments.

===

## ee.Reducer.sensSlope
Creates a two-input reducer that computes the Sen's slope estimator. The inputs are expected to be x data followed by y data. It returns two double values; the estimated slope and the offset. No arguments.

===

## ee.Reducer.setOutputs
Returns a Reducer with the same inputs as the given Reducer, but with outputs renamed and/or removed.

===

## ee.Reducer.skew
Returns a Reducer that Computes the skewness of its inputs. Where applicable, the output name is "skew". No arguments.

===

## ee.Reducer.spearmansCorrelation
Creates a two-input reducer that computes the Spearman's rank-moment correlation. See https://commons.apache.org/proper/commons-math/javadocs/api-3.6/org/apache/commons/math3/stat/correlation/SpearmansCorrelation.html for details. No arguments.

===

## ee.Reducer.splitWeights
Returns a Reducer with the same outputs as the given Reducer, but with each weighted input replaced by two unweighted inputs.

===

## ee.Reducer.stdDev
Returns a Reducer that computes the standard deviation of its inputs. Where applicable, the output name is "stdDev". No arguments.

===

## ee.Reducer.sum
Returns a Reducer that computes the (weighted) sum of its inputs. Where applicable, the output name is "sum". No arguments.

===

## ee.Reducer.toCollection
Returns a reducer that collects its inputs into a FeatureCollection.

===

## ee.Reducer.toList
Creates a reducer that collects its inputs into a list, optionally grouped into tuples.

===

## ee.Reducer.unweighted
Returns a Reducer with the same inputs and outputs as the given Reducer, but with no weighted inputs.

===

## ee.Reducer.variance
Returns a Reducer that computes the variance of its inputs. Where applicable, the output name is "variance". No arguments.

===

## ee.String
Constructs a new String.

===

## ee.String.aside
Calls a function passing this object as the first argument, and returning itself. Convenient e.g. when debugging: var c = ee.ImageCollection('foo').aside(print) .filterDate('2001-01-01', '2002-01-01').aside(print, 'In 2001') .filterBounds(geom).aside(print, 'In region') .aside(Map.addLayer, {min: 0, max: 142}, 'Filtered') .select('a', 'b'); Returns the same object, for chaining.

===

## ee.String.cat
Concatenates two strings.

===

## ee.String.compareTo
Compares two strings lexicographically. Returns: the value 0 if the two strings are lexicographically equal; -1 if string1 is less than string2; and 1 if string1 is lexicographically greater than string2.

===

## ee.String.decodeJSON
Decodes a JSON string.

===

## ee.String.encodeJSON
Encodes an object to JSON. Supports primitives, lists, and dictionaries.

===

## ee.String.equals
Checks for string equality with a given object. Returns true if the target is a string and is lexicographically equal to the reference, or false otherwise.

===

## ee.String.evaluate
Asynchronously retrieves the value of this object from the server and passes it to the provided callback function.

===

## ee.String.getInfo
Retrieves the value of this object from the server. If no callback function is provided, the request is made synchronously. If a callback is provided, the request is made asynchronously. The asynchronous mode is preferred because the synchronous mode stops all other code (for example, the EE Code Editor UI) while waiting for the server. To make an asynchronous request, evaluate() is preferred over getInfo(). Returns the computed value of this object.

===

## ee.String.index
Searches a string for the first occurrence of a substring.  Returns the index of the first match, or -1.

===

## ee.String.length
Returns the length of a string.

===

## ee.String.match
Matches a string against a regular expression. Returns a list of matching strings.

===

## ee.String.replace
Returns a new string with some or all matches of a pattern replaced.

===

## ee.String.rindex
Searches a string for the last occurrence of a substring. Returns the index of the first match, or -1.

===

## ee.String.serialize
Returns the serialized representation of this object.

===

## ee.String.slice
Returns a substring of the given string. If the specified range exceeds the length of the string, returns a shorter substring.

===

## ee.String.split
Splits a string on a regular expression, Returning a list of strings.

===

## ee.String.toLowerCase
Converts all of the characters in a string to lower case.

===

## ee.String.toUpperCase
Converts all of the characters in a string to upper case.

===

## ee.String.trim
Returns a string whose value is the original string, with any leading and trailing whitespace removed.

===

## ee.Terrain.aspect
Calculates aspect in degrees from a terrain DEM. The local gradient is computed using the 4-connected neighbors of each pixel, so missing values will occur around the edges of an image.

===

## ee.Terrain.fillMinima
Fills local minima. Only works on INT types.

===

## ee.Terrain.hillShadow
Creates a shadow band, with output 1 where pixels are illumunated and 0 where they are shadowed. Takes as input an elevation band, azimuth and zenith of the light source in degrees, a neighborhood size, and whether or not to apply hysteresis when a shadow appears. Currently, this algorithm only works for Mercator projections, in which light rays are parallel.

===

## ee.Terrain.hillshade
Computes a simple hillshade from a DEM.

===

## ee.Terrain.products
Calculates slope, aspect, and a simple hillshade from a terrain DEM. Expects an image containing either a single band of elevation, measured in meters, or if there's more than one band, one named 'elevation'. Adds output bands named 'slope' and 'aspect' measured in degrees plus an unsigned byte output band named 'hillshade' for visualization. All other bands and metadata are copied from the input image. The local gradient is computed using the 4-connected neighbors of each pixel, so missing values will occur around the edges of an image.

===

## ee.Terrain.slope
Calculates slope in degrees from a terrain DEM. The local gradient is computed using the 4-connected neighbors of each pixel, so missing values will occur around the edges of an image.

===

## ee.apply
Call a function with a dictionary of named arguments. Returns an object representing the called function. If the signature specifies a recognized return type, the returned value will be cast to that type.

===

## ee.call
Call a function with the given positional arguments. Returns an object representing the called function. If the signature specifies a recognized return type, the returned value will be cast to that type.

===

## ee.data.authenticateViaOauth
Configures client-side authentication of EE API calls through the Google APIs Client Library for JavaScript. The library will be loaded automatically if it is not already loaded on the page. The user will be asked to grant the application identified by clientId access to their EE data if they have not done so previously. This or another authentication method should be called before ee.initialize(). Note that if the user has not previously granted access to the application identified by the client ID, by default this will try to pop up a dialog window prompting the user to grant the required permission. However, this popup can be blocked by the browser. To avoid this, specify the opt_onImmediateFailed callback, and in it render an in-page login button, then call ee.data.authenticateViaPopup() from the click event handler of this button. This stops the browser from blocking the popup, as it is now the direct result of a user action. The auth token will be refreshed automatically when possible. You can safely assume that all async calls will be sent with the appropriate credentials. For synchronous calls, however, you should check for an auth token with ee.data.getAuthToken() and call ee.data.refreshAuthToken() manually if there is none. The token refresh operation is asynchronous and cannot be performed behind-the-scenes on-demand prior to synchronous calls.

===

## ee.data.authenticateViaPopup
Shows a popup asking for the user's permission. Should only be called if ee.data.authenticate() called its opt_onImmediateFailed argument in the past. May be blocked by pop-up blockers if called outside a user-initiated handler.

===

## ee.data.authenticateViaPrivateKey
Configures server-side authentication of EE API calls through the Google APIs Node.js Client. Private key authentication is strictly for server-side API calls: for browser-based applications, use ee.data.authenticateViaOauth(). No user interaction (e.g. authentication popup) is necessary when using server-side authentication. This or another authentication method should be called before ee.initialize(). The auth token will be refreshed automatically when possible. You can safely assume that all async calls will be sent with the appropriate credentials. For synchronous calls, however, you should check for an auth token with ee.data.getAuthToken() and call ee.data.refreshAuthToken() manually if there is none. The token refresh operation is asynchronous and cannot be performed behind-the-scenes, on demand, prior to synchronous calls.

===

## ee.data.cancelOperation
Cancels the given operation(s).

===

## ee.data.computeValue
Sends a request to compute a value. Returns result

===

## ee.data.copyAsset
Copies the asset from sourceId into destinationId.

===

## ee.data.createAsset
Creates an asset from a JSON value. To create an empty image collection or folder, pass in a "value" object with a "type" key whose value is one of ee.data.AssetType.* (i.e. "ImageCollection" or "Folder"). Returns a description of the saved asset, including a generated ID, or null if a callback is specified.

===

## ee.data.createAssetHome
Attempts to create a home root folder (e.g. "users/joe") for the current user. This results in an error if the user already has a home root folder or the requested ID is unavailable.

===

## ee.data.createFolder
Creates an asset folder. Returns a description of the newly created folder.

===

## ee.data.deleteAsset
Deletes the asset with the given id.

===

## ee.data.getAsset
Load info for an asset, given an asset id. Returns the value call results, or null if a callback is specified.

===

## ee.data.getAssetAcl
Returns the access control list of the asset with the given ID. The authenticated user must be a writer or owner of an asset to see its ACL.

===

## ee.data.getAssetRootQuota
Returns quota usage details for the asset root with the given ID. Usage notes: - The id must be a root folder like "users/foo" (not "users/foo/bar"). - The authenticated user must own the asset root to see its quota usage.

===

## ee.data.getDownloadId
Get a Download ID. Returns a download id and token, or null if a callback is specified.

===

## ee.data.getFeatureViewTilesKey
Get a tiles key for a given map or asset. The tiles key can be passed to an instance of FeatureViewTileSource which can be rendered on a base map outside of the Code Editor. Returns the call results. Null if a callback is specified.

===

## ee.data.getFilmstripThumbId
Get a Filmstrip Thumbnail Id for a given asset. Returns the thumb ID and optional token, or null if a callback is specified.

===

## ee.data.getMapId
Get a Map ID for a given asset Returns the mapId call results, which may be passed to ee.data.getTileUrl or ui.Map.addLayer. Null if a callback is specified.

===

## ee.data.getOperation
Gets information on an operation or list of operations. See more details on Operations here: https://cloud.google.com/apis/design/design_patterns#long_running_operations Returns operation status, or a map from operation names to status. Each Operation contains: - name: operation name in the format projects/X/operations/Y - done: true when operation has finished running. - error: may be set when done=true. Contains message and other fields from https://cloud.google.com/tasks/docs/reference/rpc/google.rpc#status - metadata, which contains + state: PENDING, RUNNING, CANCELLING, SUCCEEDED, CANCELLED, or FAILED + description: Supplied task description + type: EXPORT_IMAGE, EXPORT_FEATURES, etc. + create_time: Time the operation was first submitted. + update_time: Timestamp of most recent update. + start_time: Time the operation started, when so. + end_time: Time the operation finished running, when so. + attempt: Number of retries of this task, starting at 1. + destination_uris: Resources output by this operation. + batch_eecu_usage_seconds: CPU used by this operation.

===

## ee.data.getTableDownloadId
Get a download ID. Returns a download id and token, or null if a callback is specified.

===

## ee.data.getThumbId
Get a Thumbnail Id for a given asset. Returns the thumb ID and optional token, or null if a callback is specified.

===

## ee.data.getTileUrl
Generate a URL for map tiles from a Map ID and coordinates. If formatTileUrl is not present, we generate it by using or guessing the urlFormat string, and add urlFormat and formatTileUrl to id for future use. Returns the tile URL.

===

## ee.data.getVideoThumbId
Get a Video Thumbnail Id for a given asset. Returns the thumb ID and optional token, or null if a callback is specified.

===

## ee.data.getWorkloadTag
Returns the currently set workload tag. No arguments.

===

## ee.data.listAssets
Returns a list of the contents in an asset collection or folder, in an object that includes an `assets` array and an optional `nextPageToken`.

===

## ee.data.listBuckets
Returns top-level assets and folders for the Cloud Project or user. Leave the project field blank to use the current project.

===

## ee.data.listFeatures
List features for a given table asset. Returns the call results. Null if a callback is specified.

===

## ee.data.listImages
Returns a list of the contents in an image collection, in an object that includes an images array and an optional nextPageToken .

===

## ee.data.listOperations
Returns see getOperation for details on the Operation object.

===

## ee.data.makeDownloadUrl
Create a download URL from a docid and token. Returns the download URL.

===

## ee.data.makeTableDownloadUrl
Create a table download URL from a docid and token. Returns the download URL.

===

## ee.data.makeThumbUrl
Create a thumbnail URL from a thumbid and token. Returns the thumbnail URL.

===

## ee.data.newTaskId
Generates an "unsubmitted" ID for a long-running task. Before tasks are submitted, they may be assigned IDs to track them. The server ensures that the same ID cannot be reused. These IDs have a UUID format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx. Tasks that are running on the server have a ID without hyphens. This is returned by ee.data.startProcessing and other batch methods. Returns an array containing generated ID strings, or null if a callback is specified.

===

## ee.data.renameAsset
Renames the asset from sourceId to destinationId.

===

## ee.data.resetWorkloadTag
Resets the tag back to the default. If resetDefault parameter is set to true, the default will be set to empty before resetting.

===

## ee.data.setAssetAcl
Sets the access control list of the asset with the given ID. The owner ACL cannot be changed, and the final ACL of the asset is constructed by merging the OWNER entries of the old ACL with the incoming ACL record. The authenticated user must be a writer or owner of an asset to set its ACL.

===

## ee.data.setDefaultWorkloadTag
Sets the workload tag, and as the default for which to reset back to. For example, calling ee.data.resetWorkloadTag() will reset the workload tag back to the default chosen here. To reset the default back to none, pass in an empty string or pass in true to ee.data.resetWorkloadTag(true) , like so. Workload tag must be 1 - 63 characters, beginning and ending with an alphanumeric character ([a-z0-9A-Z]) with dashes (-), underscores (_), dots (.), and alphanumerics between, or an empty string to reset the default back to none.

===

## ee.data.setWorkloadTag
Sets the workload tag, used to label computation and exports. Workload tag must be 1 - 63 characters, beginning and ending with an alphanumeric character ([a-z0-9A-Z]) with dashes (-), underscores (_), dots (.), and alphanumerics between, or an empty string to clear the workload tag.

===

## ee.data.startIngestion
Creates an image asset ingestion task. See ee.data.startProcessing for details on task IDs and response format.

===

## ee.data.startProcessing
Create processing task that exports or pre-renders an image. Return value is null if a callback is specified. Returns an object with fields: - taskId: Submitted task ID (without hyphens). - name: Full operation name in the format projects/X/operations/Y - started: will be 'OK' - note: may have value 'ALREADY_EXISTS' if an identical task with the same unsubmitted ID already exists.

===

## ee.data.startTableIngestion
Creates a table asset ingestion task. See ee.data.startProcessing for details on task IDs and response format.

===

## ee.data.updateAsset
Updates an asset. The authenticated user must be a writer or owner of the asset.

===

## ee.data.updateTask
Update one or more tasks' properties. For now, only the following properties may be updated: State (to CANCELLED) Returns an array of updated tasks, or null if a callback is specified.

===

## ee.initialize
Initialize the library.  If this hasn't been called by the time any object constructor is used, it will be called then.  If this is called a second time with a different baseurl or tileurl, this doesn't do an un-initialization of e.g.: the previously loaded Algorithms, but will overwrite them and let point at alternate servers. If initialize() is first called in asynchronous mode (by passing a success callback), any future asynchronous mode calls will add their callbacks to a queue and all the callbacks will be run together. If a synchronous mode call is made after any number of asynchronous calls, it will block and execute all the previously supplied callbacks before returning. In most cases, an authorization token should be set before the library is initialized, either with ee.data.authorize() or ee.data.setAuthToken(). In Python, this method is named ee.Initialize, with a capital I.  Note that some parameters differ between JavaScript and Python.  In addition to opt_url and project below, Python also supports: credentials - a google.oauth2.Credentials object or 'persistent' to use stored credentials (the default); http_transport - a httplib2.Http client.

===

## ee.reset
Reset the library to its base state. Useful for re-initializing to a different server. No arguments.

===

## ee :: class ee.apifunction. ApiFunction ( name , opt_signature=None ) Â¶
Bases: ee.function.Function An object representing an EE API Function. classmethod allSignatures ( ) Â¶ Returns a map from the name to signature for all API functions. classmethod apply_ ( name , named_args ) Â¶ Call a named API function with a dictionary of named arguments. Args: name: The name of the API function to call.
named_args: A dictionary of arguments to the function. Returns: An object representing the called function. If the signature specifies
a recognized return type, the returned value will be cast to that type. classmethod call_ ( name , *args , **kwargs ) Â¶ Call a named API function with positional and keyword arguments. Args: name: The name of the API function to call. * args: Positional arguments to pass to the function. ** kwargs: Keyword arguments to pass to the function. Returns: An object representing the called function. If the signature specifies
a recognized return type, the returned value will be cast to that type. static clearApi ( target ) Â¶ Removes all m
**Usage:** `class ee.apifunction. ApiFunction ( name , opt_signature=None ) Â¶`

===

## ee :: classmethod allSignatures ( ) Â¶
Returns a map from the name to signature for all API functions.
**Usage:** `classmethod allSignatures ( ) Â¶`

===

## ee :: classmethod apply_ ( name , named_args ) Â¶
Call a named API function with a dictionary of named arguments. Args: name: The name of the API function to call.
named_args: A dictionary of arguments to the function. Returns: An object representing the called function. If the signature specifies
a recognized return type, the returned value will be cast to that type.
**Usage:** `classmethod apply_ ( name , named_args ) Â¶`

===

## ee :: Args:
name: The name of the API function to call.
named_args: A dictionary of arguments to the function.
**Usage:** `Args:`

===

## ee :: classmethod call_ ( name , *args , **kwargs ) Â¶
Call a named API function with positional and keyword arguments. Args: name: The name of the API function to call. * args: Positional arguments to pass to the function. ** kwargs: Keyword arguments to pass to the function. Returns: An object representing the called function. If the signature specifies
a recognized return type, the returned value will be cast to that type.
**Usage:** `classmethod call_ ( name , *args , **kwargs ) Â¶`

===

## ee :: static clearApi ( target ) Â¶
Removes all methods added by importApi() from a target class. Args: target: The class to remove from.
**Usage:** `static clearApi ( target ) Â¶`

===

## ee :: encode_cloud_invocation ( unused_encoder ) Â¶
Encodes the function as a FunctionInvocation. Args: encoder: A function that can be called to encode the components of an object. Returns a reference to the encoded value. Returns: The encoded form of the function.
**Usage:** `encode_cloud_invocation ( unused_encoder ) Â¶`

===

## ee :: encoder: A function that can be called to encode the components of
an object. Returns a reference to the encoded value.
**Usage:** `encoder: A function that can be called to encode the components of`

===

## ee :: encode_invocation ( unused_encoder ) Â¶
Encodes the function in a format compatible with Serializer. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the function.
**Usage:** `encode_invocation ( unused_encoder ) Â¶`

===

## ee :: getSignature ( ) Â¶
Returns a description of the interface provided by this function.
**Usage:** `getSignature ( ) Â¶`

===

## ee :: classmethod importApi ( target , prefix , type_name , opt_prepend=None ) Â¶
Adds all API functions that begin with a given prefix to a target class. Args: target: The class to add to.
prefix: The prefix to search for in the signatures.
type_name: The name of the objectâs type. Functions whose first argument matches this type are bound as instance methods, and
those whose first argument doesnât match are bound as static methods. opt_prepend: An optional string to prepend to the names of the added functions.
**Usage:** `classmethod importApi ( target , prefix , type_name , opt_prepend=None ) Â¶`

===

## ee :: opt_prepend: An optional string to prepend to the names of the
added functions.
**Usage:** `opt_prepend: An optional string to prepend to the names of the`

===

## ee :: classmethod initialize ( ) Â¶
Initializes the list of signatures from the Earth Engine front-end.
**Usage:** `classmethod initialize ( ) Â¶`

===

## ee :: classmethod lookup ( name ) Â¶
Looks up an API function by name. Args: name: The name of the function to get. Returns: The requested ApiFunction.
**Usage:** `classmethod lookup ( name ) Â¶`

===

## ee :: classmethod lookupInternal ( name ) Â¶
Looks up an API function by name. Args: name: The name of the function to get. Returns: The requested ApiFunction or None if not found.
**Usage:** `classmethod lookupInternal ( name ) Â¶`

===

## ee :: classmethod reset ( ) Â¶
Clears the API functions list so it will be reloaded from the server.
**Usage:** `classmethod reset ( ) Â¶`

===

## ee :: classmethod unboundFunctions ( ) Â¶
Returns the functions that have not been bound using importApi() yet.
**Usage:** `classmethod unboundFunctions ( ) Â¶`

===

## ee :: class ee.apitestcase. ApiTestCase ( methodName='runTest' ) Â¶
Bases: unittest.case.TestCase InitializeApi ( ) Â¶ Initializes the library with standard API methods. This is normally invoked during setUp(), but subclasses may invoke
it manually instead if they prefer. MockSend ( path , params , unused_method=None , unused_raw=None ) Â¶ setUp ( ) Â¶ Hook method for setting up the test fixture before exercising it.
**Usage:** `class ee.apitestcase. ApiTestCase ( methodName='runTest' ) Â¶`

===

## ee :: InitializeApi ( ) Â¶
Initializes the library with standard API methods. This is normally invoked during setUp(), but subclasses may invoke
it manually instead if they prefer.
**Usage:** `InitializeApi ( ) Â¶`

===

## ee :: MockSend ( path , params , unused_method=None , unused_raw=None ) Â¶
**Usage:** `MockSend ( path , params , unused_method=None , unused_raw=None ) Â¶`

===

## ee :: setUp ( ) Â¶
Hook method for setting up the test fixture before exercising it.
**Usage:** `setUp ( ) Â¶`

===

## ee :: ee.apitestcase. UsingCloudApi ( cloud_api_resource=None , mock_http=None ) Â¶
Returns a context manager under which the Cloud API is enabled.
**Usage:** `ee.apitestcase. UsingCloudApi ( cloud_api_resource=None , mock_http=None ) Â¶`

===

## ee :: ee.batch. ConvertFormatSpecificParams ( configDict ) Â¶
Mutates configDict into server params by extracting format options. For example: {âfileFormatâ: âGeoTIFFâ, âformatOptionsâ: {âcloudOptimizedâ: true}} becomes: {âfileFormatâ: âGeoTIFFâ, âtiffCloudOptimizedâ: true} Also performs checks to make sure any specified options are valid and/or
wonât collide with top level arguments when converted to server-friendly
parameters. Args: configDict: A task config dict Raises: EEException: We were unable to create format specific parameters for the
server.
**Usage:** `ee.batch. ConvertFormatSpecificParams ( configDict ) Â¶`

===

## ee :: For example:
{âfileFormatâ: âGeoTIFFâ, âformatOptionsâ: {âcloudOptimizedâ: true}}
**Usage:** `For example:`

===

## ee :: class ee.batch. Export Â¶
Bases: object A class with static methods to start export tasks. class image Â¶ Bases: object A static class with methods to start image export tasks. static toAsset ( image , description='myExportImageTask' , assetId=None , pyramidingPolicy=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , **kwargs ) Â¶ Creates a task to export an EE Image to an EE Asset. Args: image: The image to be exported.
description: Human-readable name of the task.
assetId: The destination asset ID.
pyramidingPolicy: The pyramiding policy to apply to each band in the image, a dictionary keyed by band name. Values must be
one of: âmeanâ, âsampleâ, âminâ, âmaxâ, or âmodeâ.
Defaults to âmeanâ. A special key, â.defaultâ, may be used to
change the default for all bands. dimensions: The dimensions of the exported image. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each posi
**Usage:** `class ee.batch. Export Â¶`

===

## ee :: class image Â¶
Bases: object A static class with methods to start image export tasks. static toAsset ( image , description='myExportImageTask' , assetId=None , pyramidingPolicy=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , **kwargs ) Â¶ Creates a task to export an EE Image to an EE Asset. Args: image: The image to be exported.
description: Human-readable name of the task.
assetId: The destination asset ID.
pyramidingPolicy: The pyramiding policy to apply to each band in the image, a dictionary keyed by band name. Values must be
one of: âmeanâ, âsampleâ, âminâ, âmaxâ, or âmodeâ.
Defaults to âmeanâ. A special key, â.defaultâ, may be used to
change the default for all bands. dimensions: The dimensions of the exported image. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat coordinates for a LinearRing or Polygon speci
**Usage:** `class image Â¶`

===

## ee :: static toAsset ( image , description='myExportImageTask' , assetId=None , pyramidingPolicy=None , dimensions=None , regi
Creates a task to export an EE Image to an EE Asset. Args: image: The image to be exported.
description: Human-readable name of the task.
assetId: The destination asset ID.
pyramidingPolicy: The pyramiding policy to apply to each band in the image, a dictionary keyed by band name. Values must be
one of: âmeanâ, âsampleâ, âminâ, âmaxâ, or âmodeâ.
Defaults to âmeanâ. A special key, â.defaultâ, may be used to
change the default for all bands. dimensions: The dimensions of the exported image. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat coordinates for a LinearRing or Polygon specifying the region to export. Can be specified as a nested
lists of numbers or a serialized string. Defaults to the imageâs
region. scale: The resolution in meters per pixel. Defaults to the native resolution of the image assset unless a crsTransform
is specified. crs: The co
**Usage:** `static toAsset ( image , description='myExportImageTask' , assetId=None , pyramidingPolicy=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , **kwargs ) Â¶`

===

## ee :: dimensions: The dimensions of the exported image. Takes either a
single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers.
**Usage:** `dimensions: The dimensions of the exported image. Takes either a`

===

## ee :: static toCloudStorage ( image , description='myExportImageTask' , bucket=None , fileNamePrefix=None , dimensions=None , 
Creates a task to export an EE Image to Google Cloud Storage. Args: image: The image to be exported.
description: Human-readable name of the task.
bucket: The name of a Cloud Storage bucket for the export.
fileNamePrefix: Cloud Storage object name prefix for the export. Defaults to the name of the task. dimensions: The dimensions of the exported image. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat coordinates for a LinearRing or Polygon specifying the region to export. Can be specified as a nested
lists of numbers or a serialized string. Defaults to the imageâs
region. scale: The resolution in meters per pixel. Defaults to the native resolution of the image assset unless a crsTransform
is specified. crs: The coordinate reference system of the exported imageâs projection. Defaults to the imageâs default projection. crsTransform: A comma-separated string of 6 numbers descr
**Usage:** `static toCloudStorage ( image , description='myExportImageTask' , bucket=None , fileNamePrefix=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , shardSize=None , fileDimensions=None , skipEmptyTiles=None , fileFormat=None , formatOptions=None , **kwargs ) Â¶`

===

## ee :: static toDrive ( image , description='myExportImageTask' , folder=None , fileNamePrefix=None , dimensions=None , region=
Creates a task to export an EE Image to Drive. Args: image: The image to be exported.
description: Human-readable name of the task.
folder: The name of a unique folder in your Drive account to export into. Defaults to the root of the drive. fileNamePrefix: The Google Drive filename for the export. Defaults to the name of the task. dimensions: The dimensions of the exported image. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat coordinates for a LinearRing or Polygon specifying the region to export. Can be specified as a nested
lists of numbers or a serialized string. Defaults to the imageâs
region. scale: The resolution in meters per pixel. Defaults to the native resolution of the image assset unless a crsTransform
is specified. crs: The coordinate reference system of the exported imageâs projection. Defaults to the imageâs default projection. crsTransform: A comma-separat
**Usage:** `static toDrive ( image , description='myExportImageTask' , folder=None , fileNamePrefix=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , shardSize=None , fileDimensions=None , skipEmptyTiles=None , fileFormat=None , formatOptions=None , **kwargs ) Â¶`

===

## ee :: fileNamePrefix: The Google Drive filename for the export.
Defaults to the name of the task.
**Usage:** `fileNamePrefix: The Google Drive filename for the export.`

===

## ee :: class map Â¶
Bases: object A class with a static method to start map export tasks. static toCloudStorage ( image , description='myExportMapTask' , bucket=None , fileFormat=None , path=None , writePublicTiles=None , maxZoom=None , scale=None , minZoom=None , region=None , skipEmptyTiles=None , mapsApiKey=None , **kwargs ) Â¶ Creates a task to export an Image as a pyramid of map tiles. Exports a rectangular pyramid of map tiles for use with web map
viewers. The map tiles will be accompanied by a reference
index.html file that displays them using the Google Maps API,
and an earth.html file for opening the map on Google Earth. Args: image: The image to export as tiles.
description: Human-readable name of the task.
bucket: The destination bucket to write to.
fileFormat: The map tilesâ file format, one of âautoâ, âpngâ, or âjpegâ. Defaults to âautoâ, which means that opaque tiles
will be encoded as âjpgâ and tiles with transparency will be
encoded as âpngâ. path: The string used
**Usage:** `class map Â¶`

===

## ee :: static toCloudStorage ( image , description='myExportMapTask' , bucket=None , fileFormat=None , path=None , writePublicT
Creates a task to export an Image as a pyramid of map tiles. Exports a rectangular pyramid of map tiles for use with web map
viewers. The map tiles will be accompanied by a reference
index.html file that displays them using the Google Maps API,
and an earth.html file for opening the map on Google Earth. Args: image: The image to export as tiles.
description: Human-readable name of the task.
bucket: The destination bucket to write to.
fileFormat: The map tilesâ file format, one of âautoâ, âpngâ, or âjpegâ. Defaults to âautoâ, which means that opaque tiles
will be encoded as âjpgâ and tiles with transparency will be
encoded as âpngâ. path: The string used as the outputâs path. A trailing â/â is optional. Defaults to the taskâs description. writePublicTiles: Whether to write public tiles instead of using the bucketâs default object ACL. Defaults to True and requires the
invoker to be an OWNER of bucket. maxZoom: The maximum zoom level of the map tiles t
**Usage:** `static toCloudStorage ( image , description='myExportMapTask' , bucket=None , fileFormat=None , path=None , writePublicTiles=None , maxZoom=None , scale=None , minZoom=None , region=None , skipEmptyTiles=None , mapsApiKey=None , **kwargs ) Â¶`

===

## ee :: path: The string used as the outputâs path. A trailing â/â
is optional. Defaults to the taskâs description.
**Usage:** `path: The string used as the outputâs path. A trailing â/â`

===

## ee :: skipEmptyTiles: If true, skip writing empty (i.e. fully-transparent)
map tiles. Defaults to false.
**Usage:** `skipEmptyTiles: If true, skip writing empty (i.e. fully-transparent)`

===

## ee :: class table Â¶
Bases: object A class with static methods to start table export tasks. static toAsset ( collection , description='myExportTableTask' , assetId=None , **kwargs ) Â¶ Creates a task to export a FeatureCollection to an EE table asset. Args: collection: The feature collection to be exported.
description: Human-readable name of the task.
assetId: The destination asset ID. ** kwargs: Holds other keyword arguments that may have been deprecated. Returns: An unstarted Task that exports the table. static toCloudStorage ( collection , description='myExportTableTask' , bucket=None , fileNamePrefix=None , fileFormat=None , selectors=None , **kwargs ) Â¶ Creates a task to export a FeatureCollection to Google Cloud Storage. Args: collection: The feature collection to be exported.
description: Human-readable name of the task.
bucket: The name of a Cloud Storage bucket for the export.
fileNamePrefix: Cloud Storage object name prefix for the export. Defaults to the name of the task. fileFormat: The outpu
**Usage:** `class table Â¶`

===

## ee :: static toAsset ( collection , description='myExportTableTask' , assetId=None , **kwargs ) Â¶
Creates a task to export a FeatureCollection to an EE table asset. Args: collection: The feature collection to be exported.
description: Human-readable name of the task.
assetId: The destination asset ID. ** kwargs: Holds other keyword arguments that may have been deprecated. Returns: An unstarted Task that exports the table.
**Usage:** `static toAsset ( collection , description='myExportTableTask' , assetId=None , **kwargs ) Â¶`

===

## ee :: static toCloudStorage ( collection , description='myExportTableTask' , bucket=None , fileNamePrefix=None , fileFormat=No
Creates a task to export a FeatureCollection to Google Cloud Storage. Args: collection: The feature collection to be exported.
description: Human-readable name of the task.
bucket: The name of a Cloud Storage bucket for the export.
fileNamePrefix: Cloud Storage object name prefix for the export. Defaults to the name of the task. fileFormat: The output format: âCSVâ (default), âGeoJSONâ, âKMLâ, âKMZâ, âSHPâ, or âTFRecordâ. selectors: The list of properties to include in the output, as a list of strings or a comma-separated string. By default, all properties
are included. ** kwargs: Holds other keyword arguments that may have been deprecated such as âoutputBucketâ. Returns: An unstarted Task that exports the table.
**Usage:** `static toCloudStorage ( collection , description='myExportTableTask' , bucket=None , fileNamePrefix=None , fileFormat=None , selectors=None , **kwargs ) Â¶`

===

## ee :: fileFormat: The output format: âCSVâ (default), âGeoJSONâ, âKMLâ, âKMZâ,
âSHPâ, or âTFRecordâ.
**Usage:** `fileFormat: The output format: âCSVâ (default), âGeoJSONâ, âKMLâ, âKMZâ,`

===

## ee :: static toDrive ( collection , description='myExportTableTask' , folder=None , fileNamePrefix=None , fileFormat=None , se
Creates a task to export a FeatureCollection to Google Cloud Storage. Args: collection: The feature collection to be exported.
description: Human-readable name of the task.
folder: The name of a unique folder in your Drive account to export into. Defaults to the root of the drive. fileNamePrefix: The Google Drive filename for the export. Defaults to the name of the task. fileFormat: The output format: âCSVâ (default), âGeoJSONâ, âKMLâ, âKMZâ, âSHPâ, or âTFRecordâ. selectors: The list of properties to include in the output, as a list of strings or a comma-separated string. By default, all properties
are included. ** kwargs: Holds other keyword arguments that may have been deprecated such as âdriveFolderâ and âdriveFileNamePrefixâ. Returns: An unstarted Task that exports the table.
**Usage:** `static toDrive ( collection , description='myExportTableTask' , folder=None , fileNamePrefix=None , fileFormat=None , selectors=None , **kwargs ) Â¶`

===

## ee :: class video Â¶
Bases: object A class with static methods to start video export task. static toCloudStorage ( collection , description='myExportVideoTask' , bucket=None , fileNamePrefix=None , framesPerSecond=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , maxFrames=None , **kwargs ) Â¶ Creates a task to export an ImageCollection video to Cloud Storage. Args: collection: The image collection to be exported. The collection must only contain RGB images. description: Human-readable name of the task.
bucket: The name of a Cloud Storage bucket for the export.
fileNamePrefix: Cloud Storage object name prefix for the export. Defaults to the taskâs description. framesPerSecond: A number between .1 and 120 describing the framerate of the exported video. dimensions: The dimensions of the exported video. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat 
**Usage:** `class video Â¶`

===

## ee :: static toCloudStorage ( collection , description='myExportVideoTask' , bucket=None , fileNamePrefix=None , framesPerSeco
Creates a task to export an ImageCollection video to Cloud Storage. Args: collection: The image collection to be exported. The collection must only contain RGB images. description: Human-readable name of the task.
bucket: The name of a Cloud Storage bucket for the export.
fileNamePrefix: Cloud Storage object name prefix for the export. Defaults to the taskâs description. framesPerSecond: A number between .1 and 120 describing the framerate of the exported video. dimensions: The dimensions of the exported video. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat coordinates for a LinearRing or Polygon specifying the region to export. Can be specified as a nested
lists of numbers or a serialized string. Defaults to the first
imageâs region. scale: The resolution in meters per pixel.
crs: The coordinate reference system of the exported videoâs projection. Defaults to SR-ORG:6627
**Usage:** `static toCloudStorage ( collection , description='myExportVideoTask' , bucket=None , fileNamePrefix=None , framesPerSecond=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , maxFrames=None , **kwargs ) Â¶`

===

## ee :: collection: The image collection to be exported. The collection must
only contain RGB images.
**Usage:** `collection: The image collection to be exported. The collection must`

===

## ee :: framesPerSecond: A number between .1 and 120 describing the
framerate of the exported video.
**Usage:** `framesPerSecond: A number between .1 and 120 describing the`

===

## ee :: crsTransform: A comma-separated string of 6 numbers describing
the affine transform of the coordinate reference system of the
exported videoâs projection, in the order: xScale, xShearing,
xTranslation, yShearing, yScale and yTranslation. Defaults to
the image collectionâs native CRS transform.
**Usage:** `crsTransform: A comma-separated string of 6 numbers describing`

===

## ee :: static toDrive ( collection , description='myExportVideoTask' , folder=None , fileNamePrefix=None , framesPerSecond=None
Creates a task to export an ImageCollection as a video to Drive. Args: collection: The image collection to be exported. The collection must only contain RGB images. description: Human-readable name of the task.
folder: The name of a unique folder in your Drive account to export into. Defaults to the root of the drive. fileNamePrefix: The Google Drive filename for the export. Defaults to the name of the task. framesPerSecond: A number between .1 and 120 describing the framerate of the exported video. dimensions: The dimensions of the exported video. Takes either a single positive integer as the maximum dimension or âWIDTHxHEIGHTâ
where WIDTH and HEIGHT are each positive integers. region: The lon,lat coordinates for a LinearRing or Polygon specifying the region to export. Can be specified as a nested
lists of numbers or a serialized string. Defaults to the first
imageâs region. scale: The resolution in meters per pixel.
crs: The coordinate reference system of the exported videoâs
**Usage:** `static toDrive ( collection , description='myExportVideoTask' , folder=None , fileNamePrefix=None , framesPerSecond=None , dimensions=None , region=None , scale=None , crs=None , crsTransform=None , maxPixels=None , maxFrames=None , **kwargs ) Â¶`

===

## ee :: class ee.batch. Task ( task_id , task_type , state , config=None , name=None ) Â¶
Bases: object A batch task that can be run on the EE batch processing system. class ExportDestination Â¶ Bases: object ASSET = 'ASSET' Â¶ DRIVE = 'DRIVE' Â¶ GCS = 'GOOGLE_CLOUD_STORAGE' Â¶ class State Â¶ Bases: object CANCELLED = 'CANCELLED' Â¶ CANCEL_REQUESTED = 'CANCEL_REQUESTED' Â¶ COMPLETED = 'COMPLETED' Â¶ FAILED = 'FAILED' Â¶ READY = 'READY' Â¶ RUNNING = 'RUNNING' Â¶ UNSUBMITTED = 'UNSUBMITTED' Â¶ class Type Â¶ Bases: object EXPORT_IMAGE = 'EXPORT_IMAGE' Â¶ EXPORT_MAP = 'EXPORT_TILES' Â¶ EXPORT_TABLE = 'EXPORT_FEATURES' Â¶ EXPORT_VIDEO = 'EXPORT_VIDEO' Â¶ active ( ) Â¶ Returns whether the task is still running. cancel ( ) Â¶ Cancels the task. static list ( ) Â¶ Returns the tasks submitted to EE by the current user. These include all currently running tasks as well as recently canceled or
failed tasks. Returns: A list of Tasks. start ( ) Â¶ Starts the task. No-op for started tasks. status ( ) Â¶ Fetches the current status of the task. Returns: A dictionary describing the current s
**Usage:** `class ee.batch. Task ( task_id , task_type , state , config=None , name=None ) Â¶`

===

## ee :: class ExportDestination Â¶
Bases: object ASSET = 'ASSET' Â¶ DRIVE = 'DRIVE' Â¶ GCS = 'GOOGLE_CLOUD_STORAGE' Â¶
**Usage:** `class ExportDestination Â¶`

===

## ee :: ASSET = 'ASSET' Â¶
**Usage:** `ASSET = 'ASSET' Â¶`

===

## ee :: DRIVE = 'DRIVE' Â¶
**Usage:** `DRIVE = 'DRIVE' Â¶`

===

## ee :: GCS = 'GOOGLE_CLOUD_STORAGE' Â¶
**Usage:** `GCS = 'GOOGLE_CLOUD_STORAGE' Â¶`

===

## ee :: class State Â¶
Bases: object CANCELLED = 'CANCELLED' Â¶ CANCEL_REQUESTED = 'CANCEL_REQUESTED' Â¶ COMPLETED = 'COMPLETED' Â¶ FAILED = 'FAILED' Â¶ READY = 'READY' Â¶ RUNNING = 'RUNNING' Â¶ UNSUBMITTED = 'UNSUBMITTED' Â¶
**Usage:** `class State Â¶`

===

## ee :: CANCELLED = 'CANCELLED' Â¶
**Usage:** `CANCELLED = 'CANCELLED' Â¶`

===

## ee :: CANCEL_REQUESTED = 'CANCEL_REQUESTED' Â¶
**Usage:** `CANCEL_REQUESTED = 'CANCEL_REQUESTED' Â¶`

===

## ee :: COMPLETED = 'COMPLETED' Â¶
**Usage:** `COMPLETED = 'COMPLETED' Â¶`

===

## ee :: FAILED = 'FAILED' Â¶
**Usage:** `FAILED = 'FAILED' Â¶`

===

## ee :: READY = 'READY' Â¶
**Usage:** `READY = 'READY' Â¶`

===

## ee :: RUNNING = 'RUNNING' Â¶
**Usage:** `RUNNING = 'RUNNING' Â¶`

===

## ee :: UNSUBMITTED = 'UNSUBMITTED' Â¶
**Usage:** `UNSUBMITTED = 'UNSUBMITTED' Â¶`

===

## ee :: class Type Â¶
Bases: object EXPORT_IMAGE = 'EXPORT_IMAGE' Â¶ EXPORT_MAP = 'EXPORT_TILES' Â¶ EXPORT_TABLE = 'EXPORT_FEATURES' Â¶ EXPORT_VIDEO = 'EXPORT_VIDEO' Â¶
**Usage:** `class Type Â¶`

===

## ee :: EXPORT_IMAGE = 'EXPORT_IMAGE' Â¶
**Usage:** `EXPORT_IMAGE = 'EXPORT_IMAGE' Â¶`

===

## ee :: EXPORT_MAP = 'EXPORT_TILES' Â¶
**Usage:** `EXPORT_MAP = 'EXPORT_TILES' Â¶`

===

## ee :: EXPORT_TABLE = 'EXPORT_FEATURES' Â¶
**Usage:** `EXPORT_TABLE = 'EXPORT_FEATURES' Â¶`

===

## ee :: EXPORT_VIDEO = 'EXPORT_VIDEO' Â¶
**Usage:** `EXPORT_VIDEO = 'EXPORT_VIDEO' Â¶`

===

## ee :: active ( ) Â¶
Returns whether the task is still running.
**Usage:** `active ( ) Â¶`

===

## ee :: cancel ( ) Â¶
Cancels the task.
**Usage:** `cancel ( ) Â¶`

===

## ee :: static list ( ) Â¶
Returns the tasks submitted to EE by the current user. These include all currently running tasks as well as recently canceled or
failed tasks. Returns: A list of Tasks.
**Usage:** `static list ( ) Â¶`

===

## ee :: Returns:
A list of Tasks.
**Usage:** `Returns:`

===

## ee :: start ( ) Â¶
Starts the task. No-op for started tasks.
**Usage:** `start ( ) Â¶`

===

## ee :: status ( ) Â¶
Fetches the current status of the task. Returns: A dictionary describing the current status of the task as it appears on
the EE server. Includes the following fields:
- state: One of the values in Task.State.
- creation_timestamp_ms: The Unix timestamp of when the task was created.
- update_timestamp_ms: The Unix timestamp of when the task last changed.
- output_url: URL of the output. Appears only if state is COMPLETED.
- error_message: Failure reason. Appears only if state is FAILED.
May also include other fields.
**Usage:** `status ( ) Â¶`

===

## ee :: class ee.collection. Collection ( func , args , opt_varName=None ) Â¶
Bases: ee.element.Element Base class for ImageCollection and FeatureCollection. static elementType ( ) Â¶ Returns the type of the collectionâs elements. filter ( new_filter ) Â¶ Apply a filter to this collection. Args: new_filter: Filter to add to this collection. Returns: The filtered collection object. filterBounds ( geometry ) Â¶ Shortcut to add a geometry filter to a collection. Items in the collection with a footprint that fails to intersect
the given geometry will be excluded when the collection is evaluated.
This is equivalent to self.filter(Filter().geometry(â¦)). Args: geometry: The boundary to filter to either as a GeoJSON geometry, or a FeatureCollection, from which a geometry will be extracted. Returns: The filter object. filterDate ( start , opt_end=None ) Â¶ Shortcut to filter a collection with a date range. Items in the collection with a time_start property that doesnât
fall between the start and end dates will be excluded.
This is equivalent to self.filter(Filter()
**Usage:** `class ee.collection. Collection ( func , args , opt_varName=None ) Â¶`

===

## ee :: static elementType ( ) Â¶
Returns the type of the collectionâs elements.
**Usage:** `static elementType ( ) Â¶`

===

## ee :: filter ( new_filter ) Â¶
Apply a filter to this collection. Args: new_filter: Filter to add to this collection. Returns: The filtered collection object.
**Usage:** `filter ( new_filter ) Â¶`

===

## ee :: filterBounds ( geometry ) Â¶
Shortcut to add a geometry filter to a collection. Items in the collection with a footprint that fails to intersect
the given geometry will be excluded when the collection is evaluated.
This is equivalent to self.filter(Filter().geometry(â¦)). Args: geometry: The boundary to filter to either as a GeoJSON geometry, or a FeatureCollection, from which a geometry will be extracted. Returns: The filter object.
**Usage:** `filterBounds ( geometry ) Â¶`

===

## ee :: geometry: The boundary to filter to either as a GeoJSON geometry,
or a FeatureCollection, from which a geometry will be extracted.
**Usage:** `geometry: The boundary to filter to either as a GeoJSON geometry,`

===

## ee :: filterDate ( start , opt_end=None ) Â¶
Shortcut to filter a collection with a date range. Items in the collection with a time_start property that doesnât
fall between the start and end dates will be excluded.
This is equivalent to self.filter(Filter().date(â¦)). Args: start: The start date as a Date object, a string representation of a date, or milliseconds since epoch. opt_end: The end date as a Date object, a string representation of a date, or milliseconds since epoch. Returns: The filter object.
**Usage:** `filterDate ( start , opt_end=None ) Â¶`

===

## ee :: start: The start date as a Date object, a string representation of
a date, or milliseconds since epoch.
**Usage:** `start: The start date as a Date object, a string representation of`

===

## ee :: filterMetadata ( name , operator , value ) Â¶
Shortcut to add a metadata filter to a collection. This is equivalent to self.filter(Filter().metadata(â¦)). Args: name: Name of a property to filter.
operator: Name of a comparison operator as defined by FilterCollection.  Possible values are: âequalsâ, âless_thanâ,
âgreater_thanâ, ânot_equalsâ, ânot_less_thanâ, ânot_greater_thanâ,
âstarts_withâ, âends_withâ, ânot_starts_withâ, ânot_ends_withâ,
âcontainsâ, ânot_containsâ. value: The value to compare against. Returns: The filtered collection.
**Usage:** `filterMetadata ( name , operator , value ) Â¶`

===

## ee :: getInfo ( ) Â¶
Returns all the known information about this collection. This function makes an REST call to to retrieve all the known information
about this collection. Returns: The return contents vary but will include at least: features: an array containing metadata about the items in the collection that passed all filters. properties: a dictionary containing the collectionâs metadata properties.
**Usage:** `getInfo ( ) Â¶`

===

## ee :: The return contents vary but will include at least:
features: an array containing metadata about the items in the collection that passed all filters. properties: a dictionary containing the collectionâs metadata properties.
**Usage:** `The return contents vary but will include at least:`

===

## ee :: features: an array containing metadata about the items in the
collection that passed all filters.
**Usage:** `features: an array containing metadata about the items in the`

===

## ee :: iterate ( algorithm , first=None ) Â¶
Iterates over a collection with an algorithm. Applies a user-supplied function to each element of a collection. The
user-supplied function is given two arguments: the current element, and
the value returned by the previous call to iterate() or the first argument,
for the first iteration. The result is the value returned by the final
call to the user-supplied function. Args: algorithm: The function to apply to each element. Must take two arguments - an element of the collection and the value from the
previous iteration. first: The initial state. Returns: The result of the Collection.iterate() call. Raises: ee_exception.EEException: if algorithm is not a function.
**Usage:** `iterate ( algorithm , first=None ) Â¶`

===

## ee :: algorithm: The function to apply to each element. Must take two
arguments - an element of the collection and the value from the
previous iteration.
**Usage:** `algorithm: The function to apply to each element. Must take two`

===

## ee :: limit ( maximum , opt_property=None , opt_ascending=None ) Â¶
Limit a collection to the specified number of elements. This limits a collection to the specified number of elements, optionally
sorting them by a specified property first. Args: maximum: The number to limit the collection to.
opt_property: The property to sort by, if sorting.
opt_ascending: Whether to sort in ascending or descending order. The default is true (ascending). Returns: The collection.
**Usage:** `limit ( maximum , opt_property=None , opt_ascending=None ) Â¶`

===

## ee :: map ( algorithm , opt_dropNulls=None ) Â¶
Maps an algorithm over a collection. Args: algorithm: The operation to map over the images or features of the collection, a Python function that receives an image or features and
returns one. The function is called only once and the result is
captured as a description, so it cannot perform imperative operations
or rely on external state. opt_dropNulls: If true, the mapped algorithm is allowed to return nulls, and the elements for which it returns nulls will be dropped. Returns: The mapped collection. Raises: ee_exception.EEException: if algorithm is not a function.
**Usage:** `map ( algorithm , opt_dropNulls=None ) Â¶`

===

## ee :: algorithm: The operation to map over the images or features of the
collection, a Python function that receives an image or features and
returns one. The function is called only once and the result is
captured as a description, so it cannot perform imperative operations
or rely on external state.
**Usage:** `algorithm: The operation to map over the images or features of the`

===

## ee :: static name ( ) Â¶
Returns the name of the object, used in __str__().
**Usage:** `static name ( ) Â¶`

===

## ee :: sort ( prop , opt_ascending=None ) Â¶
Sort a collection by the specified property. Args: prop: The property to sort by.
opt_ascending: Whether to sort in ascending or descending order.  The default is true (ascending). Returns: The collection.
**Usage:** `sort ( prop , opt_ascending=None ) Â¶`

===

## ee :: class ee.computedobject. ComputedObject ( func , args , opt_varName=None ) Â¶
Bases: ee.encodable.Encodable A representation of an Earth Engine computed object. This is a base class for most API objects. The class itself is not abstract as it is used to wrap the return values of
algorithms that produce unrecognized types with the minimal functionality
necessary to interact well with the rest of the API. ComputedObjects come in two flavors:
1. If func != null and args != null, the ComputedObject is encoded as an invocation of func with args. If func == null and args == null, the ComputedObject is a variable
reference. The variable name is stored in its varName member. Note that
in this case, varName may still be null; this allows the name to be
deterministically generated at a later time. This is used to generate
deterministic variable names for mapped functions, ensuring that nested
mapping calls do not use the same variable name. aside ( func , *var_args ) Â¶ Calls a function passing this object as the first argument. Returns the object itself for chaining. Con
**Usage:** `class ee.computedobject. ComputedObject ( func , args , opt_varName=None ) Â¶`

===

## ee :: aside ( func , *var_args ) Â¶
Calls a function passing this object as the first argument. Returns the object itself for chaining. Convenient e.g. when debugging: c = (ee.ImageCollection(âfooâ).aside(logging.info) .filterDate(â2001-01-01â, â2002-01-01â).aside(logging.info)
.filterBounds(geom).aside(logging.info)
.aside(addToMap, {âminâ: 0, âmaxâ: 142})
.select(âaâ, âbâ)) Args: func: The function to call. * var_args: Any extra arguments to pass to the function. Returns: The same object, for chaining.
**Usage:** `aside ( func , *var_args ) Â¶`

===

## ee :: c = (ee.ImageCollection(âfooâ).aside(logging.info)
.filterDate(â2001-01-01â, â2002-01-01â).aside(logging.info)
.filterBounds(geom).aside(logging.info)
.aside(addToMap, {âminâ: 0, âmaxâ: 142})
.select(âaâ, âbâ))
**Usage:** `c = (ee.ImageCollection(âfooâ).aside(logging.info)`

===

## ee :: encode ( encoder ) Â¶
Encodes the object in a format compatible with Serializer.
**Usage:** `encode ( encoder ) Â¶`

===

## ee :: encode_cloud_value ( encoder ) Â¶
Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object.
**Usage:** `encode_cloud_value ( encoder ) Â¶`

===

## ee :: static freeze ( obj ) Â¶
Freeze a list or dict so it can be hashed.
**Usage:** `static freeze ( obj ) Â¶`

===

## ee :: isVariable ( ) Â¶
Returns whether this computed object is a variable reference.
**Usage:** `isVariable ( ) Â¶`

===

## ee :: classmethod name ( ) Â¶
Returns the name of the object, used in __str__().
**Usage:** `classmethod name ( ) Â¶`

===

## ee :: serialize ( opt_pretty=False , for_cloud_api=False ) Â¶
Serialize this object into a JSON string. Args: opt_pretty: A flag indicating whether to pretty-print the JSON.
for_cloud_api: Whether the encoding should be done for the Cloud API or the legacy API. Returns: The serialized representation of this object.
**Usage:** `serialize ( opt_pretty=False , for_cloud_api=False ) Â¶`

===

## ee :: class ee.computedobject. ComputedObjectMetaclass Â¶
Bases: type A meta-class that makes type coercion idempotent. If an instance of a ComputedObject subclass is instantiated by passing
another instance of that class as the sole argument, this short-circuits
and returns that argument.
**Usage:** `class ee.computedobject. ComputedObjectMetaclass Â¶`

===

## ee :: class ee.customfunction. CustomFunction ( signature , body ) Â¶
Bases: ee.function.Function , ee.encodable.Encodable An object representing a custom EE Function. static create ( func , return_type , arg_types ) Â¶ Creates a CustomFunction. The result calls a given native function with the specified return type and
argument types and auto-generated argument names. Args: func: The native function to wrap.
return_type: The type of the return value, either as a string or a class reference. arg_types: The types of the arguments, either as strings or class references. Returns: The constructed CustomFunction. encode ( encoder ) Â¶ Encodes the object in a format compatible with Serializer. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object. encode_cloud_invocation ( encoder ) Â¶ Encodes the function as a FunctionInvocation. Args: encoder: A function that can be called to encode the components of an object. Returns a reference to the encoded value. Returns: The encoded form of the func
**Usage:** `class ee.customfunction. CustomFunction ( signature , body ) Â¶`

===

## ee :: static create ( func , return_type , arg_types ) Â¶
Creates a CustomFunction. The result calls a given native function with the specified return type and
argument types and auto-generated argument names. Args: func: The native function to wrap.
return_type: The type of the return value, either as a string or a class reference. arg_types: The types of the arguments, either as strings or class references. Returns: The constructed CustomFunction.
**Usage:** `static create ( func , return_type , arg_types ) Â¶`

===

## ee :: arg_types: The types of the arguments, either as strings or class
references.
**Usage:** `arg_types: The types of the arguments, either as strings or class`

===

## ee :: encode_cloud_invocation ( encoder ) Â¶
Encodes the function as a FunctionInvocation. Args: encoder: A function that can be called to encode the components of an object. Returns a reference to the encoded value. Returns: The encoded form of the function.
**Usage:** `encode_cloud_invocation ( encoder ) Â¶`

===

## ee :: encode_invocation ( encoder ) Â¶
Encodes the function in a format compatible with Serializer. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the function.
**Usage:** `encode_invocation ( encoder ) Â¶`

===

## ee :: static variable ( type_name , name ) Â¶
Returns a placeholder variable with a given name and EE type. Args: type_name: A class to mimic.
name: The name of the variable as it will appear in the arguments of the custom functions that use this variable. If null,
a name will be auto-generated in _resolveNamelessArgs(). Returns: A variable with the given name implementing the given type.
**Usage:** `static variable ( type_name , name ) Â¶`

===

## ee :: class ee.data. TileFetcher ( url_format , map_name=None ) Â¶
Bases: object A helper class to fetch image tiles. fetch_tile ( x , y , z ) Â¶ Fetches the map tile specified by (x, y, z). This method uses any credentials that were specified to ee.Initialize(). Args: x: The tile x coordinate.
y: The tile y coordinate.
z: The tile zoom level. Returns: The map tile image data bytes. Raises: EEException if the fetch fails. format_tile_url ( x , y , z ) Â¶ Generates the URL for a particular tile. Args: x: The tile x coordinate.
y: The tile y coordinate.
z: The tile zoom level. Returns: The tileâs URL. url_format Â¶ Gets the URL format for this tile fetcher. Returns: A format string with {x}, {y}, and {z} placeholders.
If you are using the Cloud API, and have not provided an API
key, then this URL will require authorization. Use the credentials
provided to ee.Initialize() to provide this authorization. Alternatively,
use âfetch_tileâ to fetch the tile data, which will handle the
authorization for you.
**Usage:** `class ee.data. TileFetcher ( url_format , map_name=None ) Â¶`

===

## ee :: fetch_tile ( x , y , z ) Â¶
Fetches the map tile specified by (x, y, z). This method uses any credentials that were specified to ee.Initialize(). Args: x: The tile x coordinate.
y: The tile y coordinate.
z: The tile zoom level. Returns: The map tile image data bytes. Raises: EEException if the fetch fails.
**Usage:** `fetch_tile ( x , y , z ) Â¶`

===

## ee :: format_tile_url ( x , y , z ) Â¶
Generates the URL for a particular tile. Args: x: The tile x coordinate.
y: The tile y coordinate.
z: The tile zoom level. Returns: The tileâs URL.
**Usage:** `format_tile_url ( x , y , z ) Â¶`

===

## ee :: url_format Â¶
Gets the URL format for this tile fetcher. Returns: A format string with {x}, {y}, and {z} placeholders.
If you are using the Cloud API, and have not provided an API
key, then this URL will require authorization. Use the credentials
provided to ee.Initialize() to provide this authorization. Alternatively,
use âfetch_tileâ to fetch the tile data, which will handle the
authorization for you.
**Usage:** `url_format Â¶`

===

## ee :: ee.data. authorizeHttp ( http ) Â¶
**Usage:** `ee.data. authorizeHttp ( http ) Â¶`

===

## ee :: ee.data. cancelOperation ( operation_name ) Â¶
**Usage:** `ee.data. cancelOperation ( operation_name ) Â¶`

===

## ee :: ee.data. cancelTask ( taskId ) Â¶
Cancels a batch task.
DEPRECATED: Use cancelOperation
**Usage:** `ee.data. cancelTask ( taskId ) Â¶`

===

## ee :: ee.data. computeValue ( obj ) Â¶
Sends a request to compute a value. Args: obj: A ComputedObject whose value is desired. Returns: The result of evaluating that object on the server.
**Usage:** `ee.data. computeValue ( obj ) Â¶`

===

## ee :: ee.data. convert_asset_id_to_asset_name ( asset_id ) Â¶
Converts an internal asset ID to a Cloud API asset name. If asset_id already matches the format âprojects/ /assets/* â, it is returned
as-is. Args: asset_id: The asset ID to convert. Returns: An asset name string in the format âprojects/ /assets/* â.
**Usage:** `ee.data. convert_asset_id_to_asset_name ( asset_id ) Â¶`

===

## ee :: ee.data. copyAsset ( sourceId , destinationId , allowOverwrite=False ) Â¶
Copies the asset from sourceId into destinationId. Args: sourceId: The ID of the asset to copy.
destinationId: The ID of the new asset created by copying.
allowOverwrite: If True, allows overwriting an existing asset.
**Usage:** `ee.data. copyAsset ( sourceId , destinationId , allowOverwrite=False ) Â¶`

===

## ee :: ee.data. createAsset ( value , opt_path=None , opt_force=False , opt_properties=None ) Â¶
Creates an asset from a JSON value. To create an empty image collection or folder, pass in a âvalueâ object
with a âtypeâ key whose value is âImageCollectionâ or âFolderâ.
If you are using the Cloud API, use âIMAGE_COLLECTIONâ or âFOLDERâ. Args: value: An object describing the asset to create or a JSON string with the already-serialized value for the new asset. opt_path: An optional desired ID, including full path.
opt_force: True if asset overwrite is allowed
opt_properties: The keys and values of the properties to set on the created asset. Returns: A description of the saved asset, including a generated ID.
**Usage:** `ee.data. createAsset ( value , opt_path=None , opt_force=False , opt_properties=None ) Â¶`

===

## ee :: value: An object describing the asset to create or a JSON string
with the already-serialized value for the new asset.
**Usage:** `value: An object describing the asset to create or a JSON string`

===

## ee :: ee.data. createAssetHome ( requestedId ) Â¶
Attempts to create a home root folder for the current user (âusers/joeâ). Results in an error if the user already has a home root folder or the
requested ID is unavailable. Args: requestedId: The requested ID of the home folder (e.g. âusers/joeâ).
**Usage:** `ee.data. createAssetHome ( requestedId ) Â¶`

===

## ee :: ee.data. create_assets ( asset_ids , asset_type , mk_parents ) Â¶
Creates the specified assets if they do not exist.
**Usage:** `ee.data. create_assets ( asset_ids , asset_type , mk_parents ) Â¶`

===

## ee :: ee.data. deleteAsset ( assetId ) Â¶
Deletes the asset with the given id. Args: assetId: The ID of the asset to delete.
**Usage:** `ee.data. deleteAsset ( assetId ) Â¶`

===

## ee :: ee.data. exportImage ( request_id , params ) Â¶
Starts an image export task running. Args: request_id (string): A unique ID for the task, from newTaskId. If you are using the cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried. params: The object that describes the export task. If you are using the cloud API, this should be an ExportImageRequest.
However, the âexpressionâ parameter can be the actual Image to be
exported, not its serialized form. Returns: A dict with information about the created task.
If you are using the cloud API, this will be an Operation.
**Usage:** `ee.data. exportImage ( request_id , params ) Â¶`

===

## ee :: request_id (string): A unique ID for the task, from newTaskId.
If you are using the cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried.
**Usage:** `request_id (string): A unique ID for the task, from newTaskId.`

===

## ee :: ee.data. exportMap ( request_id , params ) Â¶
Starts a map export task running. Args: request_id (string): A unique ID for the task, from newTaskId. If you are using the cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried. params: The object that describes the export task. If you are using the cloud API, this should be an ExportMapRequest.
However, the âexpressionâ parameter can be the actual Image to be
exported, not its serialized form. Returns: A dict with information about the created task.
If you are using the cloud API, this will be an Operation.
**Usage:** `ee.data. exportMap ( request_id , params ) Â¶`

===

## ee :: ee.data. exportTable ( request_id , params ) Â¶
Starts a table export task running. Args: request_id (string): A unique ID for the task, from newTaskId. If you are using the cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried. params: The object that describes the export task. If you are using the cloud API, this should be an ExportTableRequest.
However, the âexpressionâ parameter can be the actual FeatureCollection
to be exported, not its serialized form. Returns: A dict with information about the created task.
If you are using the cloud API, this will be an Operation.
**Usage:** `ee.data. exportTable ( request_id , params ) Â¶`

===

## ee :: ee.data. exportVideo ( request_id , params ) Â¶
Starts a video export task running. Args: request_id (string): A unique ID for the task, from newTaskId. If you are using the cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried. params: The object that describes the export task. If you are using the cloud API, this should be an ExportVideoRequest.
However, the âexpressionâ parameter can be the actual ImageCollection
to be exported, not its serialized form. Returns: A dict with information about the created task.
If you are using the cloud API, this will be an Operation.
**Usage:** `ee.data. exportVideo ( request_id , params ) Â¶`

===

## ee :: ee.data. getAlgorithms ( ) Â¶
Get the list of algorithms. Returns: The dictionary of algorithms.  Each algorithm is a dictionary containing
the following fields: âdescriptionâ - (string) A text description of the algorithm.
âreturnsâ - (string) The return type of the algorithm.
âargsâ - An array of arguments.  Each argument specifies the following: ânameâ - (string) The name of the argument.
âdescriptionâ - (string) A text description of the argument.
âtypeâ - (string) The type of the argument.
âoptionalâ - (boolean) Whether the argument is optional or not.
âdefaultâ - A representation of the default value if the argument is not specified.
**Usage:** `ee.data. getAlgorithms ( ) Â¶`

===

## ee :: ee.data. getAsset ( asset_id ) Â¶
Loads info for an asset, given an asset id. Args: asset_id: The asset to be retrieved. Returns: The assetâs information, as an EarthEngineAsset.
**Usage:** `ee.data. getAsset ( asset_id ) Â¶`

===

## ee :: ee.data. getAssetAcl ( assetId ) Â¶
Returns the access control list of the asset with the given ID. Args: assetId: The ID of the asset to check. Returns: A dict describing the assetâs ACL. Looks like: { âownersâ : [â user @ domain1 . com â],
âwritersâ: [â user2 @ domain1 . com â, â user3 @ domain1 . com â],
âreadersâ: [â some_group @ domain2 . com â],
âall_users_can_readâ : True } If you are using the cloud API, then the entities in the ACL will
be prefixed by a type tag, such as âuser:â or âgroup:â. DEPRECATED: Use getIamPolicy
**Usage:** `ee.data. getAssetAcl ( assetId ) Â¶`

===

## ee :: A dict describing the assetâs ACL. Looks like:
{ âownersâ : [â user @ domain1 . com â],
âwritersâ: [â user2 @ domain1 . com â, â user3 @ domain1 . com â],
âreadersâ: [â some_group @ domain2 . com â],
âall_users_can_readâ : True }
**Usage:** `A dict describing the assetâs ACL. Looks like:`

===

## ee :: {
âownersâ : [â user @ domain1 . com â],
âwritersâ: [â user2 @ domain1 . com â, â user3 @ domain1 . com â],
âreadersâ: [â some_group @ domain2 . com â],
âall_users_can_readâ : True
**Usage:** `{`

===

## ee :: ee.data. getAssetRootQuota ( rootId ) Â¶
Returns quota usage details for the asset root with the given ID. Usage notes: The id must be a root folder like âusers/fooâ (not âusers/foo/barâ). The authenticated user must own the asset root to see its quota usage. Args: rootId: The ID of the asset to check. Returns: A dict describing the assetâs quota usage. Looks like, with size in bytes: { asset_count: {usage: number, limit: number},
asset_size: {usage: number, limit: number}, }
**Usage:** `ee.data. getAssetRootQuota ( rootId ) Â¶`

===

## ee :: A dict describing the assetâs quota usage. Looks like, with size in bytes:
{ asset_count: {usage: number, limit: number},
asset_size: {usage: number, limit: number}, }
**Usage:** `A dict describing the assetâs quota usage. Looks like, with size in bytes:`

===

## ee :: ee.data. getAssetRoots ( ) Â¶
Returns the list of the root folders the user owns. Note: The âidâ values for roots are two levels deep, e.g. âusers/johndoeâ not âusers/johndoe/notarootâ. Returns: A list of folder descriptions formatted like: [ {âtypeâ: âFolderâ, âidâ: âusers/fooâ},
{âtypeâ: âFolderâ, âidâ: âprojects/barâ}, ]
**Usage:** `ee.data. getAssetRoots ( ) Â¶`

===

## ee :: Note: The âidâ values for roots are two levels deep, e.g. âusers/johndoeâ
not âusers/johndoe/notarootâ.
**Usage:** `Note: The âidâ values for roots are two levels deep, e.g. âusers/johndoeâ`

===

## ee :: A list of folder descriptions formatted like:
[ {âtypeâ: âFolderâ, âidâ: âusers/fooâ},
{âtypeâ: âFolderâ, âidâ: âprojects/barâ}, ]
**Usage:** `A list of folder descriptions formatted like:`

===

## ee :: [
{âtypeâ: âFolderâ, âidâ: âusers/fooâ},
{âtypeâ: âFolderâ, âidâ: âprojects/barâ},
**Usage:** `[`

===

## ee :: ee.data. getDownloadId ( params ) Â¶
Get a Download ID. Args: params: An object containing visualization options with the following possible values: name - a base name to use when constructing filenames.
bands - a description of the bands to download. Must be an array of dictionaries, each with the following keys: id - the name of the band, a string, required.
crs - an optional CRS string defining the band projection.
crs_transform - an optional array of 6 numbers specifying an affine transform from the specified CRS, in the order: xScale,
yShearing, xShearing, yScale, xTranslation and yTranslation. dimensions - an optional array of two integers defining the width and height to which the band is cropped. scale - an optional number, specifying the scale in meters of the band; ignored if crs and crs_transform is specified. crs - a default CRS string to use for any bands that do not explicitly specify one. crs_transform - a default affine transform to use for any bands that do not specify one, of the same format as the crs_t
**Usage:** `ee.data. getDownloadId ( params ) Â¶`

===

## ee :: params: An object containing visualization options with the following
possible values: name - a base name to use when constructing filenames.
bands - a description of the bands to download. Must be an array of dictionaries, each with the following keys: id - the name of the band, a string, required.
crs - an optional CRS string defining the band projection.
crs_transform - an optional array of 6 numbers specifying an affine transform from the specified CRS, in the order: xScale,
yShearing, xShearing, yScale, xTranslation and yTranslation. dimensions - an optional array of two integers defining the width and height to which the band is cropped. scale - an optional number, specifying the scale in meters of the band; ignored if crs and crs_transform is specified. crs - a default CRS string to use for any bands that do not explicitly specify one. crs_transform - a default affine transform to use for any bands that do not specify one, of the same format as the crs_transform of bands. dimensions - default image cropping dimensions to use for any bands that do 
**Usage:** `params: An object containing visualization options with the following`

===

## ee :: possible values:
name - a base name to use when constructing filenames.
bands - a description of the bands to download. Must be an array of dictionaries, each with the following keys: id - the name of the band, a string, required.
crs - an optional CRS string defining the band projection.
crs_transform - an optional array of 6 numbers specifying an affine transform from the specified CRS, in the order: xScale,
yShearing, xShearing, yScale, xTranslation and yTranslation. dimensions - an optional array of two integers defining the width and height to which the band is cropped. scale - an optional number, specifying the scale in meters of the band; ignored if crs and crs_transform is specified. crs - a default CRS string to use for any bands that do not explicitly specify one. crs_transform - a default affine transform to use for any bands that do not specify one, of the same format as the crs_transform of bands. dimensions - default image cropping dimensions to use for any bands that do not specify them.
**Usage:** `possible values:`

===

## ee :: dimensions - an optional array of two integers defining the width and
height to which the band is cropped.
**Usage:** `dimensions - an optional array of two integers defining the width and`

===

## ee :: crs - a default CRS string to use for any bands that do not explicitly
specify one.
**Usage:** `crs - a default CRS string to use for any bands that do not explicitly`

===

## ee :: ee.data. getIamPolicy ( asset_id ) Â¶
Loads ACL info for an asset, given an asset id. Args: asset_id: The asset to be retrieved. Returns: The assetâs ACL, as an IAM Policy.
**Usage:** `ee.data. getIamPolicy ( asset_id ) Â¶`

===

## ee :: ee.data. getInfo ( asset_id ) Â¶
Load info for an asset, given an asset id. Args: asset_id: The asset to be retrieved. Returns: The value call results, or None if the asset does not exist. DEPRECATED: Use getAsset
**Usage:** `ee.data. getInfo ( asset_id ) Â¶`

===

## ee :: ee.data. getList ( params ) Â¶
Get a list of contents for a collection asset. Args: params: An object containing request parameters with the following possible values: id (string) The asset id of the collection to list.
starttime (number) Start time, in msec since the epoch.
endtime (number) End time, in msec since the epoch.
fields (comma-separated strings) Field names to return. Returns: The list call results. DEPRECATED: Use listAssets or listImages
**Usage:** `ee.data. getList ( params ) Â¶`

===

## ee :: params: An object containing request parameters with the
following possible values: id (string) The asset id of the collection to list.
starttime (number) Start time, in msec since the epoch.
endtime (number) End time, in msec since the epoch.
fields (comma-separated strings) Field names to return.
**Usage:** `params: An object containing request parameters with the`

===

## ee :: following possible values:
id (string) The asset id of the collection to list.
starttime (number) Start time, in msec since the epoch.
endtime (number) End time, in msec since the epoch.
fields (comma-separated strings) Field names to return.
**Usage:** `following possible values:`

===

## ee :: ee.data. getMapId ( params ) Â¶
Get a Map ID for a given asset. Args: params: An object containing visualization options with the following possible values: image - The image to render, as an Image or a JSON string. The JSON string format is deprecated. version - (number) Version number of image (or latest).
bands - (comma-separated strings) Comma-delimited list of band names to be mapped to RGB. min - (comma-separated numbers) Value (or one per band) to map onto 00. max - (comma-separated numbers) Value (or one per band) to map onto FF. gain - (comma-separated numbers) Gain (or one per band) to map onto 00-FF. bias - (comma-separated numbers) Offset (or one per band) to map onto 00-FF. gamma - (comma-separated numbers) Gamma correction factor (or one per band). palette - (comma-separated strings) A string of comma-separated CSS-style color strings (single-band previews only). For example,
âFF0000,000000â. format - (string) The desired map tile image format. If omitted, one is chosen automatically. Can be âjpgâ
**Usage:** `ee.data. getMapId ( params ) Â¶`

===

## ee :: params: An object containing visualization options with the
following possible values: image - The image to render, as an Image or a JSON string. The JSON string format is deprecated. version - (number) Version number of image (or latest).
bands - (comma-separated strings) Comma-delimited list of band names to be mapped to RGB. min - (comma-separated numbers) Value (or one per band) to map onto 00. max - (comma-separated numbers) Value (or one per band) to map onto FF. gain - (comma-separated numbers) Gain (or one per band) to map onto 00-FF. bias - (comma-separated numbers) Offset (or one per band) to map onto 00-FF. gamma - (comma-separated numbers) Gamma correction factor (or one per band). palette - (comma-separated strings) A string of comma-separated CSS-style color strings (single-band previews only). For example,
âFF0000,000000â. format - (string) The desired map tile image format. If omitted, one is chosen automatically. Can be âjpgâ (does not support transparency)
or âpngâ (supports transparency).
**Usage:** `params: An object containing visualization options with the`

===

## ee :: image - The image to render, as an Image or a JSON string.
The JSON string format is deprecated.
**Usage:** `image - The image to render, as an Image or a JSON string.`

===

## ee :: min - (comma-separated numbers) Value (or one per band)
to map onto 00.
**Usage:** `min - (comma-separated numbers) Value (or one per band)`

===

## ee :: ee.data. getOperation ( operation_name ) Â¶
Retrieves the status of a long-running operation. Args: operation_name: The name of the operation to retrieve, in the format operations/AAAABBBBCCCCDDDDEEEEFFFF. Returns: An Operation status dictionary for the requested operation.
**Usage:** `ee.data. getOperation ( operation_name ) Â¶`

===

## ee :: operation_name: The name of the operation to retrieve, in the format
operations/AAAABBBBCCCCDDDDEEEEFFFF.
**Usage:** `operation_name: The name of the operation to retrieve, in the format`

===

## ee :: ee.data. getTableDownloadId ( params ) Â¶
Get a Download ID. Args: params: An object containing table download options with the following possible values: format - The download format, CSV or JSON.
selectors - Comma separated string of selectors that can be used to determine which attributes will be downloaded. filename - The name of the file that will be downloaded. Returns: A dict containing a docid and token.
**Usage:** `ee.data. getTableDownloadId ( params ) Â¶`

===

## ee :: params: An object containing table download options with the following
possible values: format - The download format, CSV or JSON.
selectors - Comma separated string of selectors that can be used to determine which attributes will be downloaded. filename - The name of the file that will be downloaded.
**Usage:** `params: An object containing table download options with the following`

===

## ee :: ee.data. getTaskList ( ) Â¶
Retrieves a list of the userâs tasks. Returns: A list of task status dictionaries, one for each task submitted to EE by
the current user. These include currently running tasks as well as recently
canceled or failed tasks. DEPRECATED: Use listOperations
**Usage:** `ee.data. getTaskList ( ) Â¶`

===

## ee :: ee.data. getTaskStatus ( taskId ) Â¶
Retrieve status of one or more long-running tasks. Args: taskId: ID of the task or a list of multiple IDs. Returns: List containing one object for each queried task, in the same order as
the input array, each object containing the following values: id (string) ID of the task.
state (string) State of the task, one of READY, RUNNING, COMPLETED, FAILED, CANCELLED; or UNKNOWN if the task with the specified ID
doesnât exist. error_message (string) For a FAILED task, a description of the error. DEPRECATED: Use getOperation
**Usage:** `ee.data. getTaskStatus ( taskId ) Â¶`

===

## ee :: ee.data. getThumbId ( params ) Â¶
Get a Thumbnail ID for a given asset. Args: params: Parameters identical to getMapId, plus: size - (a number or pair of numbers in format WIDTHxHEIGHT) Maximum dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ (default) or âjpgâ. Returns: A dictionary containing âthumbidâ and âtokenâ strings, which identify the
thumbnail.
**Usage:** `ee.data. getThumbId ( params ) Â¶`

===

## ee :: params: Parameters identical to getMapId, plus:
size - (a number or pair of numbers in format WIDTHxHEIGHT) Maximum dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ (default) or âjpgâ.
**Usage:** `params: Parameters identical to getMapId, plus:`

===

## ee :: size - (a number or pair of numbers in format WIDTHxHEIGHT) Maximum
dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling.
**Usage:** `size - (a number or pair of numbers in format WIDTHxHEIGHT) Maximum`

===

## ee :: ee.data. getThumbnail ( params ) Â¶
Get a Thumbnail for a given asset. Args: params: Parameters identical to getMapId, plus: size - (a number or pair of numbers in format WIDTHxHEIGHT) Maximum dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ (default) or âjpgâ. Returns: A thumbnail image as raw PNG data. DEPRECATED: Use getThumbId and makeThumbUrl
**Usage:** `ee.data. getThumbnail ( params ) Â¶`

===

## ee :: ee.data. getTileUrl ( mapid , x , y , z ) Â¶
Generate a URL for map tiles from a Map ID and coordinates. Args: mapid: The Map ID to generate tiles for, a dictionary returned by getMapId. x: The tile x coordinate.
y: The tile y coordinate.
z: The tile zoom level. Returns: The tile URL.
**Usage:** `ee.data. getTileUrl ( mapid , x , y , z ) Â¶`

===

## ee :: mapid: The Map ID to generate tiles for, a dictionary returned
by getMapId.
**Usage:** `mapid: The Map ID to generate tiles for, a dictionary returned`

===

## ee :: ee.data. getValue ( params ) Â¶
Retrieve a processed value from the front end. Args: params: A dictionary containing: json - (String) A JSON object to be evaluated. Returns: The value call results. DEPRECATED: Use computeValue
**Usage:** `ee.data. getValue ( params ) Â¶`

===

## ee :: params: A dictionary containing:
json - (String) A JSON object to be evaluated.
**Usage:** `params: A dictionary containing:`

===

## ee :: ee.data. initialize ( credentials=None , api_base_url=None , tile_base_url=None , use_cloud_api=None , cloud_api_base_ur
Initializes the data module, setting credentials and base URLs. If any of the arguments are unspecified, they will keep their old values;
the defaults if initialize() has never been called before. At least one of âcredentialsâ and âcloud_api_keyâ must be provided. If both
are provided, both will be used; in this case, the API keyâs project must
match the credentialsâ project. Args: credentials: The OAuth2 credentials.
api_base_url: The EarthEngine REST API endpoint.
tile_base_url: The EarthEngine REST tile endpoint.
use_cloud_api: Whether to use the EarthEngine Cloud API rather than the older REST API. cloud_api_base_url: The EarthEngine Cloud API endpoint.
cloud_api_key: The API key to use with the Cloud API.
project: The default cloud project associated with the user.
http_transport: The http transport to use
**Usage:** `ee.data. initialize ( credentials=None , api_base_url=None , tile_base_url=None , use_cloud_api=None , cloud_api_base_url=None , cloud_api_key=None , project=None , http_transport=None ) Â¶`

===

## ee :: ee.data. listAssets ( params ) Â¶
**Usage:** `ee.data. listAssets ( params ) Â¶`

===

## ee :: ee.data. listBuckets ( project=None ) Â¶
**Usage:** `ee.data. listBuckets ( project=None ) Â¶`

===

## ee :: ee.data. listImages ( params ) Â¶
**Usage:** `ee.data. listImages ( params ) Â¶`

===

## ee :: ee.data. listOperations ( project=None ) Â¶
Retrieves a list of the userâs tasks. Args: project: The project to list operations for, uses the default set project if none is provided. Returns: A list of Operation status dictionaries, one for each task submitted to EE
by the current user. These include currently running tasks as well as
recently canceled or failed tasks.
**Usage:** `ee.data. listOperations ( project=None ) Â¶`

===

## ee :: project: The project to list operations for, uses the default set project
if none is provided.
**Usage:** `project: The project to list operations for, uses the default set project`

===

## ee :: ee.data. makeDownloadUrl ( downloadId ) Â¶
Create a download URL from the given docid and token. Args: downloadId: An object containing a download docid and token. Returns: A URL from which the download can be obtained. DEPRECATED: Use getThumbId and makeThumbUrl
**Usage:** `ee.data. makeDownloadUrl ( downloadId ) Â¶`

===

## ee :: ee.data. makeTableDownloadUrl ( downloadId ) Â¶
Create a table download URL from a docid and token. Args: downloadId: A table download id and token. Returns: A Url from which the download can be obtained.
**Usage:** `ee.data. makeTableDownloadUrl ( downloadId ) Â¶`

===

## ee :: ee.data. makeThumbUrl ( thumbId ) Â¶
Create a thumbnail URL from the given thumbid and token. Args: thumbId: An object containing a thumbnail thumbid and token. Returns: A URL from which the thumbnail can be obtained.
**Usage:** `ee.data. makeThumbUrl ( thumbId ) Â¶`

===

## ee :: ee.data. newTaskId ( count=1 ) Â¶
Generate an ID for a long-running task. Args: count: Optional count of IDs to generate, one by default. Returns: A list containing generated ID strings.
**Usage:** `ee.data. newTaskId ( count=1 ) Â¶`

===

## ee :: ee.data. profiling ( hook ) Â¶
Returns a context manager which enables or disables profiling. If hook is not None, enables profiling for all API calls in its scope and
calls the hook function with all resulting profile IDs. If hook is null,
disables profiling (or leaves it disabled). Args: hook: A function of one argument which is called with each profile ID obtained from API calls, just before the API call returns.
**Usage:** `ee.data. profiling ( hook ) Â¶`

===

## ee :: hook: A function of one argument which is called with each profile
ID obtained from API calls, just before the API call returns.
**Usage:** `hook: A function of one argument which is called with each profile`

===

## ee :: ee.data. renameAsset ( sourceId , destinationId ) Â¶
Renames the asset from sourceId to destinationId. Args: sourceId: The ID of the asset to rename.
destinationId: The new ID of the asset.
**Usage:** `ee.data. renameAsset ( sourceId , destinationId ) Â¶`

===

## ee :: ee.data. reset ( ) Â¶
Resets the data module, clearing credentials and custom base URLs.
**Usage:** `ee.data. reset ( ) Â¶`

===

## ee :: ee.data. send_ ( path , params , opt_method='POST' , opt_raw=False ) Â¶
Send an API call. Args: path: The API endpoint to call, or a full URL.
params: The call parameters.
opt_method: The HTTPRequest method (GET or POST).
opt_raw: Whether the data should be returned raw, without attempting to decode it as JSON. Returns: The data object returned by the API call. Raises: EEException: For malformed requests or errors from the server.
**Usage:** `ee.data. send_ ( path , params , opt_method='POST' , opt_raw=False ) Â¶`

===

## ee :: ee.data. setAssetAcl ( assetId , aclUpdate ) Â¶
Sets the access control list of the asset with the given ID. The owner ACL cannot be changed, and the final ACL of the asset
is constructed by merging the OWNER entries of the old ACL with
the incoming ACL record. Args: assetId: The ID of the asset to set the ACL on.
aclUpdate: The updated ACL for the asset. Must be formatted like the value returned by getAssetAcl but without âownersâ. DEPRECATED: Use setIamPolicy
**Usage:** `ee.data. setAssetAcl ( assetId , aclUpdate ) Â¶`

===

## ee :: ee.data. setAssetProperties ( assetId , properties ) Â¶
Sets metadata properties of the asset with the given ID. To delete a property, set its value to None.
The authenticated user must be a writer or owner of the asset. Args: assetId: The ID of the asset to set the ACL on.
properties: A dictionary of keys and values for the properties to update. DEPRECATED: Use updateAsset
**Usage:** `ee.data. setAssetProperties ( assetId , properties ) Â¶`

===

## ee :: ee.data. setCloudApiKey ( cloud_api_key ) Â¶
Sets the Cloud API key parameter (âapi_keyâ) for all requests.
**Usage:** `ee.data. setCloudApiKey ( cloud_api_key ) Â¶`

===

## ee :: ee.data. setCloudApiUserProject ( cloud_api_user_project ) Â¶
**Usage:** `ee.data. setCloudApiUserProject ( cloud_api_user_project ) Â¶`

===

## ee :: ee.data. setDeadline ( milliseconds ) Â¶
Sets the timeout length for API requests. Args: milliseconds: The number of milliseconds to wait for a request before considering it timed out. 0 means no limit.
**Usage:** `ee.data. setDeadline ( milliseconds ) Â¶`

===

## ee :: milliseconds: The number of milliseconds to wait for a request
before considering it timed out. 0 means no limit.
**Usage:** `milliseconds: The number of milliseconds to wait for a request`

===

## ee :: ee.data. setIamPolicy ( asset_id , policy ) Â¶
Sets ACL info for an asset. Args: asset_id: The asset to set the ACL policy on.
policy: The new Policy to apply to the asset. This replaces the current Policy. Returns: The new ACL, as an IAM Policy.
**Usage:** `ee.data. setIamPolicy ( asset_id , policy ) Â¶`

===

## ee :: ee.data. startIngestion ( request_id , params , allow_overwrite=False ) Â¶
Creates an image asset import task. Args: request_id (string): A unique ID for the ingestion, from newTaskId. If you are using the Cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried. params: The object that describes the import task, which can have these fields: id (string) The destination asset id (e.g. users/foo/bar).
tilesets (array) A list of Google Cloud Storage source file paths formatted like: [{âsourcesâ: [ {âprimaryPathâ: âfoo.tifâ, âadditionalPathsâ: [âfoo.prjâ]},
{âprimaryPathâ: âbar.tifâ, âadditionalPathsâ: [âbar.prjâ}, ]}] Where path values correspond to source filesâ Google Cloud Storage
object names, e.g. âgs://bucketname/filename.tifâ bands (array) An optional list of band names formatted like: [{âidâ: âRâ}, {âidâ: âGâ}, {âidâ: âB
**Usage:** `ee.data. startIngestion ( request_id , params , allow_overwrite=False ) Â¶`

===

## ee :: request_id (string): A unique ID for the ingestion, from newTaskId.
If you are using the Cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried.
**Usage:** `request_id (string): A unique ID for the ingestion, from newTaskId.`

===

## ee :: have these fields:
id (string) The destination asset id (e.g. users/foo/bar).
tilesets (array) A list of Google Cloud Storage source file paths formatted like: [{âsourcesâ: [ {âprimaryPathâ: âfoo.tifâ, âadditionalPathsâ: [âfoo.prjâ]},
{âprimaryPathâ: âbar.tifâ, âadditionalPathsâ: [âbar.prjâ}, ]}] Where path values correspond to source filesâ Google Cloud Storage
object names, e.g. âgs://bucketname/filename.tifâ bands (array) An optional list of band names formatted like: [{âidâ: âRâ}, {âidâ: âGâ}, {âidâ: âBâ}]
**Usage:** `have these fields:`

===

## ee :: formatted like:
[{âsourcesâ: [ {âprimaryPathâ: âfoo.tifâ, âadditionalPathsâ: [âfoo.prjâ]},
{âprimaryPathâ: âbar.tifâ, âadditionalPathsâ: [âbar.prjâ}, ]}]
**Usage:** `formatted like:`

===

## ee :: [{âsourcesâ: [
{âprimaryPathâ: âfoo.tifâ, âadditionalPathsâ: [âfoo.prjâ]},
{âprimaryPathâ: âbar.tifâ, âadditionalPathsâ: [âbar.prjâ},
**Usage:** `[{âsourcesâ: [`

===

## ee :: bands (array) An optional list of band names formatted like:
[{âidâ: âRâ}, {âidâ: âGâ}, {âidâ: âBâ}]
**Usage:** `bands (array) An optional list of band names formatted like:`

===

## ee :: ee.data. startProcessing ( taskId , params ) Â¶
Create processing task that exports or pre-renders an image. Args: taskId: ID for the task (obtained using newTaskId).
params: The object that describes the processing task; only fields that are common for all processing types are documented below. type (string) Either âEXPORT_IMAGEâ, âEXPORT_FEATURESâ, âEXPORT_VIDEOâ or âEXPORT_TILESâ. json (string) JSON description of the image. Returns: A dict with optional notes about the created task. DEPRECATED: Use one of the export* functions
**Usage:** `ee.data. startProcessing ( taskId , params ) Â¶`

===

## ee :: that are common for all processing types are documented below.
type (string) Either âEXPORT_IMAGEâ, âEXPORT_FEATURESâ, âEXPORT_VIDEOâ or âEXPORT_TILESâ. json (string) JSON description of the image.
**Usage:** `that are common for all processing types are documented below.`

===

## ee :: type (string) Either âEXPORT_IMAGEâ, âEXPORT_FEATURESâ,
âEXPORT_VIDEOâ or âEXPORT_TILESâ.
**Usage:** `type (string) Either âEXPORT_IMAGEâ, âEXPORT_FEATURESâ,`

===

## ee :: ee.data. startTableIngestion ( request_id , params , allow_overwrite=False ) Â¶
Creates a table asset import task. Args: request_id (string): A unique ID for the ingestion, from newTaskId. If you are using the Cloud API, this does not need to be from newTaskId,
(though thatâs a good idea, as itâs a good source of unique strings).
It can also be empty, but in that case the request is more likely to
fail as it cannot be safely retried. params: The object that describes the import task, which can have these fields: id (string) The destination asset id (e.g. users/foo/bar).
sources (array) A list of CNS source file paths with optional character encoding formatted like:
âsourcesâ: [{ âprimaryPathâ: âstates.shpâ, âcharsetâ: âUTF-8â }]
Where path values correspond to source filesâ CNS locations,
e.g. âgooglefile://namespace/foobar.shpâ, and âcharsetâ refers to
the character encoding of the source file. If you are using the Cloud API, this object must instead be a dict
representation of a TableManifest. allow_overwrite: Whether the ingest
**Usage:** `ee.data. startTableIngestion ( request_id , params , allow_overwrite=False ) Â¶`

===

## ee :: ee.data. updateAsset ( asset_id , asset , update_mask ) Â¶
Updates an asset. Args: asset_id: The ID of the asset to update.
asset: The updated version of the asset, containing only the new values of the fields to be updated. Only the âstart_timeâ, âend_timeâ, and
âpropertiesâ fields can be updated. If a value is named in âupdate_maskâ,
but is unset in âassetâ, then that value will be deleted from the asset. update_mask: A list of the values to update. This should contain the strings âstart_timeâ or âend_timeâ to update the corresponding timestamp. If
a property is to be updated or deleted, it should be named here as
âproperties.THAT_PROPERTY_NAMEâ. If the entire property set is to be
replaced, this should contain the string âpropertiesâ. If this list is
empty, all properties and both timestamps will be updated.
**Usage:** `ee.data. updateAsset ( asset_id , asset , update_mask ) Â¶`

===

## ee :: update_mask: A list of the values to update. This should contain the strings
âstart_timeâ or âend_timeâ to update the corresponding timestamp. If
a property is to be updated or deleted, it should be named here as
âproperties.THAT_PROPERTY_NAMEâ. If the entire property set is to be
replaced, this should contain the string âpropertiesâ. If this list is
empty, all properties and both timestamps will be updated.
**Usage:** `update_mask: A list of the values to update. This should contain the strings`

===

## ee :: ee.deprecation. CanUseDeprecated ( func ) Â¶
Ignores deprecation warnings emitted while the decorated function runs.
**Usage:** `ee.deprecation. CanUseDeprecated ( func ) Â¶`

===

## ee :: ee.deprecation. Deprecated ( message ) Â¶
Returns a decorator with a given warning message.
**Usage:** `ee.deprecation. Deprecated ( message ) Â¶`

===

## ee :: ee.deserializer. decode ( json_obj ) Â¶
Decodes an object previously encoded using the EE API v2 (DAG) format. Args: json_obj: The serialied object to decode. Returns: The decoded object.
**Usage:** `ee.deserializer. decode ( json_obj ) Â¶`

===

## ee :: ee.deserializer. fromJSON ( json_obj ) Â¶
Deserialize an object from a JSON string appropriate for API calls. Args: json_obj: The JSON represenation of the input. Returns: The deserialized object.
**Usage:** `ee.deserializer. fromJSON ( json_obj ) Â¶`

===

## ee :: class ee.dictionary. Dictionary ( arg=None ) Â¶
Bases: ee.computedobject.ComputedObject An object to represent dictionaries. encode ( opt_encoder=None ) Â¶ Encodes the object in a format compatible with Serializer. encode_cloud_value ( opt_encoder=None ) Â¶ Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.dictionary. Dictionary ( arg=None ) Â¶`

===

## ee :: encode ( opt_encoder=None ) Â¶
Encodes the object in a format compatible with Serializer.
**Usage:** `encode ( opt_encoder=None ) Â¶`

===

## ee :: encode_cloud_value ( opt_encoder=None ) Â¶
Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object.
**Usage:** `encode_cloud_value ( opt_encoder=None ) Â¶`

===

## ee :: class ee.ee_date. Date ( date , opt_tz=None ) Â¶
Bases: ee.computedobject.ComputedObject An object to represent dates. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.ee_date. Date ( date , opt_tz=None ) Â¶`

===

## ee :: exception ee.ee_exception. EEException Â¶
Bases: Exception A simple exception for the EE library.
**Usage:** `exception ee.ee_exception. EEException Â¶`

===

## ee :: class ee.ee_list. List ( arg ) Â¶
Bases: ee.computedobject.ComputedObject An object to represent lists. encode ( opt_encoder=None ) Â¶ Encodes the object in a format compatible with Serializer. encode_cloud_value ( opt_encoder=None ) Â¶ Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.ee_list. List ( arg ) Â¶`

===

## ee :: class ee.ee_number. Number ( number ) Â¶
Bases: ee.computedobject.ComputedObject An object to represent numbers. encode ( opt_encoder=None ) Â¶ Encodes the object in a format compatible with Serializer. encode_cloud_value ( opt_encoder=None ) Â¶ Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.ee_number. Number ( number ) Â¶`

===

## ee :: class ee.ee_string. String ( string ) Â¶
Bases: ee.computedobject.ComputedObject An object to represent strings. encode ( opt_encoder=None ) Â¶ Encodes the object in a format compatible with Serializer. encode_cloud_value ( opt_encoder=None ) Â¶ Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.ee_string. String ( string ) Â¶`

===

## ee :: ee.ee_types. classToName ( klass ) Â¶
Converts a class to the API-friendly type name. Args: klass: The class. Returns: The name of the class, or âObjectâ if not recognized.
**Usage:** `ee.ee_types. classToName ( klass ) Â¶`

===

## ee :: ee.ee_types. isArray ( obj ) Â¶
Returns true if this object is an array or array variable. Args: obj: The object to check. Returns: Whether the object is an array or array variable.
**Usage:** `ee.ee_types. isArray ( obj ) Â¶`

===

## ee :: ee.ee_types. isNumber ( obj ) Â¶
Returns true if this object is a number or number variable. Args: obj: The object to check. Returns: Whether the object is a number or number variable.
**Usage:** `ee.ee_types. isNumber ( obj ) Â¶`

===

## ee :: ee.ee_types. isString ( obj ) Â¶
Returns true if this object is a string or string variable. Args: obj: The object to check. Returns: Whether the object is a string or string variable.
**Usage:** `ee.ee_types. isString ( obj ) Â¶`

===

## ee :: ee.ee_types. isSubtype ( firstType , secondType ) Â¶
Checks whether a type is a subtype of another. Args: firstType: The first type name.
secondType: The second type name. Returns: Whether secondType is a subtype of firstType.
**Usage:** `ee.ee_types. isSubtype ( firstType , secondType ) Â¶`

===

## ee :: ee.ee_types. nameToClass ( name ) Â¶
Converts a class name to a class.  Returns None if not an ee class. Args: name: The class name. Returns: The named class.
**Usage:** `ee.ee_types. nameToClass ( name ) Â¶`

===

## ee :: class ee.element. Element ( func , args , opt_varName=None ) Â¶
Bases: ee.computedobject.ComputedObject Base class for ImageCollection and FeatureCollection. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class. set ( *args ) Â¶ Overrides one or more metadata properties of an Element. Args: * args: Either a dictionary of properties, or a vararg sequence of properties, e.g. key1, value1, key2, value2, â¦ Returns: The element with the specified properties overridden.
**Usage:** `class ee.element. Element ( func , args , opt_varName=None ) Â¶`

===

## ee :: set ( *args ) Â¶
Overrides one or more metadata properties of an Element. Args: * args: Either a dictionary of properties, or a vararg sequence of properties, e.g. key1, value1, key2, value2, â¦ Returns: The element with the specified properties overridden.
**Usage:** `set ( *args ) Â¶`

===

## ee :: * args: Either a dictionary of properties, or a vararg sequence of
properties, e.g. key1, value1, key2, value2, â¦
**Usage:** `* args: Either a dictionary of properties, or a vararg sequence of`

===

## ee :: class ee.encodable. Encodable Â¶
Bases: object An interface implemented by objects that can serialize themselves. encode ( encoder ) Â¶ Encodes the object in a format compatible with Serializer. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object. encode_cloud_value ( encoder ) Â¶ Encodes the object as a ValueNode. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the object.
**Usage:** `class ee.encodable. Encodable Â¶`

===

## ee :: class ee.encodable. EncodableFunction Â¶
Bases: object An interface implemented by functions that can serialize themselves. encode_cloud_invocation ( encoder ) Â¶ Encodes the function as a FunctionInvocation. Args: encoder: A function that can be called to encode the components of an object. Returns a reference to the encoded value. Returns: The encoded form of the function. encode_invocation ( encoder ) Â¶ Encodes the function in a format compatible with Serializer. Args: encoder: A function that can be called to encode the components of an object. Returns: The encoded form of the function.
**Usage:** `class ee.encodable. EncodableFunction Â¶`

===

## ee :: class ee.feature. Feature ( geom , opt_properties=None ) Â¶
Bases: ee.element.Element An object representing EE Features. getMapId ( vis_params=None ) Â¶ Fetch and return a map id and token, suitable for use in a Map overlay. Args: vis_params: The visualization parameters. Currently only one parameter, âcolorâ, containing a hex RGB color string is allowed. Returns: An object containing a mapid string, an access token, plus a
Collection.draw image wrapping a FeatureCollection containing
this feature. classmethod initialize ( ) Â¶ Imports API functions to this class. static name ( ) Â¶ Returns the name of the object, used in __str__(). classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.feature. Feature ( geom , opt_properties=None ) Â¶`

===

## ee :: getMapId ( vis_params=None ) Â¶
Fetch and return a map id and token, suitable for use in a Map overlay. Args: vis_params: The visualization parameters. Currently only one parameter, âcolorâ, containing a hex RGB color string is allowed. Returns: An object containing a mapid string, an access token, plus a
Collection.draw image wrapping a FeatureCollection containing
this feature.
**Usage:** `getMapId ( vis_params=None ) Â¶`

===

## ee :: vis_params: The visualization parameters. Currently only one parameter,
âcolorâ, containing a hex RGB color string is allowed.
**Usage:** `vis_params: The visualization parameters. Currently only one parameter,`

===

## ee :: class ee.featurecollection. FeatureCollection ( args , opt_column=None ) Â¶
Bases: ee.collection.Collection A representation of a FeatureCollection. static elementType ( ) Â¶ Returns the type of the collectionâs elements. getDownloadURL ( filetype=None , selectors=None , filename=None ) Â¶ Get a download URL for this feature collection. Args: filetype: The filetype of download, either CSV or JSON. Defaults to CSV.
selectors: The selectors that should be used to determine which attributes will be downloaded. filename: The name of the file to be downloaded. Returns: A URL to download the specified feature collection. getDownloadUrl ( filetype=None , selectors=None , filename=None ) Â¶ Get a download URL for this feature collection. Args: filetype: The filetype of download, either CSV or JSON. Defaults to CSV.
selectors: The selectors that should be used to determine which attributes will be downloaded. filename: The name of the file to be downloaded. Returns: A URL to download the specified feature collection. DEPRECATED: Use getDownloadURL(). getMapId ( vis_p
**Usage:** `class ee.featurecollection. FeatureCollection ( args , opt_column=None ) Â¶`

===

## ee :: getDownloadURL ( filetype=None , selectors=None , filename=None ) Â¶
Get a download URL for this feature collection. Args: filetype: The filetype of download, either CSV or JSON. Defaults to CSV.
selectors: The selectors that should be used to determine which attributes will be downloaded. filename: The name of the file to be downloaded. Returns: A URL to download the specified feature collection.
**Usage:** `getDownloadURL ( filetype=None , selectors=None , filename=None ) Â¶`

===

## ee :: getDownloadUrl ( filetype=None , selectors=None , filename=None ) Â¶
Get a download URL for this feature collection. Args: filetype: The filetype of download, either CSV or JSON. Defaults to CSV.
selectors: The selectors that should be used to determine which attributes will be downloaded. filename: The name of the file to be downloaded. Returns: A URL to download the specified feature collection. DEPRECATED: Use getDownloadURL().
**Usage:** `getDownloadUrl ( filetype=None , selectors=None , filename=None ) Â¶`

===

## ee :: select ( propertySelectors , newProperties=None , retainGeometry=True , *args ) Â¶
Select properties from each feature in a collection. Args: propertySelectors: An array of names or regexes specifying the properties to select. newProperties: An array of strings specifying the new names for the selected properties.  If supplied, the length must match the number
of properties selected. retainGeometry: A boolean.  When false, the result will have no geometry. * args: Selector elements as varargs. Returns: The feature collection with selected properties.
**Usage:** `select ( propertySelectors , newProperties=None , retainGeometry=True , *args ) Â¶`

===

## ee :: propertySelectors: An array of names or regexes specifying the properties
to select.
**Usage:** `propertySelectors: An array of names or regexes specifying the properties`

===

## ee :: Example usage:
Filter(âtimeâ, low, high) .bounds(ring)
.eq(âtimeâ, value)
.lt(âtimeâ, value)
**Usage:** `Example usage:`

===

## ee :: Filter(âtimeâ, low, high)
.bounds(ring)
.eq(âtimeâ, value)
.lt(âtimeâ, value)
**Usage:** `Filter(âtimeâ, low, high)`

===

## ee :: class ee.filter. Filter ( opt_filter=None ) Â¶
Bases: ee.computedobject.ComputedObject An object to represent collection filters. static And ( *args ) Â¶ Combine two or more filters using boolean AND. Not ( ) Â¶ Returns the opposite of this filter. Returns: The negated filter, which will match iff this filter doesnât. static Or ( *args ) Â¶ Combine two or more filters using boolean OR. static date ( start , opt_end=None ) Â¶ Filter images by date. The start and end may be a Date, numbers (interpreted as milliseconds since
1970-01-01T00:00:00Z), or strings (such as â1996-01-01T08:00â). Args: start: The inclusive start date.
opt_end: The optional exclusive end date, If not specified, a 1-millisecond range starting at âstartâ is created. Returns: The modified filter. static eq ( name , value ) Â¶ Filter to metadata equal to the given value. static geometry ( geometry , opt_errorMargin=None ) Â¶ Filter on bounds. Items in the collection with a footprint that fails to intersect
the bounds will be excluded when the collection i
**Usage:** `class ee.filter. Filter ( opt_filter=None ) Â¶`

===

## ee :: static And ( *args ) Â¶
Combine two or more filters using boolean AND.
**Usage:** `static And ( *args ) Â¶`

===

## ee :: Not ( ) Â¶
Returns the opposite of this filter. Returns: The negated filter, which will match iff this filter doesnât.
**Usage:** `Not ( ) Â¶`

===

## ee :: static Or ( *args ) Â¶
Combine two or more filters using boolean OR.
**Usage:** `static Or ( *args ) Â¶`

===

## ee :: static date ( start , opt_end=None ) Â¶
Filter images by date. The start and end may be a Date, numbers (interpreted as milliseconds since
1970-01-01T00:00:00Z), or strings (such as â1996-01-01T08:00â). Args: start: The inclusive start date.
opt_end: The optional exclusive end date, If not specified, a 1-millisecond range starting at âstartâ is created. Returns: The modified filter.
**Usage:** `static date ( start , opt_end=None ) Â¶`

===

## ee :: static eq ( name , value ) Â¶
Filter to metadata equal to the given value.
**Usage:** `static eq ( name , value ) Â¶`

===

## ee :: static geometry ( geometry , opt_errorMargin=None ) Â¶
Filter on bounds. Items in the collection with a footprint that fails to intersect
the bounds will be excluded when the collection is evaluated. Args: geometry: The geometry to filter to either as a GeoJSON geometry, or a FeatureCollection, from which a geometry will be extracted. opt_errorMargin: An optional error margin. If a number, interpreted as sphere surface meters. Returns: The modified filter.
**Usage:** `static geometry ( geometry , opt_errorMargin=None ) Â¶`

===

## ee :: geometry: The geometry to filter to either as a GeoJSON geometry,
or a FeatureCollection, from which a geometry will be extracted.
**Usage:** `geometry: The geometry to filter to either as a GeoJSON geometry,`

===

## ee :: static gt ( name , value ) Â¶
Filter on metadata greater than the given value.
**Usage:** `static gt ( name , value ) Â¶`

===

## ee :: static gte ( name , value ) Â¶
Filter on metadata greater than or equal to the given value.
**Usage:** `static gte ( name , value ) Â¶`

===

## ee :: static inList ( opt_leftField=None , opt_rightValue=None , opt_rightField=None , opt_leftValue=None ) Â¶
Filter on metadata contained in a list. Args: opt_leftField: A selector for the left operand. Should not be specified if leftValue is specified. opt_rightValue: The value of the right operand. Should not be specified if rightField is specified. opt_rightField: A selector for the right operand. Should not be specified if rightValue is specified. opt_leftValue: The value of the left operand. Should not be specified if leftField is specified. Returns: The constructed filter.
**Usage:** `static inList ( opt_leftField=None , opt_rightValue=None , opt_rightField=None , opt_leftValue=None ) Â¶`

===

## ee :: opt_leftField: A selector for the left operand.
Should not be specified if leftValue is specified.
**Usage:** `opt_leftField: A selector for the left operand.`

===

## ee :: static lt ( name , value ) Â¶
Filter to metadata less than the given value.
**Usage:** `static lt ( name , value ) Â¶`

===

## ee :: static lte ( name , value ) Â¶
Filter on metadata less than or equal to the given value.
**Usage:** `static lte ( name , value ) Â¶`

===

## ee :: static metadata_ ( name , operator , value ) Â¶
Filter on metadata. This is deprecated. Args: name: The property name to filter on.
operator: The type of comparison. One of: âequalsâ, âless_thanâ, âgreater_thanâ, âcontainsâ, âbegins_withâ,
âends_withâ, or any of these prefixed with â not_ â. value: The value to compare against. Returns: The new filter. Deprecated.  Use ee.Filter.eq(), ee.Filter.gte(), etc.â
**Usage:** `static metadata_ ( name , operator , value ) Â¶`

===

## ee :: static neq ( name , value ) Â¶
Filter to metadata not equal to the given value.
**Usage:** `static neq ( name , value ) Â¶`

===

## ee :: predicateCount ( ) Â¶
Return the number of predicates that have been added to this filter. Returns: The number of predicates that have been added to this filter.
This does not count nested predicates.
**Usage:** `predicateCount ( ) Â¶`

===

## ee :: class ee.function. Function Â¶
Bases: ee.encodable.EncodableFunction An abstract base class for functions callable by the EE API. Subclasses must implement encode_invocation() and getSignature(). apply ( named_args ) Â¶ Calls the function with a dictionary of named arguments. Args: named_args: A dictionary of named arguments to pass to the function. Returns: A ComputedObject representing the called function. If the signature
specifies a recognized return type, the returned value will be cast
to that type. call ( *args , **kwargs ) Â¶ Calls the function with the given positional and keyword arguments. Args: * args: The positional arguments to pass to the function. ** kwargs: The named arguments to pass to the function. Returns: A ComputedObject representing the called function. If the signature
specifies a recognized return type, the returned value will be cast
to that type. getReturnType ( ) Â¶ getSignature ( ) Â¶ Returns a description of the interface provided by this function. Returns: The functionâs signature, 
**Usage:** `class ee.function. Function Â¶`

===

## ee :: apply ( named_args ) Â¶
Calls the function with a dictionary of named arguments. Args: named_args: A dictionary of named arguments to pass to the function. Returns: A ComputedObject representing the called function. If the signature
specifies a recognized return type, the returned value will be cast
to that type.
**Usage:** `apply ( named_args ) Â¶`

===

## ee :: call ( *args , **kwargs ) Â¶
Calls the function with the given positional and keyword arguments. Args: * args: The positional arguments to pass to the function. ** kwargs: The named arguments to pass to the function. Returns: A ComputedObject representing the called function. If the signature
specifies a recognized return type, the returned value will be cast
to that type.
**Usage:** `call ( *args , **kwargs ) Â¶`

===

## ee :: getReturnType ( ) Â¶
**Usage:** `getReturnType ( ) Â¶`

===

## ee :: The functionâs signature, a dictionary containing:
name: string
returns: type name string
args: list of argument dictionaries, each containing: name: string
type: type name string
optional: boolean
default: an arbitrary primitive or encodable object
**Usage:** `The functionâs signature, a dictionary containing:`

===

## ee :: nameArgs ( args , extra_keyword_args=None ) Â¶
Converts a list of positional arguments to a map of keyword arguments. Uses the functionâs signature for argument names. Note that this does not
check whether the array contains enough arguments to satisfy the call. Args: args: Positional arguments to the function.
extra_keyword_args: Optional named arguments to add. Returns: Keyword arguments to the function. Raises: EEException: If conflicting arguments or too many of them are supplied.
**Usage:** `nameArgs ( args , extra_keyword_args=None ) Â¶`

===

## ee :: promoteArgs ( args ) Â¶
Promotes arguments to their types based on the functionâs signature. Verifies that all required arguments are provided and no unknown arguments
are present. Args: args: A dictionary of keyword arguments to the function. Returns: A dictionary of promoted arguments. Raises: EEException: If unrecognized arguments are passed or required ones are missing.
**Usage:** `promoteArgs ( args ) Â¶`

===

## ee :: EEException: If unrecognized arguments are passed or required ones are
missing.
**Usage:** `EEException: If unrecognized arguments are passed or required ones are`

===

## ee :: serialize ( for_cloud_api=False ) Â¶
**Usage:** `serialize ( for_cloud_api=False ) Â¶`

===

## ee :: class ee.geometry. Geometry ( geo_json , opt_proj=None , opt_geodesic=None , opt_evenOdd=None ) Â¶
Bases: ee.computedobject.ComputedObject An Earth Engine geometry. static LineString ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , *args ) Â¶ Constructs an ee.Geometry describing a LineString. Args: coords: A list of at least two points.  May be a list of coordinates in the GeoJSON âLineStringâ format, a list of at least two ee.Geometry
describing a point, or a list of at least four numbers defining the
[x,y] coordinates of at least two points. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. maxError: Max error when input geometry must be reprojected to an explicitly requested result projection 
**Usage:** `class ee.geometry. Geometry ( geo_json , opt_proj=None , opt_geodesic=None , opt_evenOdd=None ) Â¶`

===

## ee :: static LineString ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> 
Constructs an ee.Geometry describing a LineString. Args: coords: A list of at least two points.  May be a list of coordinates in the GeoJSON âLineStringâ format, a list of at least two ee.Geometry
describing a point, or a list of at least four numbers defining the
[x,y] coordinates of at least two points. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. maxError: Max error when input geometry must be reprojected to an explicitly requested result projection or geodesic state. * args: For convenience, varargs may be used when all arguments are numbers. This allows creating geodesic EPSG:4326 LineStrings given
an even number of arguments, e.g.
ee.Geometry
**Usage:** `static LineString ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , *args ) Â¶`

===

## ee :: coords: A list of at least two points.  May be a list of coordinates in
the GeoJSON âLineStringâ format, a list of at least two ee.Geometry
describing a point, or a list of at least four numbers defining the
[x,y] coordinates of at least two points.
**Usage:** `coords: A list of at least two points.  May be a list of coordinates in`

===

## ee :: static LinearRing ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> 
Constructs an ee.Geometry describing a LinearRing. If the last point is not equal to the first, a duplicate of the first
point will be added at the end. Args: coords: A list of points in the ring. May be a list of coordinates in the GeoJSON âLinearRingâ format, a list of at least three ee.Geometry
describing a point, or a list of at least six numbers defining the
[x,y] coordinates of at least three points. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. maxError: Max error when input geometry must be reprojected to an explicitly requested result projection or geodesic state. * args: For convenience, varargs may be used when all arguments are numbers. 
**Usage:** `static LinearRing ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , *args ) Â¶`

===

## ee :: coords: A list of points in the ring. May be a list of coordinates in
the GeoJSON âLinearRingâ format, a list of at least three ee.Geometry
describing a point, or a list of at least six numbers defining the
[x,y] coordinates of at least three points.
**Usage:** `coords: A list of points in the ring. May be a list of coordinates in`

===

## ee :: static MultiLineString ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object obj
Constructs an ee.Geometry describing a MultiLineString. Create a GeoJSON MultiLineString from either a list of points, or an array
of lines (each an array of Points).  If a list of points is specified,
only a single line is created. Args: coords: A list of linestrings. May be a list of coordinates in the GeoJSON âMultiLineStringâ format, a list of at least two ee.Geometry
describing a LineString, or a list of number defining a single
linestring. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. maxError: Max error when input geometry must be reprojected to an explicitly requested result projection or geodesic state. * args: For convenience, varargs may 
**Usage:** `static MultiLineString ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , *args ) Â¶`

===

## ee :: coords: A list of linestrings. May be a list of coordinates in the
GeoJSON âMultiLineStringâ format, a list of at least two ee.Geometry
describing a LineString, or a list of number defining a single
linestring.
**Usage:** `coords: A list of linestrings. May be a list of coordinates in the`

===

## ee :: static MultiPoint ( coords=<object object> , proj=<object object> , *args ) Â¶
Constructs an ee.Geometry describing a MultiPoint. Args: coords: A list of points, each in the GeoJSON âcoordinatesâ format of a Point, or a list of the x,y coordinates in the given projection, or
an ee.Geometry describing a point. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are
no ee.Geometry inputs. * args: For convenience, varargs may be used when all arguments are numbers. This allows creating EPSG:4326 MultiPoints given an even
number of arguments, e.g.
ee.Geometry.MultiPoint(aLng, aLat, bLng, bLat, â¦). Returns: An ee.Geometry describing a MultiPoint.
**Usage:** `static MultiPoint ( coords=<object object> , proj=<object object> , *args ) Â¶`

===

## ee :: coords: A list of points, each in the GeoJSON âcoordinatesâ format of a
Point, or a list of the x,y coordinates in the given projection, or
an ee.Geometry describing a point.
**Usage:** `coords: A list of points, each in the GeoJSON âcoordinatesâ format of a`

===

## ee :: static MultiPolygon ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object
Constructs an ee.Geometry describing a MultiPolygon. If created from points, only one polygon can be specified. Args: coords: A list of polygons. May be a list of coordinates in the GeoJSON âMultiPolygonâ format, a list of ee.Geometry describing a Polygon,
or a list of number defining a single polygon boundary. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. maxError: Max error when input geometry must be reprojected to an explicitly requested result projection or geodesic state. evenOdd: If true, polygon interiors will be determined by the even/odd rule, where a point is inside if it crosses an odd number of edges to
reach a point at infinity. Otherw
**Usage:** `static MultiPolygon ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , evenOdd=<object object> , *args ) Â¶`

===

## ee :: coords: A list of polygons. May be a list of coordinates in the GeoJSON
âMultiPolygonâ format, a list of ee.Geometry describing a Polygon,
or a list of number defining a single polygon boundary.
**Usage:** `coords: A list of polygons. May be a list of coordinates in the GeoJSON`

===

## ee :: static Point ( coords=<object object> , proj=<object object> , *args , **kwargs ) Â¶
Constructs an ee.Geometry describing a point. Args: coords: A list of two [x,y] coordinates in the given projection.
proj: The projection of this geometry, or EPSG:4326 if unspecified. * args: For convenience, varargs may be used when all arguments are numbers. This allows creating EPSG:4326 points, e.g.
ee.Geometry.Point(lng, lat). ** kwargs: Keyword args that accept âlonâ and âlatâ for backward- compatibility. Returns: An ee.Geometry describing a point.
**Usage:** `static Point ( coords=<object object> , proj=<object object> , *args , **kwargs ) Â¶`

===

## ee :: ** kwargs: Keyword args that accept âlonâ and âlatâ for backward-
compatibility.
**Usage:** `** kwargs: Keyword args that accept âlonâ and âlatâ for backward-`

===

## ee :: static Polygon ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , e
Constructs an ee.Geometry describing a polygon. Args: coords: A list of rings defining the boundaries of the polygon. May be a list of coordinates in the GeoJSON âPolygonâ format, a list of
ee.Geometry describing a LinearRing, or a list of number defining a
single polygon boundary. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. maxError: Max error when input geometry must be reprojected to an explicitly requested result projection or geodesic state. evenOdd: If true, polygon interiors will be determined by the even/odd rule, where a point is inside if it crosses an odd number of edges to
reach a point at infinity. Otherwise polygons use the left-insi
**Usage:** `static Polygon ( coords=<object object> , proj=<object object> , geodesic=<object object> , maxError=<object object> , evenOdd=<object object> , *args ) Â¶`

===

## ee :: coords: A list of rings defining the boundaries of the polygon. May be a
list of coordinates in the GeoJSON âPolygonâ format, a list of
ee.Geometry describing a LinearRing, or a list of number defining a
single polygon boundary.
**Usage:** `coords: A list of rings defining the boundaries of the polygon. May be a`

===

## ee :: static Rectangle ( coords=<object object> , proj=<object object> , geodesic=<object object> , evenOdd=<object object> , 
Constructs an ee.Geometry describing a rectangular polygon. Args: coords: The minimum and maximum corners of the rectangle, as a list of two points each in the format of GeoJSON âPointâ coordinates, or a
list of two ee.Geometry describing a point, or a list of four
numbers in the order xMin, yMin, xMax, yMax. proj: The projection of this geometry. If unspecified, the default is the projection of the input ee.Geometry, or EPSG:4326 if there are no
ee.Geometry inputs. geodesic: If false, edges are straight in the projection. If true, edges are curved to follow the shortest path on the surface of the Earth.
The default is the geodesic state of the inputs, or true if the
inputs are numbers. evenOdd: If true, polygon interiors will be determined by the even/odd rule, where a point is inside if it crosses an odd number of edges to
reach a point at infinity. Otherwise polygons use the left-inside
rule, where interiors are on the left side of the shellâs edges when
walking the vertices i
**Usage:** `static Rectangle ( coords=<object object> , proj=<object object> , geodesic=<object object> , evenOdd=<object object> , *args , **kwargs ) Â¶`

===

## ee :: coords: The minimum and maximum corners of the rectangle, as a list of
two points each in the format of GeoJSON âPointâ coordinates, or a
list of two ee.Geometry describing a point, or a list of four
numbers in the order xMin, yMin, xMax, yMax.
**Usage:** `coords: The minimum and maximum corners of the rectangle, as a list of`

===

## ee :: serialize ( ) Â¶
Returns the serialized representation of this object.
**Usage:** `serialize ( ) Â¶`

===

## ee :: toGeoJSON ( ) Â¶
Returns a GeoJSON representation of the geometry.
**Usage:** `toGeoJSON ( ) Â¶`

===

## ee :: toGeoJSONString ( ) Â¶
Returns a GeoJSON string representation of the geometry.
**Usage:** `toGeoJSONString ( ) Â¶`

===

## ee :: class ee.image. Image ( args=None , version=None ) Â¶
Bases: ee.element.Element An object to represent an Earth Engine image. static cat ( *args ) Â¶ Concatenate the given images together into a single image. clip ( clip_geometry ) Â¶ Clips an image to a Geometry or Feature. The output bands correspond exactly the input bands, except data not
covered by the geometry is masked. The output image retains the
metadata of the input image. Use clipToCollection to clip an image to a FeatureCollection. Args: clip_geometry: The Geometry or Feature to clip to. Returns: The clipped image. static combine_ ( images , names=None ) Â¶ Combine all the bands from the given images into a single image. Args: images: The images to be combined.
names: An array of names for the output bands. Returns: The combined image. expression ( expression , opt_map=None ) Â¶ Evaluates an arithmetic expression on an image or images. The bands of the primary input image are available using the built-in
function b(), as b(0) or b(âband_nameâ). Variables in the expression
**Usage:** `class ee.image. Image ( args=None , version=None ) Â¶`

===

## ee :: static cat ( *args ) Â¶
Concatenate the given images together into a single image.
**Usage:** `static cat ( *args ) Â¶`

===

## ee :: clip ( clip_geometry ) Â¶
Clips an image to a Geometry or Feature. The output bands correspond exactly the input bands, except data not
covered by the geometry is masked. The output image retains the
metadata of the input image. Use clipToCollection to clip an image to a FeatureCollection. Args: clip_geometry: The Geometry or Feature to clip to. Returns: The clipped image.
**Usage:** `clip ( clip_geometry ) Â¶`

===

## ee :: static combine_ ( images , names=None ) Â¶
Combine all the bands from the given images into a single image. Args: images: The images to be combined.
names: An array of names for the output bands. Returns: The combined image.
**Usage:** `static combine_ ( images , names=None ) Â¶`

===

## ee :: expression ( expression , opt_map=None ) Â¶
Evaluates an arithmetic expression on an image or images. The bands of the primary input image are available using the built-in
function b(), as b(0) or b(âband_nameâ). Variables in the expression are interpreted as additional image parameters
which must be supplied in opt_map. The bands of each such image can be
accessed like image.band_name or image[0]. Both b() and image[] allow multiple arguments, to specify multiple bands,
such as b(1, ânameâ, 3).  Calling b() with no arguments, or using a variable
by itself, returns all bands of the image. Args: expression: The expression to evaluate.
opt_map: An optional map of input images available by name. Returns: The image computed by the provided expression.
**Usage:** `expression ( expression , opt_map=None ) Â¶`

===

## ee :: getDownloadURL ( params=None ) Â¶
Get a download URL for this image. Args: params: An object containing visualization options with the following possible values: name -  a base name to use when constructing filenames.
bands -  a description of the bands to download. Must be an array of dictionaries, each with the following keys: id -  the name of the band, a string, required.
crs -  an optional CRS string defining the band projection.
crs_transform -  an optional array of 6 numbers specifying an affine transform from the specified CRS, in the order: xScale, yShearing,
xShearing, yScale, xTranslation and yTranslation. dimensions -  an optional array of two integers defining the width and height to which the band is cropped. scale -  an optional number, specifying the scale in meters of the band; ignored if crs and crs_transform is specified. crs -  a default CRS string to use for any bands that do not explicitly specify one. crs_transform -  a default affine transform to use for any bands that do not specify one, of the
**Usage:** `getDownloadURL ( params=None ) Â¶`

===

## ee :: dimensions -  an optional array of two integers defining the width and
height to which the band is cropped.
**Usage:** `dimensions -  an optional array of two integers defining the width and`

===

## ee :: crs -  a default CRS string to use for any bands that do not explicitly
specify one.
**Usage:** `crs -  a default CRS string to use for any bands that do not explicitly`

===

## ee :: getDownloadUrl ( params=None ) Â¶
Get a download URL for this image. Args: params: An object containing visualization options with the following possible values: name -  a base name to use when constructing filenames.
bands -  a description of the bands to download. Must be an array of dictionaries, each with the following keys: id -  the name of the band, a string, required.
crs -  an optional CRS string defining the band projection.
crs_transform -  an optional array of 6 numbers specifying an affine transform from the specified CRS, in the order: xScale, yShearing,
xShearing, yScale, xTranslation and yTranslation. dimensions -  an optional array of two integers defining the width and height to which the band is cropped. scale -  an optional number, specifying the scale in meters of the band; ignored if crs and crs_transform is specified. crs -  a default CRS string to use for any bands that do not explicitly specify one. crs_transform -  a default affine transform to use for any bands that do not specify one, of the
**Usage:** `getDownloadUrl ( params=None ) Â¶`

===

## ee :: getThumbId ( params ) Â¶
Applies transformations and returns the thumbId. Args: params: Parameters identical to getMapId, plus, optionally: dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ or âjpgâ. Returns: A thumbId for the created thumbnail. Raises: EEException: If the region parameter is not an array or GeoJSON object.
**Usage:** `getThumbId ( params ) Â¶`

===

## ee :: params: Parameters identical to getMapId, plus, optionally:
dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ or âjpgâ.
**Usage:** `params: Parameters identical to getMapId, plus, optionally:`

===

## ee :: dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max
dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling.
**Usage:** `dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max`

===

## ee :: getThumbURL ( params=None ) Â¶
Get a thumbnail URL for this image. Args: params: Parameters identical to getMapId, plus, optionally: dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ or âjpgâ. Returns: A URL to download a thumbnail the specified image. Raises: EEException: If the region parameter is not an array or GeoJSON object.
**Usage:** `getThumbURL ( params=None ) Â¶`

===

## ee :: getThumbUrl ( params=None ) Â¶
Get a thumbnail URL for this image. Args: params: Parameters identical to getMapId, plus, optionally: dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max dimensions of the thumbnail to render, in pixels. If only one number
is passed, it is used as the maximum, and the other dimension is
computed by proportional scaling. region - (E,S,W,N or GeoJSON) Geospatial region of the image to render. By default, the whole image. format - (string) Either âpngâ or âjpgâ. Returns: A URL to download a thumbnail the specified image. Raises: EEException: If the region parameter is not an array or GeoJSON object. DEPRECATED: Use getThumbURL().
**Usage:** `getThumbUrl ( params=None ) Â¶`

===

## ee :: prepare_for_export ( params ) Â¶
Applies all relevant export parameters to an image. Args: params: the export request parameters. Returns: A tuple containing:
- an image that has had many of the request parameters applied to it any remaining parameters.
**Usage:** `prepare_for_export ( params ) Â¶`

===

## ee :: rename ( names , *args ) Â¶
Rename the bands of an image. Can be called with either a list of strings or any number of strings. Args: names: An array of strings specifying the new names for the bands.  Must exactly match the number of bands in the image. * args: Band names as varargs. Returns: An image with the renamed bands.
**Usage:** `rename ( names , *args ) Â¶`

===

## ee :: names: An array of strings specifying the new names for the
bands.  Must exactly match the number of bands in the image.
**Usage:** `names: An array of strings specifying the new names for the`

===

## ee :: static rgb ( r , g , b ) Â¶
Create a 3-band image. This creates a 3-band image specifically for visualization using
the first band in each image. Args: r: The red image.
g: The green image.
b: The blue image. Returns: The combined image.
**Usage:** `static rgb ( r , g , b ) Â¶`

===

## ee :: select ( opt_selectors=None , opt_names=None , *args ) Â¶
Selects bands from an image. Can be called in one of two ways: Passed any number of non-list arguments. All of these will be
interpreted as band selectors. These can be band names, regexes, or
numeric indices. E.g.
selected = image.select(âaâ, âbâ, 3, âdâ); Passed two lists. The first will be used as band selectors and the
second as new names for the selected bands. The number of new names
must match the number of selected bands. E.g.
selected = image.select([âaâ, 4], [ânewAâ, ânewBâ]); Args: opt_selectors: An array of names, regexes or numeric indices specifying the bands to select. opt_names: An array of strings specifying the new names for the selected bands. * args: Selector elements as varargs. Returns: An image with the selected bands.
**Usage:** `select ( opt_selectors=None , opt_names=None , *args ) Â¶`

===

## ee :: Can be called in one of two ways:
Passed any number of non-list arguments. All of these will be
interpreted as band selectors. These can be band names, regexes, or
numeric indices. E.g.
selected = image.select(âaâ, âbâ, 3, âdâ); Passed two lists. The first will be used as band selectors and the
second as new names for the selected bands. The number of new names
must match the number of selected bands. E.g.
selected = image.select([âaâ, 4], [ânewAâ, ânewBâ]);
**Usage:** `Can be called in one of two ways:`

===

## ee :: opt_selectors: An array of names, regexes or numeric indices specifying
the bands to select.
**Usage:** `opt_selectors: An array of names, regexes or numeric indices specifying`

===

## ee :: class ee.imagecollection. ImageCollection ( args ) Â¶
Bases: ee.collection.Collection Representation for an Earth Engine ImageCollection. static elementType ( ) Â¶ Returns the type of the collectionâs elements. first ( ) Â¶ Returns the first entry from a given collection. Returns: The first entry from the collection. getFilmstripThumbURL ( params=None ) Â¶ Get the URL for a âfilmstripâ thumbnail of the given collection. Args: params: Parameters identical to getMapId, plus, optionally:
dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max dimensions of
the thumbnail to render, in pixels. If only one number is passed, it is
used as the maximum, and the other dimension is computed by proportional
scaling. crs - a CRS string specifying the projection of the output.
crs_transform - the affine transform to use for the output pixel grid.
scale - a scale to determine the output pixel grid; ignored if both crs and crs_transform are specified. region - (E,S,W,N or GeoJSON) Geospatial region of the result. By default, the whole
**Usage:** `class ee.imagecollection. ImageCollection ( args ) Â¶`

===

## ee :: first ( ) Â¶
Returns the first entry from a given collection. Returns: The first entry from the collection.
**Usage:** `first ( ) Â¶`

===

## ee :: getFilmstripThumbURL ( params=None ) Â¶
Get the URL for a âfilmstripâ thumbnail of the given collection. Args: params: Parameters identical to getMapId, plus, optionally:
dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max dimensions of
the thumbnail to render, in pixels. If only one number is passed, it is
used as the maximum, and the other dimension is computed by proportional
scaling. crs - a CRS string specifying the projection of the output.
crs_transform - the affine transform to use for the output pixel grid.
scale - a scale to determine the output pixel grid; ignored if both crs and crs_transform are specified. region - (E,S,W,N or GeoJSON) Geospatial region of the result. By default, the whole image. format - (string) The output format (e.g., âpngâ, âjpgâ). Returns: A URL to download a thumbnail of the specified ImageCollection. Raises: EEException: If the region parameter is not an array or GeoJSON object.
**Usage:** `getFilmstripThumbURL ( params=None ) Â¶`

===

## ee :: region - (E,S,W,N or GeoJSON) Geospatial region of the result. By default,
the whole image.
**Usage:** `region - (E,S,W,N or GeoJSON) Geospatial region of the result. By default,`

===

## ee :: select ( selectors , opt_names=None , *args ) Â¶
Select bands from each image in a collection. Args: selectors: An array of names, regexes or numeric indices specifying the bands to select. opt_names: An array of strings specifying the new names for the selected bands.  If supplied, the length must match the number
of bands selected. * args: Selector elements as varargs. Returns: The image collection with selected bands.
**Usage:** `select ( selectors , opt_names=None , *args ) Â¶`

===

## ee :: selectors: An array of names, regexes or numeric indices specifying
the bands to select.
**Usage:** `selectors: An array of names, regexes or numeric indices specifying`

===

## ee :: ee.mapclient. MakeOverlay ( mapid , baseurl='https://earthengine.googleapis.com' ) Â¶
Create an overlay from a mapid.
**Usage:** `ee.mapclient. MakeOverlay ( mapid , baseurl='https://earthengine.googleapis.com' ) Â¶`

===

## ee :: class ee.mapclient. MapClient ( opt_overlay=None , opt_width=1024 , opt_height=768 ) Â¶
Bases: threading.Thread A simple discrete zoom level map viewer. AddTile ( image , key , overlay , layer ) Â¶ Add a tile to the map. This keeps track of the tiles for each overlay in each grid cell.
As new tiles come in, all the tiles in a grid cell are composited together
into a new tile and any old tile for that spot is replaced. Args: image: The image tile to display.
key: A tuple containing the key of the image (level, x, y)
overlay: The overlay this tile belongs to.
layer: The layer number this overlay corresponds to.  Only used for caching purposes. CenterMap ( lon , lat , opt_zoom=None ) Â¶ Center the map at the given lon, lat and zoom level. ClickHandler ( event ) Â¶ Records the anchor location and sets drag handler. CompositeTiles ( key ) Â¶ Composite together all the tiles in this cell into a single image. DragHandler ( event ) Â¶ Updates the map position and anchor position. Flush ( ) Â¶ Empty out all the image fetching queues. GetFrameSize ( ) Â¶ GetMapSize ( ) Â¶ GetViewpo
**Usage:** `class ee.mapclient. MapClient ( opt_overlay=None , opt_width=1024 , opt_height=768 ) Â¶`

===

## ee :: AddTile ( image , key , overlay , layer ) Â¶
Add a tile to the map. This keeps track of the tiles for each overlay in each grid cell.
As new tiles come in, all the tiles in a grid cell are composited together
into a new tile and any old tile for that spot is replaced. Args: image: The image tile to display.
key: A tuple containing the key of the image (level, x, y)
overlay: The overlay this tile belongs to.
layer: The layer number this overlay corresponds to.  Only used for caching purposes.
**Usage:** `AddTile ( image , key , overlay , layer ) Â¶`

===

## ee :: CenterMap ( lon , lat , opt_zoom=None ) Â¶
Center the map at the given lon, lat and zoom level.
**Usage:** `CenterMap ( lon , lat , opt_zoom=None ) Â¶`

===

## ee :: ClickHandler ( event ) Â¶
Records the anchor location and sets drag handler.
**Usage:** `ClickHandler ( event ) Â¶`

===

## ee :: CompositeTiles ( key ) Â¶
Composite together all the tiles in this cell into a single image.
**Usage:** `CompositeTiles ( key ) Â¶`

===

## ee :: DragHandler ( event ) Â¶
Updates the map position and anchor position.
**Usage:** `DragHandler ( event ) Â¶`

===

## ee :: Flush ( ) Â¶
Empty out all the image fetching queues.
**Usage:** `Flush ( ) Â¶`

===

## ee :: GetFrameSize ( ) Â¶
**Usage:** `GetFrameSize ( ) Â¶`

===

## ee :: GetMapSize ( ) Â¶
**Usage:** `GetMapSize ( ) Â¶`

===

## ee :: GetViewport ( ) Â¶
Return the visible portion of the map as [xlo, ylo, xhi, yhi].
**Usage:** `GetViewport ( ) Â¶`

===

## ee :: KeypressHandler ( event ) Â¶
Handle keypress events.
**Usage:** `KeypressHandler ( event ) Â¶`

===

## ee :: LoadTiles ( ) Â¶
Refresh the entire map.
**Usage:** `LoadTiles ( ) Â¶`

===

## ee :: ReleaseHandler ( unused_event ) Â¶
Unbind drag handler and redraw.
**Usage:** `ReleaseHandler ( unused_event ) Â¶`

===

## ee :: ResizeHandler ( event ) Â¶
Handle resize events.
**Usage:** `ResizeHandler ( event ) Â¶`

===

## ee :: Zoom ( event , direction ) Â¶
Zoom the map. Args: event: The event that caused this zoom request.
direction: The direction to zoom.  +1 for higher zoom, -1 for lower.
**Usage:** `Zoom ( event , direction ) Â¶`

===

## ee :: addOverlay ( overlay ) Â¶
Add an overlay to the map.
**Usage:** `addOverlay ( overlay ) Â¶`

===

## ee :: run ( ) Â¶
Set up the user interface.
**Usage:** `run ( ) Â¶`

===

## ee :: class ee.mapclient. MapOverlay ( url , tile_fetcher=None ) Â¶
Bases: object A class representing a map overlay. CalcTiles ( level , bbox ) Â¶ Calculate which tiles to load based on the visible viewport. Args: level: The level at which to calculate the required tiles.
bbox: The viewport coordinates as a tuple (xlo, ylo, xhi, yhi]) Returns: The list of tile keys to fill the given viewport. Flush ( ) Â¶ Empty the tile queue. GetCachedTile ( key ) Â¶ Returns the specified tile if itâs in the cache. Interpolate ( key , callback ) Â¶ Upsample a lower res tile if one is available. Args: key: The tile key to upsample.
callback: The callback to call when the tile is ready. MAX_CACHE = 1000 Â¶ PutCacheTile ( key , image ) Â¶ Insert a new tile in the cache and eject old ones if itâs too big. TILE_HEIGHT = 256 Â¶ TILE_WIDTH = 256 Â¶ class TileFetcher ( overlay ) Â¶ Bases: threading.Thread A threaded URL fetcher. run ( ) Â¶ Pull URLs off the ovelayâs queue and call the callback when done. getTile ( key , callback ) Â¶ Get the requested tile. If the requ
**Usage:** `class ee.mapclient. MapOverlay ( url , tile_fetcher=None ) Â¶`

===

## ee :: CalcTiles ( level , bbox ) Â¶
Calculate which tiles to load based on the visible viewport. Args: level: The level at which to calculate the required tiles.
bbox: The viewport coordinates as a tuple (xlo, ylo, xhi, yhi]) Returns: The list of tile keys to fill the given viewport.
**Usage:** `CalcTiles ( level , bbox ) Â¶`

===

## ee :: GetCachedTile ( key ) Â¶
Returns the specified tile if itâs in the cache.
**Usage:** `GetCachedTile ( key ) Â¶`

===

## ee :: Interpolate ( key , callback ) Â¶
Upsample a lower res tile if one is available. Args: key: The tile key to upsample.
callback: The callback to call when the tile is ready.
**Usage:** `Interpolate ( key , callback ) Â¶`

===

## ee :: MAX_CACHE = 1000 Â¶
**Usage:** `MAX_CACHE = 1000 Â¶`

===

## ee :: PutCacheTile ( key , image ) Â¶
Insert a new tile in the cache and eject old ones if itâs too big.
**Usage:** `PutCacheTile ( key , image ) Â¶`

===

## ee :: TILE_HEIGHT = 256 Â¶
**Usage:** `TILE_HEIGHT = 256 Â¶`

===

## ee :: TILE_WIDTH = 256 Â¶
**Usage:** `TILE_WIDTH = 256 Â¶`

===

## ee :: class TileFetcher ( overlay ) Â¶
Bases: threading.Thread A threaded URL fetcher. run ( ) Â¶ Pull URLs off the ovelayâs queue and call the callback when done.
**Usage:** `class TileFetcher ( overlay ) Â¶`

===

## ee :: getTile ( key , callback ) Â¶
Get the requested tile. If the requested tile is already cached, itâs returned (sent to the
callback) directly.  If itâs not cached, a check is made to see if
a lower-res version is cached, and if so thatâs interpolated up, before
a request for the actual tile is made. Args: key: The key of the tile to fetch.
callback: The callback to call when the tile is available.  The callback may be called more than once if a low-res version is available.
**Usage:** `getTile ( key , callback ) Â¶`

===

## ee :: ee.mapclient. addToMap ( eeobject , vis_params=None , *unused_args ) Â¶
Adds a layer to the default map instance. Args: eeobject: the object to add to the map.
vis_params: a dictionary of visualization parameters.  See ee.data.getMapId(). * unused_args: unused arguments, left for compatibility with the JS API. This call exists to be an equivalent to the playground addToMap() call.
It uses a global MapInstance to hang on to âthe mapâ.  If the MapInstance
isnât initialized, this creates a new one.
**Usage:** `ee.mapclient. addToMap ( eeobject , vis_params=None , *unused_args ) Â¶`

===

## ee :: ee.mapclient. centerMap ( lng , lat , zoom ) Â¶
Center the default map instance at the given lat, lon and zoom values.
**Usage:** `ee.mapclient. centerMap ( lng , lat , zoom ) Â¶`

===

## ee :: ee.oauth. get_authorization_url ( ) Â¶
Returns a URL to generate an auth code.
**Usage:** `ee.oauth. get_authorization_url ( ) Â¶`

===

## ee :: ee.oauth. get_credentials_path ( ) Â¶
**Usage:** `ee.oauth. get_credentials_path ( ) Â¶`

===

## ee :: ee.oauth. request_token ( auth_code ) Â¶
Uses authorization code to request tokens.
**Usage:** `ee.oauth. request_token ( auth_code ) Â¶`

===

## ee :: ee.oauth. write_token ( refresh_token ) Â¶
Attempts to write the passed token to the given user directory.
**Usage:** `ee.oauth. write_token ( refresh_token ) Â¶`

===

## ee :: ee.serializer. DatetimeToMicroseconds ( date ) Â¶
Convert a datetime to a timestamp, microseconds since the epoch.
**Usage:** `ee.serializer. DatetimeToMicroseconds ( date ) Â¶`

===

## ee :: class ee.serializer. Serializer ( is_compound=True , for_cloud_api=False ) Â¶
Bases: object A serializer for EE object trees.
**Usage:** `class ee.serializer. Serializer ( is_compound=True , for_cloud_api=False ) Â¶`

===

## ee :: ee.serializer. encode ( obj , is_compound=True , for_cloud_api=False ) Â¶
Serialize an object to a JSON-compatible structure for API calls. Args: obj: The object to serialize.
is_compound: Whether the encoding should factor out shared subtrees.
for_cloud_api: Whether the encoding should be done for the Cloud API or the legacy API. Returns: A JSON-compatible structure representing the input.
**Usage:** `ee.serializer. encode ( obj , is_compound=True , for_cloud_api=False ) Â¶`

===

## ee :: ee.serializer. toJSON ( obj , opt_pretty=False , for_cloud_api=False ) Â¶
Serialize an object to a JSON string appropriate for API calls. Args: obj: The object to serialize.
opt_pretty: True to pretty-print the object.
for_cloud_api: Whether the encoding should be done for the Cloud API or the legacy API. Returns: A JSON string representing the input.
**Usage:** `ee.serializer. toJSON ( obj , opt_pretty=False , for_cloud_api=False ) Â¶`

===

## ee :: ee.serializer. toReadableJSON ( obj ) Â¶
Convert an object to readable JSON.
**Usage:** `ee.serializer. toReadableJSON ( obj ) Â¶`

===

## ee :: class ee.terrain. Terrain Â¶
Bases: object An namespace for Terrain Algorithms. classmethod initialize ( ) Â¶ Imports API functions to this class. classmethod reset ( ) Â¶ Removes imported API functions from this class.
**Usage:** `class ee.terrain. Terrain Â¶`

===

## ee :: ee. Initialize ( credentials='persistent' , opt_url=None , use_cloud_api=False , cloud_api_key=None , http_transport=Non
Initialize the EE library. If this hasnât been called by the time any object constructor is used,
it will be called then.  If this is called a second time with a different
URL, this doesnât do an un-initialization of e.g.: the previously loaded
Algorithms, but will overwrite them and let point at alternate servers. Args: credentials: OAuth2 credentials.  âpersistentâ (default) means use credentials already stored in the filesystem, or raise an explanatory
exception guiding the user to create those credentials. opt_url: The base url for the EarthEngine REST API to connect to.
use_cloud_api: Whether the Cloud API should be used.
cloud_api_key: An optional API key to use the Cloud API.
http_transport: The http transport method to use when making requests.
project: The project-id or number to use when making api calls.
**Usage:** `ee. Initialize ( credentials='persistent' , opt_url=None , use_cloud_api=False , cloud_api_key=None , http_transport=None , project=None ) Â¶`

===

## ee :: credentials: OAuth2 credentials.  âpersistentâ (default) means use
credentials already stored in the filesystem, or raise an explanatory
exception guiding the user to create those credentials.
**Usage:** `credentials: OAuth2 credentials.  âpersistentâ (default) means use`

===

## ee :: ee. Reset ( ) Â¶
Reset the library. Useful for re-initializing to a different server.
**Usage:** `ee. Reset ( ) Â¶`

===

## ee.Image.And
Returns 1 if both values are non-zero; otherwise 0.

Returns 1 if and only if both values are non-zero for each matched pair of
bands in image1 and image2.

If either image1 or image2 has only 1 band, then it is used against all the
bands in the other image. If the images have the same number of bands, but
not the same names, they're used pairwise in the natural order. The output
bands are named for the longer of the two inputs, or if they're equal in
length, in image1's order. The type of the output pixels is boolean.

Args:
  image2: The image from which the right operand bands are taken.

Returns:
  An ee.Image.
**Usage:** `ee.Image.And(self, image2: '_arg_types.Image') -> 'Image'`

===

## ee.Image.Not
Returns 0 if the input is non-zero, and 1 otherwise.
**Usage:** `ee.Image.Not(self) -> 'Image'`

===

## ee.Image.Or
Returns 1 if the pixel in either image is non-zero, and 0 otherwise.

Returns 1 if and only if either input value is non-zero for each matched
pair of bands in image1 and image2. If either image1 or image2 has only 1
band, then it is used against all the bands in the other image. If the
images have the same number of bands, but not the same names, they're used
pairwise in the natural order. The output bands are named for the longer of
the two inputs, or if they're equal in length, in image1's order. The type
of the output pixels is boolean.

Args:
  image2: The image from which the right operand bands are taken.
**Usage:** `ee.Image.Or(self, image2: '_arg_types.Image') -> 'Image'`

===

## ee.Image.combine_
Combine all the bands from the given images into a single image.

Args:
  images: The images to be combined.
  names: An array of names for the output bands.

Returns:
  The combined image.
**Usage:** `ee.Image.combine_(images: 'Any', names: 'Any | None' = None) -> 'Image'`

===

## ee.Image.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Image.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Image.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Image.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Image.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Image.freeze(obj: 'Any') -> 'Any'`

===

## ee.Image.getDownloadUrl
Get a download URL for an image chunk.

Generates a download URL for small chunks of image data in GeoTIFF or NumPy
format. Maximum request size is 32 MB, maximum grid dimension is 10000.

Use getThumbURL for RGB visualization formats PNG and JPG.

Args:
  params: An object containing download options with the following
      possible values:
    - name: a base name to use when constructing filenames. Only applicable
        when format is "ZIPPED_GEO_TIFF" (default),
        "ZIPPED_GEO_TIFF_PER_BAND" or filePerBand is true. Defaults to the
        image id (or "download" for computed images) when format is
        "ZIPPED_GEO_TIFF", "ZIPPED_GEO_TIFF_PER_BAND", or filePerBand is
        true, otherwise a random character string is generated. Band names
        are appended when filePerBand is true.
    - bands: a description of the bands to download. Must be an array of
        band names or an array of dictionaries, each with the
        following keys:
      + id: the name of the ba
**Usage:** `ee.Image.getDownloadUrl(self, params: 'dict[str, Any] | None' = None) -> 'str'`

===

## ee.Image.getThumbUrl
Get a thumbnail URL for this image.

Args:
  params: Parameters identical to getMapId, plus, optionally:
      dimensions - (a number or pair of numbers in format WIDTHxHEIGHT) Max
        dimensions of the thumbnail to render, in pixels. If only one number
        is passed, it is used as the maximum, and the other dimension is
        computed by proportional scaling.
      region - (ee.Geometry, GeoJSON, list of numbers, list of points)
        Geospatial region of the image to render. By default, the whole
        image. If given a list of min lon, min lat, max lon, max lat,
        a planar rectangle is created. If given a list of points a
        planar polygon is created.
      format - (string) Either 'png' or 'jpg'.

Returns:
  A URL to download a thumbnail the specified image.

Raises:
  EEException: If the region parameter is not an array or GeoJSON object.

DEPRECATED: Use getThumbURL().
**Usage:** `ee.Image.getThumbUrl(self, params: 'dict[str, Any] | None' = None) -> 'str'`

===

## ee.Image.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Image.isVariable(self) -> 'bool'`

===

## ee.Image.is_func_returning_same
**Usage:** `ee.Image.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Image.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Image.name() -> 'str'`

===

## ee.Image.prepare_for_export
Applies all relevant export parameters to an image.

Args:
  params: the export request parameters.

Returns:
  A tuple containing:
  - an image that has had many of the request parameters applied
    to it
  - any remaining parameters.
**Usage:** `ee.Image.prepare_for_export(self, params: 'dict[str, Any]') -> 'Any'`

===

## ee.ImageCollection.And
Returns an image with the logical AND of the collection.

Reduces an image collection by setting each pixel to 1 if and only if all
the non-masked values at that pixel are non-zero across the stack of all
matching bands. Bands are matched by name.
**Usage:** `ee.ImageCollection.And(self) -> 'image.Image'`

===

## ee.ImageCollection.Or
Returns an image with the logical OR of the collection.

Reduces an image collection by setting each pixel to 1 if and only if any of
the non-masked values at that pixel are non-zero across the stack of all
matching bands. Bands are matched by name.
**Usage:** `ee.ImageCollection.Or(self) -> 'image.Image'`

===

## ee.ImageCollection.elementType
Returns the type of the collection's elements.
**Usage:** `ee.ImageCollection.elementType()`

===

## ee.ImageCollection.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.ImageCollection.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.ImageCollection.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.ImageCollection.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.ImageCollection.filterMetadata
Shortcut to add a metadata filter to a collection.

This is equivalent to self.filter(Filter().metadata(...)).

Args:
  name: Name of a property to filter.
  operator: Name of a comparison operator as defined
      by FilterCollection. Possible values are: "equals", "less_than",
      "greater_than", "not_equals", "not_less_than", "not_greater_than",
      "starts_with", "ends_with", "not_starts_with", "not_ends_with",
      "contains", "not_contains".
  value: The value to compare against.

Returns:
  The filtered collection.
**Usage:** `ee.ImageCollection.filterMetadata(self: 'T', name: 'str', operator: 'str', value: 'int | str') -> 'T'`

===

## ee.ImageCollection.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.ImageCollection.freeze(obj: 'Any') -> 'Any'`

===

## ee.ImageCollection.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.ImageCollection.isVariable(self) -> 'bool'`

===

## ee.ImageCollection.is_func_returning_same
**Usage:** `ee.ImageCollection.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.ImageCollection.loadTable
Returns a Collection of features from a specified table.

Args:
  tableId: The asset ID of the table to load.
  geometryColumn: The name of the column to use as the main feature
    geometry. Not used if tableId is an asset ID.
  version: The version of the asset. -1 signifies the latest version.
    Ignored unless tableId is an asset ID.
**Usage:** `ee.ImageCollection.loadTable(tableId: '_arg_types.String', geometryColumn: '_arg_types.String | None' = None, version: '_arg_types.Integer | None' = None) -> 'featurecollection.FeatureCollection'`

===

## ee.ImageCollection.name
Returns the name of the object, used in __str__().
**Usage:** `ee.ImageCollection.name() -> 'str'`

===

## ee.ImageCollection.prepare_for_export
Applies all relevant export parameters to an ImageCollection.

Args:
  params: The export request parameters.

Returns:
  A tuple containing:
  - an ImageCollection that has had many of the request parameters applied
    to it
  - any remaining parameters.
**Usage:** `ee.ImageCollection.prepare_for_export(self, params: 'dict[str, Any]') -> 'tuple[ImageCollection, dict[str, Any]]'`

===

## ee.FeatureCollection.elementType
Returns the type of the collection's elements.
**Usage:** `ee.FeatureCollection.elementType() -> 'type[feature.Feature]'`

===

## ee.FeatureCollection.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.FeatureCollection.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.FeatureCollection.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.FeatureCollection.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.FeatureCollection.filterMetadata
Shortcut to add a metadata filter to a collection.

This is equivalent to self.filter(Filter().metadata(...)).

Args:
  name: Name of a property to filter.
  operator: Name of a comparison operator as defined
      by FilterCollection. Possible values are: "equals", "less_than",
      "greater_than", "not_equals", "not_less_than", "not_greater_than",
      "starts_with", "ends_with", "not_starts_with", "not_ends_with",
      "contains", "not_contains".
  value: The value to compare against.

Returns:
  The filtered collection.
**Usage:** `ee.FeatureCollection.filterMetadata(self: 'T', name: 'str', operator: 'str', value: 'int | str') -> 'T'`

===

## ee.FeatureCollection.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.FeatureCollection.freeze(obj: 'Any') -> 'Any'`

===

## ee.FeatureCollection.getDownloadUrl
Gets a download URL.

When the URL is accessed, the FeatureCollection is downloaded in one of
several formats.

Args:
  filetype: The format of download, one of: "csv", "json", "geojson", "kml",
      "kmz" ("json" outputs GeoJSON). If unspecified, defaults to "csv".
  selectors: Feature property names used to select the attributes to be
      downloaded. If unspecified, all properties are included.
  filename: Name of the file to be downloaded; extension is appended by
      default. If unspecified, defaults to "table".

Returns:
  A URL to download this FeatureCollection.

DEPRECATED: Use getDownloadURL().
**Usage:** `ee.FeatureCollection.getDownloadUrl(self, filetype: 'str | None' = None, selectors: 'Any | None' = None, filename: 'str | None' = None) -> 'str'`

===

## ee.FeatureCollection.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.FeatureCollection.isVariable(self) -> 'bool'`

===

## ee.FeatureCollection.is_func_returning_same
**Usage:** `ee.FeatureCollection.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.FeatureCollection.loadTable
Returns a Collection of features from a specified table.

Args:
  tableId: The asset ID of the table to load.
  geometryColumn: The name of the column to use as the main feature
    geometry. Not used if tableId is an asset ID.
  version: The version of the asset. -1 signifies the latest version.
    Ignored unless tableId is an asset ID.
**Usage:** `ee.FeatureCollection.loadTable(tableId: '_arg_types.String', geometryColumn: '_arg_types.String | None' = None, version: '_arg_types.Integer | None' = None) -> 'featurecollection.FeatureCollection'`

===

## ee.FeatureCollection.name
Returns the name of the object, used in __str__().
**Usage:** `ee.FeatureCollection.name() -> 'str'`

===

## ee.Feature.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Feature.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Feature.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Feature.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Feature.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Feature.freeze(obj: 'Any') -> 'Any'`

===

## ee.Feature.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Feature.isVariable(self) -> 'bool'`

===

## ee.Feature.is_func_returning_same
**Usage:** `ee.Feature.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Feature.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Feature.name() -> 'str'`

===

## ee.Geometry.encode
Returns a GeoJSON-compatible representation of the geometry.
**Usage:** `ee.Geometry.encode(self, encoder: 'Any | None' = None) -> 'dict[str, Any]'`

===

## ee.Geometry.encode_cloud_value
Returns a server-side invocation of the appropriate constructor.
**Usage:** `ee.Geometry.encode_cloud_value(self, encoder: 'Any') -> 'Any'`

===

## ee.Geometry.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Geometry.freeze(obj: 'Any') -> 'Any'`

===

## ee.Geometry.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Geometry.isVariable(self) -> 'bool'`

===

## ee.Geometry.is_func_returning_same
**Usage:** `ee.Geometry.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Geometry.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Geometry.name() -> 'str'`

===

## ee.Filter.And
Combine two or more filters using boolean AND.
**Usage:** `ee.Filter.And(*args) -> 'Filter'`

===

## ee.Filter.Not
Returns the opposite of this filter.

Returns:
  The negated filter, which will match if and only if this filter does not.
**Usage:** `ee.Filter.Not(self) -> 'Filter'`

===

## ee.Filter.Or
Combine two or more filters using boolean OR.
**Usage:** `ee.Filter.Or(*args) -> 'Filter'`

===

## ee.Filter.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Filter.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Filter.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Filter.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Filter.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Filter.freeze(obj: 'Any') -> 'Any'`

===

## ee.Filter.geometry
Filter on intersection with geometry.

Items in the collection with a footprint that fails to intersect
the given geometry will be excluded.

Args:
  geometry: The geometry to filter to either as a GeoJSON geometry, or a
    FeatureCollection, from which a geometry will be extracted.
  errorMargin: An optional error margin. If a number, interpreted as sphere
    surface meters.

Returns:
  The constructed filter.
**Usage:** `ee.Filter.geometry(geometry: '_arg_types.Geometry', errorMargin: '_arg_types.ErrorMargin | None' = None)`

===

## ee.Filter.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Filter.isVariable(self) -> 'bool'`

===

## ee.Filter.is_func_returning_same
**Usage:** `ee.Filter.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Filter.metadata_
Filter on metadata. This is deprecated.

Args:
  name: The property name to filter on.
  operator: The type of comparison. One of:
      "equals", "less_than", "greater_than", "contains", "begins_with",
      "ends_with", or any of these prefixed with "not_".
  value: The value to compare against.

Returns:
  The new filter.

Deprecated. Use ee.Filter.eq(), ee.Filter.gte(), etc.'
**Usage:** `ee.Filter.metadata_(name: 'str', operator: 'str', value: '_arg_types.Any') -> 'Filter'`

===

## ee.Filter.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Filter.name() -> 'str'`

===

## ee.Filter.predicateCount
Return the number of predicates that have been added to this filter.

Returns:
  The number of predicates that have been added to this filter.
  This does not count nested predicates.
**Usage:** `ee.Filter.predicateCount(self) -> 'int'`

===

## ee.Reducer.And
Returns a Reducer that returns 1 if all inputs are not 0.

Otherwise, it returns 0.

Where applicable, the output name is "all".

DEPRECATED: Use Reducer.allNonZero().
**Usage:** `ee.Reducer.And() -> 'Reducer'`

===

## ee.Reducer.Or
Returns a Reducer that returns 1 if any inputs are not 0, 0 otherwise.

Where applicable, the output name is "any".

DEPRECATED: Use Reducer.anyNonZero().
**Usage:** `ee.Reducer.Or() -> 'Reducer'`

===

## ee.Reducer.aside
Calls a function passing this object as the first argument.

Returns the object itself for chaining. Convenient when debugging. For
example:

c = (ee.ImageCollection('foo').aside(logging.info)
         .filterDate('2001-01-01', '2002-01-01').aside(logging.info)
         .filterBounds(geom).aside(logging.info)
         .aside(addToMap, {'min': 0, 'max': 142})
         .select('a', 'b'))

Args:
  func: The function to call.
  *var_args: Any extra arguments to pass to the function.

Returns:
  The same object, for chaining.
**Usage:** `ee.Reducer.aside(self, func: 'Any', *var_args) -> 'ComputedObject'`

===

## ee.Reducer.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Reducer.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Reducer.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Reducer.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Reducer.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Reducer.freeze(obj: 'Any') -> 'Any'`

===

## ee.Reducer.getInfo
Fetch and return information about this object.

Returns:
  The object can evaluate to anything.
**Usage:** `ee.Reducer.getInfo(self) -> 'Any | None'`

===

## ee.Reducer.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Reducer.isVariable(self) -> 'bool'`

===

## ee.Reducer.is_func_returning_same
**Usage:** `ee.Reducer.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Reducer.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Reducer.name() -> 'str'`

===

## ee.Reducer.serialize
Serialize this object into a JSON string.

Args:
  pretty: A flag indicating whether to pretty-print the JSON.
  for_cloud_api: Whether the encoding should be done for the Cloud API or
    the legacy API.

Returns:
  The serialized representation of this object.
**Usage:** `ee.Reducer.serialize(self, pretty: 'bool' = False, for_cloud_api: 'bool' = True) -> 'str'`

===

## ee.Number.And
Returns 1 if and only if both values are non-zero.

Args:
  right: The value to and with the current ee.Number.

Returns:
  An ee.Number.
**Usage:** `ee.Number.And(self, right: '_arg_types.Number') -> 'Number'`

===

## ee.Number.Not
Returns 0 if the input is non-zero, and 1 otherwise.
**Usage:** `ee.Number.Not(self) -> 'Number'`

===

## ee.Number.Or
Returns 1 if and only if either input value is non-zero.

Args:
  right: The value to or with.
**Usage:** `ee.Number.Or(self, right: '_arg_types.Number') -> 'Number'`

===

## ee.Number.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Number.encode(self, encoder: 'Any' = None) -> 'Any'`

===

## ee.Number.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Number.encode_cloud_value(self, encoder: 'Any' = None) -> 'Any'`

===

## ee.Number.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Number.freeze(obj: 'Any') -> 'Any'`

===

## ee.Number.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Number.isVariable(self) -> 'bool'`

===

## ee.Number.is_func_returning_same
**Usage:** `ee.Number.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Number.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Number.name() -> 'str'`

===

## ee.String.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.String.encode(self, encoder: 'Any' = None) -> 'Any'`

===

## ee.String.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.String.encode_cloud_value(self, encoder: 'Any' = None) -> 'Any'`

===

## ee.String.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.String.freeze(obj: 'Any') -> 'Any'`

===

## ee.String.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.String.isVariable(self) -> 'bool'`

===

## ee.String.is_func_returning_same
**Usage:** `ee.String.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.String.name
Returns the name of the object, used in __str__().
**Usage:** `ee.String.name() -> 'str'`

===

## ee.List.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.List.encode(self, encoder: 'Any | None' = None) -> 'Any'`

===

## ee.List.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.List.encode_cloud_value(self, encoder: 'Any | None' = None) -> 'Any'`

===

## ee.List.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.List.freeze(obj: 'Any') -> 'Any'`

===

## ee.List.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.List.isVariable(self) -> 'bool'`

===

## ee.List.is_func_returning_same
**Usage:** `ee.List.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.List.name
Returns the name of the object, used in __str__().
**Usage:** `ee.List.name() -> 'str'`

===

## ee.Dictionary.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Dictionary.encode(self, encoder=None)`

===

## ee.Dictionary.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Dictionary.encode_cloud_value(self, encoder=None)`

===

## ee.Dictionary.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Dictionary.freeze(obj: 'Any') -> 'Any'`

===

## ee.Dictionary.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Dictionary.isVariable(self) -> 'bool'`

===

## ee.Dictionary.is_func_returning_same
**Usage:** `ee.Dictionary.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Dictionary.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Dictionary.name() -> 'str'`

===

## ee.Date.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Date.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Date.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Date.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Date.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Date.freeze(obj: 'Any') -> 'Any'`

===

## ee.Date.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Date.isVariable(self) -> 'bool'`

===

## ee.Date.is_func_returning_same
**Usage:** `ee.Date.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Date.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Date.name() -> 'str'`

===

## ee.Array.And
Returns 1 if both values are non-zero.

Args:
  right: The values to add.

Returns:
  An ee.Array.
**Usage:** `ee.Array.And(self, right: '_arg_types.Array') -> 'Array'`

===

## ee.Array.Not
Returns 0 if the input is non-zero, and 1 otherwise.
**Usage:** `ee.Array.Not(self) -> 'Array'`

===

## ee.Array.Or
On an element-wise basis, returns 1 if either input value is non-zero.

Args:
  right: The right-hand value.

Returns:
  An ee.Array.
**Usage:** `ee.Array.Or(self, right: '_arg_types.Any') -> 'Array'`

===

## ee.Array.aside
Calls a function passing this object as the first argument.

Returns the object itself for chaining. Convenient when debugging. For
example:

c = (ee.ImageCollection('foo').aside(logging.info)
         .filterDate('2001-01-01', '2002-01-01').aside(logging.info)
         .filterBounds(geom).aside(logging.info)
         .aside(addToMap, {'min': 0, 'max': 142})
         .select('a', 'b'))

Args:
  func: The function to call.
  *var_args: Any extra arguments to pass to the function.

Returns:
  The same object, for chaining.
**Usage:** `ee.Array.aside(self, func: 'Any', *var_args) -> 'ComputedObject'`

===

## ee.Array.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Array.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Array.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Array.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Array.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Array.freeze(obj: 'Any') -> 'Any'`

===

## ee.Array.getInfo
Fetch and return information about this object.

Returns:
  The object can evaluate to anything.
**Usage:** `ee.Array.getInfo(self) -> 'Any | None'`

===

## ee.Array.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Array.isVariable(self) -> 'bool'`

===

## ee.Array.is_func_returning_same
**Usage:** `ee.Array.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Array.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Array.name() -> 'str'`

===

## ee.Array.serialize
Serialize this object into a JSON string.

Args:
  pretty: A flag indicating whether to pretty-print the JSON.
  for_cloud_api: Whether the encoding should be done for the Cloud API or
    the legacy API.

Returns:
  The serialized representation of this object.
**Usage:** `ee.Array.serialize(self, pretty: 'bool' = False, for_cloud_api: 'bool' = True) -> 'str'`

===

## ee.Kernel.aside
Calls a function passing this object as the first argument.

Returns the object itself for chaining. Convenient when debugging. For
example:

c = (ee.ImageCollection('foo').aside(logging.info)
         .filterDate('2001-01-01', '2002-01-01').aside(logging.info)
         .filterBounds(geom).aside(logging.info)
         .aside(addToMap, {'min': 0, 'max': 142})
         .select('a', 'b'))

Args:
  func: The function to call.
  *var_args: Any extra arguments to pass to the function.

Returns:
  The same object, for chaining.
**Usage:** `ee.Kernel.aside(self, func: 'Any', *var_args) -> 'ComputedObject'`

===

## ee.Kernel.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Kernel.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Kernel.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Kernel.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Kernel.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Kernel.freeze(obj: 'Any') -> 'Any'`

===

## ee.Kernel.getInfo
Fetch and return information about this object.

Returns:
  The object can evaluate to anything.
**Usage:** `ee.Kernel.getInfo(self) -> 'Any | None'`

===

## ee.Kernel.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Kernel.isVariable(self) -> 'bool'`

===

## ee.Kernel.is_func_returning_same
**Usage:** `ee.Kernel.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Kernel.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Kernel.name() -> 'str'`

===

## ee.Kernel.serialize
Serialize this object into a JSON string.

Args:
  pretty: A flag indicating whether to pretty-print the JSON.
  for_cloud_api: Whether the encoding should be done for the Cloud API or
    the legacy API.

Returns:
  The serialized representation of this object.
**Usage:** `ee.Kernel.serialize(self, pretty: 'bool' = False, for_cloud_api: 'bool' = True) -> 'str'`

===

## ee.Terrain.name
**Usage:** `ee.Terrain.name() -> 'str'`

===

## ee.Classifier.aside
Calls a function passing this object as the first argument.

Returns the object itself for chaining. Convenient when debugging. For
example:

c = (ee.ImageCollection('foo').aside(logging.info)
         .filterDate('2001-01-01', '2002-01-01').aside(logging.info)
         .filterBounds(geom).aside(logging.info)
         .aside(addToMap, {'min': 0, 'max': 142})
         .select('a', 'b'))

Args:
  func: The function to call.
  *var_args: Any extra arguments to pass to the function.

Returns:
  The same object, for chaining.
**Usage:** `ee.Classifier.aside(self, func: 'Any', *var_args) -> 'ComputedObject'`

===

## ee.Classifier.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Classifier.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Classifier.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Classifier.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Classifier.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Classifier.freeze(obj: 'Any') -> 'Any'`

===

## ee.Classifier.getInfo
Fetch and return information about this object.

Returns:
  The object can evaluate to anything.
**Usage:** `ee.Classifier.getInfo(self) -> 'Any | None'`

===

## ee.Classifier.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Classifier.isVariable(self) -> 'bool'`

===

## ee.Classifier.is_func_returning_same
**Usage:** `ee.Classifier.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Classifier.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Classifier.name() -> 'str'`

===

## ee.Classifier.serialize
Serialize this object into a JSON string.

Args:
  pretty: A flag indicating whether to pretty-print the JSON.
  for_cloud_api: Whether the encoding should be done for the Cloud API or
    the legacy API.

Returns:
  The serialized representation of this object.
**Usage:** `ee.Classifier.serialize(self, pretty: 'bool' = False, for_cloud_api: 'bool' = True) -> 'str'`

===

## ee.Clusterer.aside
Calls a function passing this object as the first argument.

Returns the object itself for chaining. Convenient when debugging. For
example:

c = (ee.ImageCollection('foo').aside(logging.info)
         .filterDate('2001-01-01', '2002-01-01').aside(logging.info)
         .filterBounds(geom).aside(logging.info)
         .aside(addToMap, {'min': 0, 'max': 142})
         .select('a', 'b'))

Args:
  func: The function to call.
  *var_args: Any extra arguments to pass to the function.

Returns:
  The same object, for chaining.
**Usage:** `ee.Clusterer.aside(self, func: 'Any', *var_args) -> 'ComputedObject'`

===

## ee.Clusterer.encode
Encodes the object in a format compatible with Serializer.
**Usage:** `ee.Clusterer.encode(self, encoder: 'Callable[..., Any] | None') -> 'dict[str, Any]'`

===

## ee.Clusterer.encode_cloud_value
Encodes the object as a ValueNode.

Args:
  encoder: A function that can be called to encode the components of
      an object.

Returns:
  The encoded form of the object.
**Usage:** `ee.Clusterer.encode_cloud_value(self, encoder: 'Any') -> 'dict[str, Any]'`

===

## ee.Clusterer.freeze
Freeze a list or dict so it can be hashed.
**Usage:** `ee.Clusterer.freeze(obj: 'Any') -> 'Any'`

===

## ee.Clusterer.getInfo
Fetch and return information about this object.

Returns:
  The object can evaluate to anything.
**Usage:** `ee.Clusterer.getInfo(self) -> 'Any | None'`

===

## ee.Clusterer.isVariable
Returns whether this computed object is a variable reference.
**Usage:** `ee.Clusterer.isVariable(self) -> 'bool'`

===

## ee.Clusterer.is_func_returning_same
**Usage:** `ee.Clusterer.is_func_returning_same(self, an_object: 'Any') -> 'bool'`

===

## ee.Clusterer.name
Returns the name of the object, used in __str__().
**Usage:** `ee.Clusterer.name() -> 'str'`

===

## ee.Clusterer.serialize
Serialize this object into a JSON string.

Args:
  pretty: A flag indicating whether to pretty-print the JSON.
  for_cloud_api: Whether the encoding should be done for the Cloud API or
    the legacy API.

Returns:
  The serialized representation of this object.
**Usage:** `ee.Clusterer.serialize(self, pretty: 'bool' = False, for_cloud_api: 'bool' = True) -> 'str'`

===

